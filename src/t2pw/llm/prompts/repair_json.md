The previous response contained invalid JSON. Fix it and return only the corrected JSON object.

Do not change any values, structure, or content — only fix the syntax errors (unclosed brackets,
trailing commas, unescaped characters, truncation, etc.).

Return only the valid JSON object. No explanation, no markdown fences.

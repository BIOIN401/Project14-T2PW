# H-004 — `agent/h04-durable-g11-evidence`

**Pre-Wave-A0 G11 infrastructure.** Agent type: `pwml-implementer` (isolated worktree).
Reviewer: `pwml-reviewer`. Verification: `pwml-test-runner`.
Product-owner ruling, 2026-08-05 → **D-018**. **Depends on H-003.**

---

```
[S1] [S3] [S4] [S5] [S6] [S8]

BRANCH     agent/h04-durable-g11-evidence
BASE       <CURRENT integration SHA, read with `git rev-parse sprint/pwml-recovery`
            at dispatch. Do not use a SHA copied from any document.>
WORKTREE   the one the harness gives you
WAVE       pre-A0 (G11 infrastructure)
DEPENDS    H-003 (the wrapper must reliably WRITE a report before we require one)

OWNS -- exclusive.
  docs/pwml_recovery_sprint/TEST_MATRIX.md  -- section 0 only
  docs/pwml_recovery_sprint/prompts/_SHARED_BLOCKS.md  -- block S8 only
  NEW  docs/pwml_recovery_sprint/evidence/g11/README.md  (or a path you justify)
  NEW  .gitignore rule, ONLY if needed to exclude captured stdout

  NOT YOURS:
    bounded_run.py -- H-003's. If the durable-report requirement needs a wrapper
      change, STOP and report; do not make it here.
    Any file under src/ or tests/. LEDGER.md, DECISIONS.md, MASTER_PLAN.md,
      BASELINE.md, any C-xxx or H-00{1,2,3} prompt.

THE GAP -- established by the closeout review, not inferred
  No bounded-run cleanup report is committed anywhere in the repository.
  evidence/ holds tooling and baselines only. The consequence, in the closeout
  reviewer's own words: it could prove the ledger's universal G11 claim was
  contradicted by another sprint document, but it COULD NOT ESTABLISH FROM THE
  REPOSITORY WHAT ACTUALLY HAPPENED on any individual job.

  G11 is a hard merge rule and TEST_MATRIX.md section 0 already requires a
  cleanup report on every test record. That requirement is currently
  unenforceable and unauditable after the fact: every agent writes its --json to
  a session scratchpad that is deleted, and pastes a table into a report that
  nobody can check.

OBJECTIVE
  Every future test, benchmark, pipeline leg or LLM-backed job receives a UNIQUE
  --json report path under a VERSION-CONTROLLED G11 evidence location, so that a
  G11 claim is checkable against a committed artifact rather than a pasted table.

  PROSPECTIVE ONLY. Do NOT synthesize, backfill, reconstruct or infer reports for
  historical runs that lack them. A reconstruction produced after the fact is not
  evidence of the original run, and the record already states plainly that the
  pre-A0 jobs cannot be reconstructed. Leave that statement standing.

IMPLEMENT
  1. Define the version-controlled evidence location and its naming scheme. Paths
     must be unique per job -- a collision that overwrites another job's report is
     the failure mode to design out. Include the task ID, the job label and a
     disambiguator that does not require a clock the scripts cannot call.
  2. Specify the durable report's REQUIRED minimum content. At minimum:
       - command or job identity
       - start and completion classification
       - the actual exit code
       - observed and terminated owned-process counts
       - final surviving owned-process count
       - runtime
       - report-schema version, if one exists
     Check what bounded_run.py's CleanupReport already emits (:240-300, and the
     --json writer at :696-698) and specify against THAT, not against a wish.
     If a required field is genuinely absent from the wrapper's output, say so
     and STOP rather than editing the wrapper -- that is H-003's file.
  3. Update TEST_MATRIX.md section 0 and shared block S8 so the requirement is
     stated where every dispatched prompt already reads it. S8 is pasted verbatim
     into every task prompt, so it is the enforcement point.
  4. State the credential rule: reports must be confirmed to contain NO
     credentials before committing. Command lines can carry tokens, connection
     strings and API keys. Say what an agent must check and what it must do if a
     report contains one.
  5. State the size rule: do NOT commit large captured stdout or cache contents.
     The report is a structured record, not a log dump. If the wrapper captures a
     child log alongside the report, that log stays out of version control.
  6. Make the requirement checkable: an orchestrator or reviewer must be able to
     go from "this branch claims G11" to "here is the committed report for each
     of its jobs" without asking the agent.

MUST NOT CHANGE
  - bounded_run.py. If you believe the wrapper must change, STOP and report.
  - The G11 rules themselves. You are making an existing rule auditable, not
    rewriting it. Do not weaken "final surviving count must be 0", do not
    introduce an exemption, and do not add a way to declare a job compliant
    without a report.
  - Any historical record. No backfill.

TESTS YOU MUST ADD
  a) A check that the documented required fields are ACTUALLY present in a report
     bounded_run.py produces -- generate one from a trivial wrapped command and
     assert the field set. This is what stops the specification drifting from the
     tool.
  b) A check that the naming scheme cannot collide for two jobs in the same task.

ACCEPTANCE -- all must hold
  [ ] The evidence location is version-controlled and the naming scheme is unique
      per job.
  [ ] The required minimum fields are specified AND verified present in real
      wrapper output.
  [ ] TEST_MATRIX.md section 0 and S8 both state the requirement.
  [ ] The credential check and the no-large-stdout rule are stated.
  [ ] Applied prospectively only -- zero historical reports synthesized.
  [ ] No G11 rule weakened; no exemption introduced.
  [ ] bounded_run.py untouched. Zero files under src/ or tests/.
  [ ] Cleanup report attached; final surviving process count = 0 -- and it is
      committed to the new location, as the first instance of the rule.

TRAPS
  TRAP-5  Never commit a cache modification.
  D-014   The seven protected scratch files and the three untracked topics_*.txt
          are never staged, committed, reset, restored, stashed, regenerated or
          reformatted. The sixteen cache_snapshot/ directories are never deleted.
  D-011   cache_snapshot/ exclusion is narrow and deliberate. If you add a
          .gitignore rule, make it equally narrow and say why.
```

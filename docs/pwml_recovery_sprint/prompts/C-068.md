# C-068 — the export golden covers the corpus again, by admitting one leg and excluding two on the record (F-069)

- **Base SHA** to be set at dispatch (currently `931c065` or later)
- **Branch** `agent/c068-golden-coverage`
- **Finding closed** F-069 (HIGH) — `FINDINGS.md:2050`
- **Ceiling** 1200 changed lines / 140 changed docstring+comment lines / 2 MB artifacts.

## 1. What is true, measured at `931c065`

| predicate | value |
|---|---|
| `git ls-files "*final_mapped.json"` — this is `_corpus()`, `test_c030_canonical_identity_fallback.py:87-90` | **35** |
| on-disk under `runs/` + `runs_verify/` — the coverage predicate, `test_compound_resolution_extraction.py:806-816` | **35** |
| `GOLDEN` unique keys, `test_compound_resolution_extraction.py:469` | **32** |
| stale entries in `GOLDEN` | **0** |

Missing, exactly three, all T-100 survivors under `runs_verify/2026-08-18_1328/papers/`:
`PMC12096016/research`, `PMC12096016/strict`, `PMC12452463/strict`.

**Note the two predicates are different in kind** — `_corpus()` reads `git ls-files` (tracked), the coverage
test reads `rglob` (on-disk). They agree at 35 today. Do not conflate them.

## 2. The biological adjudication — completed, binding on this card

An independent read-only bio-auditor adjudicated all three legs against
`src/t2pw/bench/gold/pinned_v1.json`. **Its verdicts are the input to this card; do not re-litigate them.**

| leg | verdict | why, in one line |
|---|---|---|
| `PMC12096016/research` | **`admit_with_recorded_defect`** | `quarantine_report.ok: true`, canonical payload, fully accessioned, contributes **0** census gap rows |
| `PMC12096016/strict` | **`do_not_admit`** | `ok: false`, `refusal_reasons: ["degree_zero_export:1"]` |
| `PMC12452463/strict` | **`do_not_admit`** | `ok: false`, `refusal_reasons: ["degree_zero_export:1"]` |

**The structural fact that governs both exclusions, and it is decisive:** on both strict legs
`quarantine_report.json -> ok` is `false`, so `final_mapped.json` is **the pre-quarantine fallback, not the
canonical payload.** Directly witnessed, not inferred — `removed_entity_report.json` on `PMC12452463/strict`
lists `2,3-dihydroxybenzoate`, `DHB-AMP`, `Fe2+`, `EntF` and `Fur` as **removed**, and **all five are still
present** in that leg's `final_mapped.json`. Corroborating markers: `"enrichment"` and `"ec_number"` each
occur **0** times in both strict payloads and `prefreeze_db_resolution` is `null`, against 10, 5 and
`{"available": true}` on the research leg.

**`GOLDEN` is the reference for `build_pwml_ir` over committed leg fixtures. Neither strict file is a payload
the exporter is ever handed on a completed run.** That is why they are excluded — not because their biology
is disliked.

**Also recorded, and both must be preserved in the exclusion reasons:** each strict leg's only structural
refusal is `strict_invariants.degree_zero_exports == [{"bucket": "proteins", "name": "Isochorismatase
(EntB)"}]` — **the exact row C-059's `REASON_ALREADY_COVERED` was written to reject** (F-075). So these two
artifacts are, in the single respect that determines their release status, the least likely of the 35 to
still reproduce.

## 3. Required behaviour

### 3a. `GOLDEN` gains exactly ONE leg

Add `runs_verify/2026-08-18_1328/papers/PMC12096016/research/final_mapped.json`.

**With an adjacent comment naming the three defects its digest records**, so no later reader mistakes the
digest for an endorsement:

1. `EntE-catalyzed adenylation of 2,3-DHB` has `outputs: ["enterobactin"]`. **Adenylation produces DHB-AMP.**
   A named enzyme is asserted to make a product it does not make.
2. `enterobactin secretion` carries `transporters: [{entity: "EntE", provenance: "inferred"}]` on a span
   reading *"secreted … by a **TolC**-dependent process"* — **F-058**.
3. The `EntF` assembly reaction lists `EntE` as a catalyst whose entire `evidence` is the reaction's own name
   concatenated with a truncated fragment — **not evidence**.

**This leg is also the subject of the newly registered F-079** (classified `release_ready` with
`semantic_evaluation: passed` despite defect 1). **Cross-reference F-079 in the comment. Do not fix F-079
here** — it is unowned and needs its own card and its own current-source measurement.

### 3b. Two legs are excluded ON THE RECORD, not by silence

**Do not leave the coverage tripwire permanently red.** A permanently-red tripwire decays into noise, which
is the second-order cost F-069 itself diagnoses.

**Do not re-point the assertion either.** REV-051's precedent binds: re-pointing so it passes in both
configurations destroys the property it guards.

**The shape that satisfies both:** an explicit `EXCLUDED` register carrying a **per-leg reason**, with the
tripwire at `:806-816` becoming `found == set(GOLDEN) | set(EXCLUDED)`. It then still fires on a genuinely
new committed leg — which is the property that matters — while recording why these two are out.

Each exclusion reason must state **both** facts from § 2:
* *quarantine refused, so this file is the pre-quarantine fallback and not an export fixture*; and
* *its only structural refusal is the trigger C-059 now rejects, so the artifact may no longer reflect
  pipeline behaviour.*

**A leg must not be addable to `EXCLUDED` without a reason string.** Make that structural, not conventional.

### 3c. ⚠ The `== 32` pin is FIVE assertions, not one

`test_the_census_reproduces_over_the_committed_corpus`
(`tests/test_c030_canonical_identity_fallback.py:172-185`) iterates `_corpus()`, which is `git ls-files` —
**not `GOLDEN`**. So these move **regardless of what `GOLDEN` does**:

| assertion | line | pinned | measured |
|---|---|---|---|
| `len(_corpus())` | `:176` | 32 | **35** |
| `len(gaps)` | `:177` | 49 | **55** |
| distinct files | `:178` | 19 | **21** |
| `buckets` | `:184` | `{compounds:38, protein_complexes:11}` | **`{compounds:43, protein_complexes:12}`** |
| `keys` | `:185` | `{pathbank_compound_id:38, pathbank_complex_id:11}` | **`{pathbank_compound_id:43, pathbank_complex_id:12}`** |

**Verify every one of these yourself before moving it.** They are the bio-auditor's measurements, not mine.

Per-leg contribution: `PMC12096016/research` **0**, `PMC12096016/strict` **1**, `PMC12452463/strict` **5**.
The six new gap rows resolve to `2,3-dihydro-2,3-dihydroxybenzoate (DHB)` (both strict legs — the RAG
duplicate), `isochorismate`, `2,3-dihydro-2,3-dihydroxybenzoate`, `2,3-dihydroxybenzoate`, and
**`enterobactin synthase complex`** — an entity the gold set forbids outright (`pinned_v1.json:2037`).

**Move all five under merge rule 4, with that exact table as the documented delta**, and record the
interpretation that makes it legible: **the census grew because two refused legs were committed, not because
identity resolution regressed.** 6 of the 6 new gap rows are duplicate-compound and forbidden-complex rows
arising from the pre-quarantine fallback.

**The only way to hold these numbers is to un-commit the legs, which would destroy the evidence base of D-055
and F-055…F-064. Do not propose it.**

## 4. ⚠ Measurement constraint — F-047, and it will silently mislead you

**`_leg_digest` must be measured UNDER PYTEST. Never by direct import.**
`tests/test_compound_resolution_extraction.py:413-419` records that an out-of-pytest harness reported
**every** IR-building leg as moved — at the base SHA as well as the tip. An out-of-pytest digest is not a
weaker measurement, it is a **wrong** one.

The bio-auditor deliberately produced **no digest value** for this reason, and also checked
`src/t2pw/pwml/ir.py:238-240` `_norm` against the duplicate compound names and found it does **not** collide
them — so **there is no basis to forecast the research leg's stops either way.** Measure them; do not predict
them.

## 5. Ownership boundary

**You own:** `tests/test_compound_resolution_extraction.py` (`GOLDEN`, the new `EXCLUDED` register, the
tripwire at `:806-816`) and `tests/test_c030_canonical_identity_fallback.py` (the five census assertions at
`:176-185`).

**You own nothing else.** Specifically:

* **`runs/` and `runs_verify/` are READ-ONLY.** Do not add, delete, move or regenerate a leg fixture. The
  committed legs are the evidence base of D-055 and F-055…F-064.
* `src/` is entirely out of bounds. **This card changes no production code** — F-069 is a fixture-and-pin
  decision, and the bio-auditor's § 4 states explicitly that nothing in the adjudication justifies a
  pipeline change.
* `src/t2pw/app/streamlit_app.py` carries an uncommitted product-owner edit. Never touch it.
* `docs/pwml_recovery_sprint/evidence/bounded_run.py` and `g11/g11_evidence.py` may belong to a concurrent
  card. Call them; never edit them.
* Do not edit control-plane markdown. Report the prose you want changed.

## 6. Proof obligations

* **Both tripwires currently fail unconditionally** (not `.env`-conditionally). **Reproduce both failures at
  your base first and paste them** — that is your G9 base evidence, and it is behavioural.
* At your tip both must pass, and you must show the tripwire **still fires** on a genuinely new leg.
  **Non-vacuity (RULING 13): synthesize a fourth uncovered leg path and prove the assertion goes red**, then
  remove it. A tripwire rewritten to `set(GOLDEN) | set(EXCLUDED)` that no longer detects anything is the
  exact failure this card exists to prevent, and it would look green.
* **Prove `EXCLUDED` cannot be used to silence a leg without a reason.**
* RULING 9 — where you claim a digest or a count is unchanged, **pin the exact set of differing keys**, not
  one field.

## 7. Process

* Every run through `bounded_run.py` with a real `--timeout` comfortably below your own tool wall clock.
* `--json` from `g11_evidence.py next --task C-068 --label <label>`, allocated immediately before use.
* `--basetemp` under `C:/t/c068tmp/`, parent pre-created, on every pytest invocation.
* Paste `FINAL SURVIVING COUNT: 0` and `cleanup: success` per job; run `g11_evidence.py check --task C-068`
  before reporting.
* **Certify chunk membership stem-exactly.** F-069 records that **both** these files are in **no chunk** and
  in **no SMOKE** — verify that yourself rather than trusting it, and say which way it came out.
* Offline and focused. **Do not touch `C:/t/heavylock`.** Never `pytest -n auto`.

## 8. Report

`_SHARED_BLOCKS.md` § S6, plus:

1. Both base tripwire failures, pasted.
2. Your five re-measured census values, and whether they matched the table in § 3c.
3. The measured `_leg_digest` and stops for the admitted leg, **with proof it was measured under pytest**.
4. The non-vacuity result for the rewritten tripwire.
5. Confirmation that `runs/`, `runs_verify/` and `src/` are untouched, with `git diff --stat`.
6. Anything you noticed and did not do.

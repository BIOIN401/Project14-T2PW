# C-010 — `agent/p01-stale-index`

Wave A0. Agent type: `pwml-implementer` (isolated worktree). Reviewer: the C-041 implementer.

---

```
[S1] [S3] [S4] [S5] [S6] [S8]

BRANCH     agent/p01-stale-index
BASE       <CURRENT integration SHA, read with `git rev-parse sprint/pwml-recovery`
            at dispatch. Do not use a SHA copied from any document.>
WORKTREE   .claude/worktrees/p01-stale-index
WAVE       A0
DEPENDS    NONE

OWNS -- exclusive. A diff outside this list is an automatic reject.
  src/t2pw/pipeline/strict_quarantine.py :: _surviving_processes,
                                            _degree_zero_exports,
                                            quarantine_and_close
  tests/test_strict_quarantine.py
  tests/test_strict_quarantine_real_artifact_replay.py
  docs/change_log.md

OBJECTIVE
  quarantine_and_close (:1736-2045) calls _drop_quarantined_processes at :1868,
  which compacts every process bucket, and THEN calls _degree_zero_exports at
  :1876, which resolves ORIGINAL admission indices against the now-compacted
  lists via _surviving_processes (:1193-1206). Out-of-range indices are silently
  skipped; in-range-but-shifted indices resolve to the WRONG ROW. Entities
  referenced only by skipped rows are falsely reported degree-zero and the run
  refuses export.

  After this patch, degree-zero is computed against an immutable pre-prune
  snapshot, and an admission record whose index cannot be resolved RAISES instead
  of being silently skipped.

  Regenerate the evidence before you start:
    .venv/Scripts/python.exe docs/pwml_recovery_sprint/evidence/repro_stale_index_synthetic.py
    .venv/Scripts/python.exe docs/pwml_recovery_sprint/evidence/allowlist_generator.py

MUST NOT CHANGE
  - Pruning inside the closure loop (:1826-1856). It is index-CORRECT because it
    runs PRE-drop. Do not touch it.
  - _revalidate_surviving_processes (:1413-1472). It ALREADY handles out-of-range
    correctly, quarantining with process_row_vanished_during_closure at
    :1449-1453. COPY that pattern; do not modify the function.
  - _reconcile_locked_reactions (:1571-1659) and write_quarantine_artifacts
    (:~1954). Both key off the `originals` snapshot and are already safe.
  - evaluate_core_coverage (:837-945). Record-based, not index-based. Untouched
    here -- it is C-041's.
  - Research mode's apply-nothing contract.
  - The _prune_entities / _prune_locations / _prune_biological_states fixpoint.

  _surviving_processes has exactly ONE unsafe call site. Audited safe: the
  closure loop, _referenced_state_names, _revalidate_surviving_processes,
  _reconcile_locked_reactions, write_quarantine_artifacts, evaluate_core_coverage.
  Do not "fix" any of them.

IMPLEMENT
  1. In quarantine_and_close, immediately before _drop_quarantined_processes,
     deep-copy the process buckets into an immutable snapshot.
  2. Add `strict: bool = False` to _surviving_processes. When True, an
     unresolvable (bucket, index) raises a NEW StrictQuarantineInvariantError.
  3. _degree_zero_exports takes the snapshot for the REFERENCE computation while
     still reading ENTITY rows from the post-drop payload, and calls
     _surviving_processes(strict=True).
  4. Export StrictQuarantineInvariantError in __all__.

TESTS YOU MUST ADD
  a) SYNTHETIC. The fixture in evidence/repro_stale_index_synthetic.py: five
     reactions, indices 0-1 quarantined on an undeclared participant, 2-4
     admitted with enzymes EntC/EntD/EntF.
     ASSERT degree_zero_exports == [] and ok is True.
     (Unpatched: ['EntC','EntD'] and ok False.)
  b) A fabricated admission record with an out-of-range index raises
     StrictQuarantineInvariantError.
  c) REGRESSION: a GENUINELY degree-zero protein is still reported.
  d) THE ALLOWLIST TEST, in test_strict_quarantine_real_artifact_replay.py.
     Tuple order: (dz_before, dz_after, ok_before, ok_after,
                   refusals_before, refusals_after)

     EXPECTED_P01_DELTAS = {
       "runs/2026-08-02_2130/papers/PMC12096016/strict":
           (["EntA","Unknown","enterobactin synthase complex"], [], False, True,
            ["degree_zero_export:3"], []),
       "runs/2026-08-02_2130/papers/PMC12856317/research":
           (["ALAS2","ALAS2 homodimer"], [], False, False,
            ["degree_zero_export:2","unexportable_entity:1"],
            ["unexportable_entity:1"]),
       "runs_verify/2026-08-04_1207/papers/PMC12452463/strict":
           (["EntE"], [], False, True, ["degree_zero_export:1"], []),
       "runs_verify/2026-08-04_1234/papers/PMC12096016/strict":
           (["EntA"], [], False, True, ["degree_zero_export:1"], []),
       "runs_verify/2026-08-04_1754/papers/PMC12452463/research":
           (["Isochorismatase (EntB)"], [], False, True,
            ["degree_zero_export:1"], []),
       "runs_verify/2026-08-04_1754/papers/PMC12452463/strict":
           (["EntD","EntF"], [], False, True, ["degree_zero_export:2"], []),
     }

     Assert THREE things, ALL required:
       1. every leg in the dict transitions exactly as tabulated;
       2. set(legs that changed) == set(EXPECTED_P01_DELTAS) -- no unlisted leg
          changed in ANY of the three observables;
       3. every changed leg's dz_after is [] -- the fix never ADDS a degree-zero.

     Measured across 32 archived legs (26 unchanged, 6 changed, 0 errors) with
     pathway_context=None, matching the archived-leg reality that no leg carries
     Stage-0 context. If your run disagrees with this table, STOP -- either the
     fix is wrong or the artifacts changed.

     NOTE: the two 1754 rows require runs_verify/2026-08-04_1754/ to be
     committed, which INIT-001 does. If your worktree lacks it, the generator
     finds 30 legs and 4 changes -- that is a checkout difference, not a
     disagreement. Skip-guard those two rows the way the module already
     skip-guards on runs/ absence; do NOT delete them.
  e) Re-pin FULL_STACK_BASELINE and RESIDUAL_CODES_BY_{LEG,ROW} to newly measured
     EXACT counts, and update docs/change_log.md with the same deltas. The two
     must not disagree.

FOCUSED TESTS  (through the bounded wrapper -- [S8])
  chunk A, chunk E   -- see TEST_MATRIX.md section Commands

ACCEPTANCE -- all must hold
  [ ] All six allowlist legs transition exactly as tabulated.
  [ ] Zero unlisted legs changed.
  [ ] No leg gains a degree-zero entity.
  [ ] PMC12856317/research remains ok=False on unexportable_entity:1 -- proof the
      fix removes only the FALSE refusal and weakens no biological gate.
  [ ] An out-of-range index raises; no silent skip remains anywhere.
  [ ] FULL_STACK_BASELINE re-pinned to exact new values; change_log matches.
  [ ] chunk A and chunk E pass.
  [ ] Cleanup report attached; final surviving process count = 0.

TRAPS
  TRAP-2  test_strict_quarantine_real_artifact_replay.py:416 pins
          FULL_STACK_BASELINE by exact equality. THIS PATCH CHANGES IT BY DESIGN.
          Do NOT make that test pass by reverting behaviour -- re-measure and
          re-pin, and document the delta in docs/change_log.md. A reviewer will
          reject a revert.
  TRAP-1  This patch makes PMC12452463 pass the quarantine boundary. Its gold
          export_rationale records the route as chemically BROKEN (EntA absent).
          Its correct outcome is review_required, NEVER strict success. Do not
          optimize toward "PMC12452463 passes strict".
  TRAP-5  Never commit a cache modification.

BUDGETS (D-019, ratified by WAVE-A0-BUDGET-RATIFICATION-001) -- limits only.
  HAND-AUTHORED: at most 300 changed lines, additions PLUS deletions, across this
    card's complete authorized implementation manifest. A MAXIMUM -- not a target,
    not an expectation that every line be used. Anything outside OWNS stays barred.
  MACHINE-GENERATED EVIDENCE, governed separately: 0 artifacts, 0 bytes.
  Overrun of either, predicted or discovered: STOP BEFORE COMMITTING and obtain
    renewed product-owner authority; no role may self-authorize one.
  SPLITTING: only where each half is independently implementable AND independently
    validatable (D-019). Exceeding a threshold does not itself authorize or require
    a split; no split is preauthorized here.
  Ratifies limits ONLY: no implementation approval, no unblocking, no branch, no
    dispatch authority.
```

# C-067 — structural refusal reasons reach the D-002 review seam (F-062)

- **Base SHA** `ac776682d36012b0b583952d78ac8f0cf02115a3`
- **Branch** `agent/c067-structural-review-seam`
- **Finding closed** F-062 (HIGH, `product_contract_violation`) — `FINDINGS.md:1487`
- **Ceiling** to be set at dispatch. **⚠ NOT DISPATCHABLE UNTIL § 2 IS DISCHARGED.**

## 1. The mechanism — re-measured at the base SHA. Every previously recorded address was stale.

F-062 cites `strict_quarantine.py:2025-2034`; the session handoff cites `:2013-2034`. **Both are wrong now.**
Measured at `ac77668`:

| What | Address |
|---|---|
| `coverage_reasons = [f"minimum_core:{reason}" …]` | `:2209-2211` |
| `structural_reasons` built, five kinds | `:2212-2222` |
| `verdict = coverage_verdict(coverage)` | `:2224` |
| `defensible_core = bool(verdict is not None and verdict.has_surviving_core)` | `:2225` |
| `review_reasons = coverage_reasons if defensible_core else []` | `:2230` |
| `refusal_reasons = ([] if defensible_core else list(coverage_reasons)) + structural_reasons` | **`:2231-2233`** |
| `would_have_refused = coverage_reasons + structural_reasons` | `:2237` |
| `"ok": True if research else not refusal_reasons` | `:2458` |
| `elif not strict_gates_passed: status = DIAGNOSTIC_ONLY` | `release_status.py:492-493` (F-062 said `:417-419` — stale) |
| `classify_release_status` | `release_status.py:425` |

**`structural_reasons` is appended to `refusal_reasons` unconditionally.** `review_reasons` receives **only**
`coverage_reasons`, and only when `defensible_core` holds. So any structural reason ⇒ `ok = false` ⇒
`diagnostic_only`, **however defensible the surviving core.**

**The comment block at `:2185-2207` is the proof this is an omission rather than a decision.** It records a
careful, explicit split *for the coverage reasons*: `COVERAGE_REASON_EMPTY` (nothing survived — no defensible
core to review, stays `diagnostic_only`) versus `COVERAGE_REASON_BELOW_MINIMUM` (a shortfall — reviewable),
with `has_surviving_core` deliberately **consumed** from `release_status.py` *"rather than re-derived, so this
seam and the classifier can never disagree about what defensible means."*

**That analysis was never performed for the five structural reasons.** They were left as a blanket
unconditional refusal. That is the defect.

## 2. ⚠ PREREQUISITE — a biological adjudication that does not yet exist

**This card must NOT be dispatched until each of the five structural reasons has an independent biological
ruling.** The five, at `:2213-2222`:

| Reason | Meaning | Prior evidence |
|---|---|---|
| `entity_type_overlap:{n}` | a row occupies two entity buckets | none yet |
| `degree_zero_export:{n}` | orphan row, no surviving edges | **F-062 argues reviewable**: *"a degree-zero orphan is a removable row, not an unsupportable biological requirement — the pruner already owns that reason code (`degree_zero_after_quarantine`) and applied it to Fur, EntF, RyhB and `enterobactin synthase` in the same run."* |
| `unexportable_entity:{n}` | cannot be serialized | **strong prior that it must keep refusing** — `:2243-2245` says it is passed as the serialization input *"because that is precisely what it means — the entity cannot be written without inventing it"* |
| `unaccounted_locked_reactions:{n}` | locked reactions unaccounted for | none yet |
| `closure_not_converged:{n}` | closure hit `max_iterations` | none yet |

**The reason a per-reason ruling is mandatory, and this is the heart of the card:**

* **Merge rule 7** says preserve incomplete-but-correct pathways as `review_required` rather than dropping
  them. That rule **supports** routing a removable-row reason to review.
* **Merge rule 6** forbids weakening a biological gate to increase PWML production. Routing a structural
  reason to `review_reasons` makes `ok = true` and lifts the leg from `diagnostic_only` to `review_required`
  — **which is, literally, increasing PWML production.**

**A blanket "route all five to review when `defensible_core`" would be a merge-rule-6 violation and will be
rejected.** The correct fix is almost certainly **narrow** — quite possibly `degree_zero_export` alone. Each
reason must be argued on its own biology, exactly as the coverage reasons were.

## 3. ⚠ The proof problem — F-075

**C-059 removed the `Isochorismatase (EntB)` row that triggered `degree_zero_export:1` on the PMC12452463
leg.** So the historically observed trigger is **gone**, while the seam is **uncorrected**.

**Do not confuse "the observed trigger disappeared" with "the refusal seam was corrected."** A card that
demonstrates PMC12452463 now reaching `review_required` has proved nothing about this seam unless it also
shows the seam is what changed.

**This almost certainly requires a synthetic fixture** that drives a structural reason with
`defensible_core = True` and no coverage reason. **Build it honestly: do not fabricate biology to
manufacture the condition.** A minimal constructed graph that exercises the code path is legitimate and
should be labelled as such; a fake pathway asserted to be real is not.

## 4. Contract status — RESOLVED THIS SESSION, and F-062's blocking qualification is discharged

F-062 carries a qualification that has blocked it for three sessions:

> *"the clause is conditioned on 'after the index fix', and that phrase occurs exactly once in the entire
> control plane, with no antecedent anywhere — `grep -rn "index fix" docs/pwml_recovery_sprint/*.md`."*

**That grep was restricted to `*.md`. The antecedent is in a committed `.py` docstring:**

* `docs/pwml_recovery_sprint/evidence/probe_downstream_gates.py:1` — *"How far a leg gets AFTER the
  **stale-index fix** — the honest limit of **C-010**."*
* same file `:5-6` — *"Fixing **the index defect (C-010)** is not the same as producing PWML."*
* same file `:122` — `print(f"  1. quarantine (index-fixed) : ok={result.ok}")`
* `LEDGER.md:114` — **C-010 = "p01 stale positional index", MERGED `9e06360`.**
* `MASTER_PLAN.md:215`, `:394` — `C-010 p01-stale-index`.

That probe exists specifically to measure PMC12452463 after C-010 and states *"It also gives T-100's
acceptance criterion its evidence base."*

**So "the index fix" = C-010, merged. The condition is satisfied, and `PRODUCT_CONTRACT.md:341` binds today:
PMC12452463's correct outcome is `review_required` with `strict_acceptance_eligible=false`, never strict
success.** F-062 may now be quoted as a live contract contradiction.

**This is a reading of existing evidence. No locked outcome was altered and no contract text may be edited by
this card.**

## 5. Ownership boundary — provisional, to be fixed at dispatch

**Expected to own:** `src/t2pw/pipeline/strict_quarantine.py` at the `:2209-2237` seam only, plus tests.

**Will NOT own:**
* `release_status.py`'s classifier — `has_surviving_core` is deliberately consumed, not re-derived
  (`:2196-2199`). **Preserve that.** If the fix seems to require changing the classifier, stop and report.
* `src/t2pw/app/streamlit_app.py` — uncommitted product-owner edit. Never touch.
* Any control-plane markdown.

**`would_have_refused` at `:2237` must keep its exact pre-D-002 meaning and ordering** — research mode
reports exactly this and is otherwise untouched. **Pin it.** RULING 9: pin the exact set of differing keys,
not one field.

## 6. Dispatch checklist — all must be true before this card goes out

- [ ] Biological ruling on **each** of the five structural reasons (§ 2)
- [ ] Synthetic-fixture design agreed, with the F-075 distinction explicit (§ 3)
- [ ] Ceiling set from the agreed scope
- [ ] Writer lane free
- [ ] Confirmed still true at dispatch: merge-rule-6 lock discharged (C-058 and C-059 accepted)

**Merge rule 6 lock: DISCHARGED.** C-058 (fabricated `EntE` transporter) and C-059 are both accepted, which
was F-062's stated blocking sequencing.

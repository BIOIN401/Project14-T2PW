# H-002 — `agent/h02-replay-payload-scope`

**Pre-Wave-A0 test-harness maintenance.** Agent type: `pwml-implementer` (isolated worktree).
Reviewer: `pwml-reviewer` (must not be the H-001 implementer).
Product-owner ruling, 2026-08-05 → **D-013**. Narrow: one assertion.

---

```
[S1] [S3] [S4] [S5] [S6] [S8]

BRANCH     agent/h02-replay-payload-scope
BASE       <CURRENT integration SHA, read with `git rev-parse sprint/pwml-recovery`
            at dispatch. Do not use a SHA copied from any document.>
WORKTREE   .claude/worktrees/h02-replay-payload-scope
WAVE       pre-A0 (harness maintenance)
DEPENDS    NONE  (may run concurrently with H-001 -- different functions, but see
                  COORDINATION below)

OWNS -- exclusive.
  tests/test_strict_quarantine_real_artifact_replay.py ::
      test_no_archived_leg_carries_stage_zero_context   (:240-257)   ONLY

  NOT YOURS: RUNS, _legs, _leg_ids, FULL_STACK_BASELINE, the residual-code
  dicts, and test_the_full_stack_baseline_is_exactly_what_was_reported -- all
  H-001's. Any file under src/.

THE DEFECT -- measured, not inferred
  The assertion globs EVERY json file under runs/:

    :250-257
      carriers = [str(path) for path in RUNS.rglob("*.json")
                  if path.stat().st_size <= _MAX_PAYLOAD_BYTES
                  and "key_compounds" in path.read_text(...)]
      assert carriers == []

  It fails on ORIGIN_SHA with 9 carriers. Classified, every one:

    files carrying key_compounds ............ 9
    all of them ............................. stage0_attempts.json
    PAYLOAD files carrying it ............... 0
    run directory ........................... all 9 in runs/2026-08-02_2130

  The test's own docstring states what it is actually about:
  "If a single archived PAYLOAD carried key_compounds, payload-only discovery
  would be defensible on real data." That claim is STILL TRUE -- no
  final_mapped.json or merged_payload.json carries key_compounds.

  What changed is that runs/2026-08-02_2130 introduced a new DIAGNOSTIC artifact,
  stage0_attempts.json, which persists Stage-0 attempt records for the first
  time. The assertion is broader than the claim it is defending.

  MASTER_PLAN section 1's premise is therefore INTACT: the C-010 allowlist was
  measured with pathway_context=None "matching the archived-leg reality that no
  leg carries Stage-0 context", and no payload does.

OBJECTIVE
  Narrow the assertion to the AUTHORITATIVE PAYLOAD FILES and exclude diagnostic
  attempt records, so the test defends its actual claim.

  After this patch: the test passes, and it still fails if a real PAYLOAD ever
  starts carrying Stage-0 context.

MUST NOT CHANGE -- explicit product-owner instruction
  - NO pathway biology. NO production behaviour. NO file under src/.
  - Do not delete or weaken the test. Narrowing scope is not the same as
    deleting the assertion, and a test that can no longer fail is a regression.
  - Do not change _MAX_PAYLOAD_BYTES or the payload-preference order.
  - Do not touch H-001's functions.
  - Do not "fix" this by deleting stage0_attempts.json from runs/. The artifacts
    are evidence; C-042 and C-014 depend on that run directory.

IMPLEMENT
  1. Scope the search to the authoritative payload files the harness treats as a
     leg's payload -- final_mapped.json and merged_payload.json, the same pair
     _payload_for (:138-144) prefers. Do not invent a different list.
  2. Exclude diagnostic attempt records explicitly and BY NAME, with a comment
     saying why: a diagnostic recording what Stage 0 attempted is not the same
     claim as a payload carrying Stage-0 context.
  3. Update the docstring so the assertion and the stated claim match. The
     current docstring is right about the CLAIM and the code was wrong about the
     SCOPE -- keep the claim, fix the scope, and say that diagnostics are
     deliberately excluded.
  4. Prefer excluding a known diagnostic FILENAME over excluding "anything that
     is not a payload". If a future diagnostic starts carrying Stage-0 context,
     the maintainer should have to think about it.

COORDINATION WITH H-001
  Same file, different functions. Whichever merges second REBASES and re-runs
  chunk E before review -- do not merge both on one review. If H-001 has already
  landed, your cohort comes from the manifest; that does not change your
  assertion, but re-run chunk E after rebasing and paste the result.

TESTS YOU MUST ADD
  a) The assertion PASSES on the current runs/ tree.
  b) It still FAILS when a payload file (final_mapped.json or
     merged_payload.json) carries key_compounds. Build a temporary fixture --
     this proves you narrowed the scope rather than defanging the test.
  c) A diagnostic file carrying key_compounds does NOT fail the assertion, and a
     test says so explicitly, so the exclusion is deliberate and visible.
  d) The 9 known stage0_attempts.json carriers are still detectable by a direct
     query -- the evidence is not lost, only excluded from THIS assertion.

FOCUSED TESTS  (through the bounded wrapper -- [S8])
  chunk E (this module, ~32 s) + your new tests, then chunk A (123, ~12 s)

ACCEPTANCE -- all must hold
  [ ] The assertion examines only authoritative payload files.
  [ ] Diagnostic attempt records are excluded BY NAME, with a stated reason.
  [ ] The test still fails if a real payload carries Stage-0 context -- proven by
      fixture, not by argument.
  [ ] Docstring and assertion now make the same claim.
  [ ] Zero files under src/ modified; no biology or production behaviour changed.
  [ ] chunk E and chunk A pass.
  [ ] Cleanup report attached; final surviving process count = 0.

TRAPS
  TRAP-4  Do not rebuild what exists.
  TRAP-5  Never commit a cache modification.
```

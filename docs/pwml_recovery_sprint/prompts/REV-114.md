# REV-114 — independent review of `agent/c114-d088-diagnostics`

**Chartered by the Lead Orchestrator, `ORCH-719`.** You review the **actual diff**, not the
implementer's report. You cannot edit code by construction, and you must not try.

---

## What this card was allowed to do

Read `docs/pwml_recovery_sprint/prompts/C-114.md` in full first. In one sentence: it adds
**diagnostic fields that nothing reads** to `strict_quarantine.evaluate_core_coverage`, so that
D-088 clauses 6, 7 and 8's populations are preserved before any future reclassification can erase
them — and it changes **no gate, no threshold and no score**.

Read these too, because they are why the card is shaped this way:

- `DECISION-PACKET-D088-RUNTIME-CAP.md` **including its § 6 amendment** — why the release-policy half
  is blocked and with the product owner.
- `DECISIONS.md` D-088, all ten clauses.
- `FINDINGS.md` F-167 **including its amendment**, F-168, F-169.

---

## The six things to verify, in descending order of how badly a miss would hurt

### 1. No score moved. This is the whole card.

**TRAP-7 is the acceptance criterion, not a caution.** Verify by reading the diff, not the report:

- `minimum_core_satisfied`, `reasons`, `coverage_ratio`, `thresholds`, `matched_terms`,
  `unmatched_terms` — **is any of them computed differently, in any branch?**
- Is any new field read by anything inside `evaluate_core_coverage`? Trace each new name to every
  use. A field that is written and then consulted, even once, even in a log line that changes
  control flow, is a **gate wearing a record's name** and is a reject.
- `src/t2pw/pipeline/release_status.py` must appear in **no hunk**. Check `git diff --stat` yourself
  against the base; do not accept the implementer's paste.

### 2. The base SHA, and the G9 classification

Base is `88b11c9c410344435f8c03e1c2428b219c4a5087`. **Verify the branch is actually based there**
(`git merge-base`), because a branch cut from the wrong tip can pass every test and still be
measuring a different tree.

**G9: this card is a NEW capability and its tests must be labelled as such.** There must be **no
fabricated base-SHA failure** and **no symbol-absence proof**. If the implementer has dressed this up
as a regression fix — asserting that the new fields are absent at base and present at tip — **that is
the mislabelling G9 calls a reject**, and it is the specific error this card is most likely to
attract, because "prove it fails at base" is the sprint's usual habit.

### 3. The census triple, and whether it was reproduced or reverse-engineered

The card pins **374 unmatched anchors / 60 subprocess-aligned / 90 payload-present** from
`evidence/f167_history_census.log`, and instructs the implementer to **stop and report** rather than
tune the rule if the numbers do not come out.

**Check the direction of causation.** Read the three matching rules in the diff and compare them to
the rules in `evidence/f167_history_census.py` and
`evidence/d088_subprocess_recall_ab.py`. The card required the census's rules *verbatim*, including
the deliberately crude `subprocess_aligned` substring test and the `standard` stoplist.

> **A rule that was adjusted until the numbers matched produces the same three numbers as a rule that
> was copied correctly.** The test passing tells you nothing here. Only reading the rule does. If the
> stoplist has gained or lost a word, or the substring test has been "improved", the number is no
> longer comparable to the committed census and the card's own premise is gone.

### 4. `schema_version`, the decision hash, and the policy version

- `schema_version` must still be `1`. The additions carry `d088_diagnostics_version`.
- `QUARANTINE_POLICY_VERSION` must be **untouched** — it IS a decision input and bumping it moves
  every `decision_id` in the corpus.
- The Lead established that `canonical_decision_inputs` (`strict_quarantine.py:249`) reads controls
  only and never the verdict, so additive keys cannot move `decision_input_hash`. **Confirm that is
  still true in the diff** — if the implementer added a key to `canonical_decision_inputs`, or made
  the verdict feed it, the whole corpus's decision ids move and it is a reject.

### 5. The four archived legs, checked by you

Re-run the byte-identity check yourself on at least:
```
runs_verify/2026-09-01_1612/papers/PMC12096016/strict
runs_verify/2026-09-01_1612/papers/PMC12782028/strict
runs_verify/2026-09-01_1612/papers/PMC12452463/strict
runs_verify/2026-08-21_2239/papers/PMC12782028/strict
```
The fourth is there on purpose: **it is the leg F-168 was registered from**, the draw where Stage 0
named only four subprocesses and never mentioned the mevalonate arm. If the implementation quietly
falls back to some other subprocess source when the context is thin, that leg is where it shows.

`PMC12452463` is there because of **TRAP-1** — its gold `export_rationale` records the route as
chemically **broken** and its correct outcome is `review_required` with
`strict_acceptance_eligible=false`, **never** strict success. Confirm nothing in this diff moves it.

### 6. G11, run properly, by evidence rather than by claim

Every job through `bounded_run.py`, real `--timeout`, `--basetemp` under `C:/t/` with the parent
pre-created, `--heavy-lock C-114`. **Open the committed JSON reports under `evidence/g11/C-114/`**
and check `FINAL SURVIVING COUNT : 0` and `cleanup : success` in the artifacts, not in the report
prose. Run `g11_evidence.py check --task C-114`. **A surviving `.staging` placeholder is a job that
produced no report** and must never have been committed.

Confirm the wrapper's recorded interpreter is the **primary checkout's** `.venv/Scripts/python.exe`.
A worktree has no `.venv`; a bare `python` is system 3.13 with no `streamlit` and gives 35 spurious
import errors that read exactly like a regression — **and would equally make a green run meaningless.**

---

## Two things you are explicitly NOT reviewing

- **Whether the runtime cap should change.** It should not, in this card. That question is with the
  product owner and an implementer who touched it has left the boundary — which is a finding for you
  to report, not a design question for you to weigh in on.
- **F-169's `ATP` matcher gap.** Deliberately not in this card. If the diff "helpfully" fixes it, that
  is **out of boundary and a reject**, however correct the fix looks — it would move Priority 5, and
  a change that improves a score needs its own card, its own auditor classification and its own
  review.

---

## What to report

For each of the six sections: **verified / not verified / could not check**, with the command you ran
and its output. Then:

- every place the diff differs from what the card authorised, however small;
- every claim in the implementer's report you could **not** confirm from the artifacts;
- your merge recommendation against merge rules 1-11, naming any rule you could not evaluate.

**Reviewer independence is the point of this role.** If you find nothing wrong, say what you checked
that could have been wrong and was not — a review that reports only "approved" is not evidence that
anything was examined.

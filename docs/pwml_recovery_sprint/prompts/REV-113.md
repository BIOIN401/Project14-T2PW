# REV-113 — independent review of C-113 (F-150 half 1 re-landed with its census movement)

- **Reviewing** `card/C-113-f150-with-census`, worktree `C:/t/c113`.
- **You are not the author and you do not fix the diff.**
- **Criteria fixed and committed BEFORE the diff existed.** Later additions must be labelled as
  additions.
- **Review the ACTUAL DIFF, not the report.**

---

## 0. The crux

**A card that re-pins a census to make its own change green is the highest-risk shape in this
sprint.** The pin exists to catch a regression; moving it is exactly what a regression would want.
The only thing separating a legitimate move from a laundered one is **whether the new number was
MEASURED and ATTRIBUTED, or fitted to whatever the failure message printed.**

**Your central question is not "is SMOKE green?" It is "would this pin still catch the thing it was
written to catch?"**

---

## 1. What is already settled and is NOT reopened

**REV-F150's verdict stands: VERIFIED — APPLY HALF 1**, eight conditions, independent reviewer,
diff inspected by the Lead. **Do not re-review the gold edit's merits.** If you believe it is wrong,
that is an escalation to the Lead, not a finding against this card.

**T-107 is immutable.** Verdict `NOT ACCEPTED`. Not re-run, not re-scored, not reinterpreted. **A
number that moves because the gold changed is an instrument change and must never be reported as a
pipeline regression or improvement.**

**Half 2 is not proposed.** It is preserved unanswered in `DECISION-PACKET-F150-HALF2.md`. If the
diff sets `supported_reactions_complete` on anything, that is an **immediate REJECT**.

---

## 2. Blocking criteria

| # | Criterion | How |
|---|---|---|
| **X1** | **The gold bytes are the REVIEWED bytes** | The landed change must be exactly `ac27ed7b`'s: two strings into `PMC12180156.forbidden_identifiers[0].aliases`, nothing else, no threshold moved. Verify the post-edit gold blob is **`36f4b7b690b577f72882c3045ca6728d1ec8d9d1`**. A retyped-but-equivalent edit is acceptable only if the blob matches; **if it does not, find out why before anything else.** |
| **X2** | **The gold correction is on its OWN commit** | Separate from the test re-pin, so either can be dropped independently. A single commit mixing gold and tests fails X2. |
| **X3** | **Every moved pin carries a PER-LEG attribution** | `26 → 29` must name **which three legs**, in which run trees, and why. `affected_papers` must name which legs put `PMC12180156` in. **A number without an attribution is a fitted pin — REJECT it.** The file's own existing comments set the standard; hold the diff to it. |
| **X4** | **`withheld` was MEASURED, not assumed** | `test_10`'s `assert withheld == 97` never executed at base, because `:422` aborts first. The Lead deliberately did **not** supply this value. **Confirm the author measured it.** "It still passes" is only acceptable if they demonstrate they checked, not if the test merely went green. |
| **X5** | **The measurement is INDEPENDENT of the failing assert** | A probe that walks the 72 legs and reports leg paths — not numbers read off a pytest failure message. **Reading the expected value out of the error text and pasting it in is the failure mode this criterion exists to catch.** |
| **X6** | **No assert was relaxed** | Every one of these stays `==` against a **literal**. Any `>=`, range, tolerance, computed expression or `in {...}` is an **immediate REJECT**. The file says why: a `>=` lets a leg join the corpus and go unvisited — *"a quiet vacuity of exactly the F-144 shape."* |
| **X7** | **`assert legs == 72` is UNTOUCHED** | The leg census did not move and must not be edited. If the diff touches it, ask what the author thought they were fixing. |
| **X8** | **Comments were updated with the numbers** | A moved pin with a stale comment explaining the old value is worse than no comment. The diff must also **state that the author confirmed the new legs were meant to be there, and how** — the file demands that confirmation explicitly. |
| **X9** | **SMOKE is 503 / exit 0 at the tip, WITH the gold edit in place** | This is the gate the card exists to satisfy. Also require `acceptance.py` **byte-identical** before and after (`4bd893ac410d16d3…` throughout this wave) and gold-readers **456 / 8 / exit 0** **with its gold SHA stated**. |
| **X10** | **`test_c102_coverage_denominator.py` was run ALONE as well as inside SMOKE** | A file that only ever runs inside a 22-file selection can hide an ordering dependency. |
| **X11** | **G9 labelled honestly** | The gold correction is a **correction of observable behaviour** with a real committed base failure. The census re-pin is a **deliberate pinned-baseline move under merge rule 4's second clause** — **NOT a new capability.** Mislabelling it is a reject. |
| **X12** | **Boundary held** | `pinned_v1.json` (two aliases), `test_c102_coverage_denominator.py` (pins and comments), `FINDINGS.md` (F-161 only), probe and logs. **Any scorer change, any `acceptance.py` change, any biological gate, any third test file touched to make a number work → REJECT.** |
| **X13** | **Job lifecycle** | `FINAL SURVIVING COUNT : 0`, `cleanup : success`, shape-validated G11 path, lock released, probe **and** log committed beside the report. |

---

## 3. The adversarial check — do this one properly

**X3 and X5 are satisfiable on paper by a sufficiently confident narrative. Do not accept a
narrative.**

**Re-derive at least one of the three new matched-forbidden legs yourself**, from the committed
artifacts, and confirm it is what the diff says it is. If the author's attribution names three legs,
check that those three legs exist, that they belong to `PMC12180156`, and that they carry a matched
forbidden term **under the post-edit gold and not under the pre-edit gold**.

**If you cannot re-derive it, that is a finding**, and it is a blocking one — because the entire
legitimacy of moving these pins rests on the attribution being true.

**Then mutate.** Revert the gold edit alone in a scratch copy and confirm the re-pinned test goes
**RED**. A re-pin that stays green with the gold edit removed has pinned nothing and X6's protection
is already gone.

---

## 4. F-161

The card registers it: **`tests/test_c102_coverage_denominator.py` is in SMOKE but not in the
22-file gold-readers selection**, so REV-F150's four-step A/B was structurally blind to the two
tests the edit moved and reported a byte-identical `456 / 8 / exit 0` in both arms.

**Check the framing, not just the presence.** It must be recorded as a defect in the **review
instrument**, not in the reviewer — the reviewer did exactly what its criteria asked. If the entry
blames the reviewer, say so.

**The question of whether to extend the gold-readers selection must be RAISED, not answered.** If
the diff wires a new file into a selection or a chunk, that is out of boundary — a `TEST_MATRIX`
change with its own cost.

If C-112 had not merged when the author reached `FINDINGS.md`, holding the entry for the Lead to
sequence is **correct behaviour, not an omission.** Do not mark it down for that.

---

## 5. Stop conditions

Any relaxed assert · a pin without per-leg attribution · `withheld` assumed · the gold blob not
matching `36f4b7b6…` · gold and tests in one commit · a third file edited to make a number work ·
`supported_reactions_complete` set · any scorer or biological-gate change · SMOKE not 503 at the tip.

---

## 6. Verdict

**APPROVE** · **APPROVE WITH REGISTERED RESIDUALS** · **CHANGES REQUIRED** · **REJECT**

Two automatic correction rounds; a **third is an explicit authority decision** and comes back to the
Lead. **If what remains is registerable, say APPROVE and register it.**

**State separately what you verified on the diff and what you verified behaviourally.** A static
read and a mutation-proved property are different epistemic objects.

**R9 and R10 are inherited residuals and are NOT this card's to fix** — the `reason` prose is
outside the authorized edit, and the redundant Unicode alias was in the ratified proposal. **If the
diff "tidies" either of them, that is scope creep and should be reported as such.**

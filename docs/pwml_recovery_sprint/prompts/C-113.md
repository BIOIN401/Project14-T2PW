# C-113 — re-land F-150 half 1 together with the census movement it causes

- **Card** C-113 · **Finding** F-150 half 1, plus **F-161** (below) · **Class** `gold_data_defect`
  correction, plus a **deliberate pinned-baseline move under merge rule 4's second clause**
- **Branch** `card/C-113-f150-with-census` · **Worktree** `C:/t/c113`
- **No biological gate is in scope and none may be touched.**

---

## 0. Why this card exists, and what it is NOT

**The gold edit is already verified.** REV-F150 returned **VERIFIED — APPLY HALF 1**, all eight
conditions, by an independent reviewer who did not author it. The Lead inspected the diff itself.
**Nothing about that verdict is reopened here and you are not re-reviewing it.**

What went wrong was **landing it without its full footprint.** The merge went in at `b05a7281`,
SMOKE came back **501 passed / 2 failed**, and merge rule 10 required the merge not to stand. It was
reverted at `700c9434` and integration re-proved green at **503 / exit 0**.

**Your job is to land the same edit WITH the census re-pin it requires, as one reviewed change.**

---

## 1. The A/B is already done for you — do not re-derive it

Same 22 files, same tree, same interpreter, same basetemp parent. One difference:

```
WITH the gold edit     b05a7281   501 passed / 2 failed   exit 1
WITHOUT the gold edit  700c9434   503 passed / 0 failed   exit 0
```

`evidence/orch718_smoke22_postf150.log` and `evidence/orch718_smoke22_postrevert.log`,
G11 `ORCH-718/04` and `/05`. **The two failures are caused by the gold edit and by nothing else.
That is measured, not inferred from a verdict.**

Both failures are in **`tests/test_c102_coverage_denominator.py`**:

```
test_10_f132_population_regression_over_the_six_papers          :422
    assert set(affected_papers) - set(F132_PAPERS) == {"PMC13231680"}
    got {'PMC12180156', 'PMC13231680'}

test_13_the_accepted_rate_is_a_rate_on_every_committed_leg      :549
    assert with_matched_forbidden == 26
    got 29
```

**`assert legs == 72` PASSED.** The leg census itself is unmoved. What moved is which papers show
forbidden-identifier effects, and how many legs carry a matched forbidden term.

---

## 2. The exact edit to re-land

`src/t2pw/bench/gold/pinned_v1.json`, case `PMC12180156`, `forbidden_identifiers[0].aliases`: add
`"delta-aminolevulinic acid"` and `"δ-aminolevulinic acid"`. **Add nothing else. Move no threshold.**

It exists, already reviewed, as commit **`ac27ed7b` on branch `review/REV-F150`** — cherry-pick it
rather than retyping it, so the landed bytes are provably the reviewed bytes. Pre-edit gold blob
`aee8cb4f1da3d417f36206407867585622b741c0` → post `36f4b7b690b577f72882c3045ca6728d1ec8d9d1`.

**Keep the gold correction on its own commit, separate from the test re-pin.**

---

## 3. The census re-pin — measure it, do not fit it

**This is the part that needs care, and it is the part where a card of this shape usually goes
wrong.** The temptation is to run the tests, read the numbers off the failure messages, and paste
them in. **Do not do that.** A pin fitted to whatever the test happened to print is a pin that will
absorb the next real regression silently.

**Required, per pin:**

1. **Measure the population independently of the failing assert** — a probe that walks the same 72
   legs and reports *which* legs newly carry a matched forbidden term, by leg path, not just how
   many.
2. **Show the arithmetic.** `26 → 29` must be explained as *these three specific legs, in these run
   trees, for this reason.* Follow the style the file already uses for its own pins: *"23 → 26 is
   the 3 matched-forbidden legs `runs_verify/2026-08-28_1816` contributes under commit `e77ad3d`;
   the other thirteen runs still sum to 23."* **Match that standard of attribution.**
3. **Same for `affected_papers`**: state which legs put `PMC12180156` into the set.

### One value is deliberately NOT given to you, and you must measure it

**`test_10`'s `assert withheld == 97` is UNKNOWN.** The assert at `:422` aborts the test before
`withheld` is ever checked, so the Lead does not know whether it also moves. **Measure it. Do not
assume it held, and do not assume it moved.** If it moves, it needs the same attribution as the
other two.

### What the file itself requires of you

Read the comments beside both pins before touching either. They are explicit, and they bind:

> *This stays `==` and must NOT be relaxed to `>=`. … A `>=` here would let a leg join the corpus
> and go unvisited — a quiet vacuity of exactly the F-144 shape. Moving it by hand whenever a run is
> committed is the correct cost: that is the moment someone confirms the new legs were meant to be
> there.*

**Relaxing any of these asserts to `>=`, or to a range, or to a computed expression, is an
immediate REJECT.** They stay `==` against a literal. **You are that confirmation step** — say, in
the diff's own comment, that you confirmed the new legs were meant to be there, and how.

**Update the comments beside the pins, not just the numbers.** A moved pin with a stale comment
explaining the old number is worse than no comment.

---

## 4. F-161 — register it; it is why this card exists rather than a footnote

**REV-F150's mandated gate was the 22-file gold-readers selection.
`tests/test_c102_coverage_denominator.py` is in SMOKE but NOT in that selection.** So the four-step
A/B was **structurally blind** to the two tests the edit actually moved, and returned a
byte-identical `456 / 8 / exit 0` in both arms while a real consequence sat one selection away.

**This is the exact mirror of the standing lesson that SMOKE does not cover the gold readers.
Neither selection is a superset of the other. A gold edit needs BOTH.**

The reviewer did what its criteria asked. **The criteria were incomplete.** Record it that way —
this is a defect in the review instrument, not in the reviewer.

**Write F-161 into `FINDINGS.md`.** Note that C-112 also edits `FINDINGS.md` this wave; if C-112 has
not yet merged when you reach this step, **say so and hold the FINDINGS entry for the Lead to
sequence** rather than creating a conflict. Everything else in this card proceeds regardless.

**Raise, do not answer:** whether the gold-readers selection should be extended to include every
SMOKE file that reads the gold. That is a `TEST_MATRIX` change with its own cost and it is not
yours to make.

---

## 5. Gates — and the one that actually decides this card

- **SMOKE 503 / 22 files, exit 0.** **This is the gate that decides C-113.** A green SMOKE at your
  tip *with* the gold edit in place is the whole point of the card.
- **gold-readers 456 passed / 8 skipped / exit 0** — expected unchanged, since it was byte-identical
  in both arms before. **State the gold SHA you measured against.**
- **`acceptance.py` hashed immediately before and after, byte-identical.** It was `4bd893ac410d16d3…`
  throughout this wave. **C-113 has no business changing the scorer.**
- Run `tests/test_c102_coverage_denominator.py` **alone as well as inside SMOKE.**

**COMMIT BEFORE RUNNING SMOKE.** `test_04_restore_is_byte_exact_on_the_real_mutated_module` asserts
`git status --porcelain`, and an uncommitted guarded file presents as **502/1**, which reads exactly
like a regression and is not one.

---

## 6. G9

The gold correction is a **correction of observable behaviour** and its base failure already exists
and is committed: `forbidden_match('delta-aminolevulinic acid')` returns `None` at base and matches
at tip.

**The census re-pin is NOT a new capability and must not be labelled as one.** It is a
**deliberate pinned-baseline move under merge rule 4's second clause**, and it is legitimate *only*
with the exact documented delta § 3 requires. **Mislabelling it is a reject.**

---

## 7. Boundary and stop conditions

**Files:** `src/t2pw/bench/gold/pinned_v1.json` (the two aliases only) ·
`tests/test_c102_coverage_denominator.py` (the pins and their comments only) · `FINDINGS.md`
(F-161, subject to § 4) · your probe and logs under `evidence/`.

**Stop conditions, each of them:**

- Any assert relaxed from `==` to `>=` or to a computed expression.
- A pin changed without a per-leg attribution.
- `withheld` assumed rather than measured.
- Any change to `acceptance.py`, to any scorer, or to any biological gate.
- Any second gold field touched, or `supported_reactions_complete` set on anything —
  **half 2 is not proposed and is not yours.** It is preserved unanswered in
  `DECISION-PACKET-F150-HALF2.md`.
- SMOKE not 503 at your tip. **If you cannot get 503 WITH the gold edit and an honestly measured
  re-pin, STOP and report — do not reach for a third file to make the number work.**

---

## 8. Process

Everything through `evidence/bounded_run.py` with the explicit venv interpreter
(`<primary>/.venv/Scripts/python.exe`; **a bare `python` is system 3.13 with no `streamlit` → 35
spurious import errors that read exactly like a regression**), a real `--timeout`, `--basetemp`
under `C:/t/` **with the parent pre-created**, `PYTHONPATH=<tree>/src`, `T2PW_OFFLINE_CURATOR=1`,
`PYTHONIOENCODING=utf-8`, `--heavy-lock C-113`, shape-validated G11 path.
**`FINAL SURVIVING COUNT : 0` and `cleanup : success` on every job.** Exit 95 means the child never
started — wait and retry; **never clear a lock you do not hold.**

**F-160 binds you: purge only `src/t2pw` and `tests`** — an unscoped purge deletes **56 tracked
`.pyc` files**. Restore what you purge and verify clean before SMOKE.

**Never commit** the caches, `topics_*.txt`, `streamlit_app.py`, `cache_snapshot/`, the stray
`ValueError`, or anything under `out/`, `outputs/`, `tmp/`. **Commit the probe and its log, not just
the G11 report.**

---

## 9. Two residuals inherited from REV-F150 — registered, NOT yours to fix

- **R9.** The entry's `reason` prose says *"zero occurrences in the entire 67,304-character file"*,
  which is **not literally true of the ASCII alias** — that token sequence occurs inside the ALAS2
  enzyme name the same case quotes. **No behavioural exposure:** `forbidden_match` refuses
  containment, so the acceptable enzyme survives and `PMC12856317`'s bare `aminolevulinic acid` is
  untouched. The schema carries one `reason` per **entry**, not per alias. **Amending it is outside
  the authorized edit. Leave it.**
- **R10.** The two added strings normalize to **one** alias — `_GREEK` already maps the Greek letter
  to `delta`, so both fold to the same key. The Unicode spelling is **redundant** given the ASCII
  one. It was in the ratified proposal, it is harmless, and it is recorded so a later reader does
  not read redundancy as a mistake. **Do not remove it.**

# C-091 — an interaction confers identity but never a participant role

**Card type:** production correction — **the cofactor-participation predicate only**.
**Finding:** **F-128** (`LEDGER.md`). **Authorising ruling:** **D-069 (LOCKED, 2026-08-25)**,
§ "Required follow-up", which directed the measurement and pre-authorised this correction if the
measurement returned a violation. **It did: 12 of 18.**
**Base SHA:** set at dispatch.
**Branch:** `card/C-091-d069-interaction-identity`

> ## ⚠ THIS CARD IS CHARTERED BUT MUST NOT MERGE YET
>
> Complying with D-069 **raises acceptance priority 1**, which is **absolute**. The gold's
> `forbidden_identifiers` list and D-069 currently point in opposite directions and the sprint cannot
> satisfy both. **That conflict needs a product ruling, and it has not been given.**
>
> Build it, prove it, report the exact delta — **and stop.** The orchestrator will not merge it until
> the gold-mechanism question is ruled. Do not treat the rising priority-1 count as a bug in your
> work; **it is the deliverable.**

---

## 1. Read first

1. `DECISIONS.md` § **D-069** — the ruling, in full. It is the specification.
2. `LEDGER.md` § **F-128** — the measurement, the seam, and the 12/6 split.
3. `DECISIONS.md` § **D-064** — the identity ruling this sits beside.
4. `PRODUCT_CONTRACT.md` § 2 and § 8.
5. `_SHARED_BLOCKS.md` — S1, S3, S4, S5, S6, S7, **S8**.

## 2. What D-069 refused, and what it ratified

**NOT ratified** — C-081's inherited blanket statement:

> ~~An interaction endpoint does not license a database identity.~~

**Ratified instead:**

* an interaction endpoint does **not, by itself**, prove participation in a reaction or transport;
* a **source-supported** interaction may still establish that the entity **exists**, and may support
  its **database identity**;
* interaction evidence **must not promote** an entity into a reaction-participant role;
* an **interaction-only cofactor may retain identity** when the paper explicitly supports the
  interaction **and** the entity kind is valid;
* **unsupported or merely inferred** interaction endpoints must not acquire real identifiers.

**The distinction is existence/identity versus participant role.** Do not blur it.

## 3. The seam — already located, do not re-derive it

| what | where |
|---|---|
| the refusal | `mapping/map_ids.py:8133` `_admit_identities`, **PASS C at `:8309-8352`** |
| the call | `map_ids.py:8330-8339` → `_withhold_identity(..., rule=RULE_COFACTOR_ROLE_UNUSED)` |
| the predicate | `mapping/identity_admission.py:725` `cofactor_participation` (`unsupported` at `:756`) |
| **the hinge to change** | `identity_admission.py:653-661` — the comment and the omission asserting that `interactions` is *deliberately absent* because *"an interaction endpoint is exactly the evidence that does NOT make it one"* |

**That comment states exactly the blanket rule D-069 refused.** Correcting it is the card. **But the
role half of it is RIGHT and stays right**: an interaction still must not make an entity a reaction
participant.

**PASS C ordering is load-bearing and proven so by mutation** — it must still run **last**. C-081's
reviewer built a reorder mutant and it went red on C-073's untouched assertion at `:597`. Do not
reorder the passes.

## 4. D-069's five required deliverables — all five are mandatory

1. **Valid interaction-only identity cases** — the 12. Chief among them: `PMC12856317/strict`
   `Pyridoxal 5'-phosphate`, an interaction endpoint with `ALAS2`, evidence *"a PLP-dependent
   homodimer enzyme"*, stated verbatim in `01_source_text.txt` (PLP ×22). It must **retain identity**.
2. **Unsupported interaction cases** — an interaction endpoint whose support is absent or inferred
   must **still be refused**.
3. **Reaction-participant non-promotion** — a row rescued for identity must **not** thereby become a
   reaction participant, gain a role, or enter the connected core. **Test this explicitly.** It is
   the half of C-081's rule D-069 kept.
4. **Legitimate ATP/cofactor preservation** — `ATP` declares the cofactor role ten times corpus-wide
   and is used by a reaction every time. It is untouched today and must stay untouched.
5. **Corpus-wide collateral reporting** — the full delta over all 92 artifacts: which rows regain
   identity, which stay refused, and **the exact resulting priority-1 count**.

## 5. The six that must STAY refused

They are correct for a reason D-069 endorses: **they are not interaction endpoints at all.** Those
payloads' interactions name different entities (`DHNA inhibits MenD`, `2,3-DHB inhibits EntB`,
`heme inhibits ALAS2`). An unused row with no interaction to lean on gets nothing from D-069.

Three — `NADH`, `NAD+`, `thiamine pyrophosphate` on `PMC12096016` — are additionally the
assay-reporter class `PRODUCT_CONTRACT` § 2 forbids **by name**. If your change rescues any of those
three you have violated the contract, and the contract outranks this card.

## 6. Do NOT use the gold's forbidden-name list as your acceptance oracle

This is the trap that will make a correct card look like a no-op.

The gold's `forbidden_identifiers` is matched **by name and is bucket-blind**
(`goldset.py:428-445`, consumed at `semantic.py:1029-1046`). But the gold's own `kind` for these
entries is **`cofactor_as_protein`**, which `goldset.py:314-316` defines as *"a small molecule filed
under `entities.proteins`"* — **and all 18 rows are in `entities.compounds`.** The gold's stated
failure mode obtains for none of them.

**Score against D-069's text and the papers' own sentences, not against the gold's name list.** A
card scored against that list will measure 0 collateral and conclude nothing changed, which is
exactly how C-081 arrived at a true-but-misleading zero.

## 7. Ownership boundary

**You own:** `src/t2pw/mapping/identity_admission.py` — `cofactor_participation` (`:725`),
`PARTICIPANT_FIELDS` (`:662-668`) **read-only if C-089 has landed** (see § 8), and the
`:653-661` rationale comment — plus PASS C's block in `src/t2pw/mapping/map_ids.py:8309-8352`, plus
new tests.

**Forbidden:** `src/t2pw/app/streamlit_app.py`; **`src/t2pw/bench/gold/` and
`src/t2pw/bench/goldset.py` — the gold half of this conflict is a PRODUCT RULING, not your card. Do
not edit gold to make your numbers look better. That is precisely what this sprint exists to
prevent**; `src/t2pw/pipeline/release_status.py`; caches, `out/`, `tmp/`, `outputs/`, `topics*.txt`.

## 8. Sequencing — binding

**C-089 owns the participant-schema constants and merges first.** After it lands,
`PARTICIPANT_FIELDS` is a **derived view** over `pipeline/participant_schema.PARTICIPANT_SLOTS`.
Take a **read-only dependency** on it.

Your change is about **which buckets confer a ROLE versus which confer only EXISTENCE AND IDENTITY**.
C-089's is about **what a participant key and slot are**. Keep them apart. If you find yourself
editing C-089's constants, stop and report.

## 9. G9

This is a **correction of pre-existing observable behaviour** — production refuses identity to rows
D-069 says may keep it, today, on committed artifacts. A base-failing behavioural proof is
constructible from **real committed data**: at base, `PMC12856317/strict` `Pyridoxal 5'-phosphate`
ships `ids={}`; at your tip it must ship its accessions. **Symbol absence is not proof.**

**Required tests** — one per D-069 deliverable in § 4, plus:
* the six correct refusals still refused, named individually;
* the three assay-reporter rows still refused;
* PASS C still runs last (assert the ordering property, do not just rely on it);
* the 2 PASS B kind-conflict refusals unchanged;
* **non-vacuity proof** for each.

## 10. Test-process lifecycle — S8, HARD

Every command through `bounded_run.py`: real `--timeout`; **fresh** path from
`g11_evidence.py next --task C-091 --label <label>` (last line printed, never hand-formed);
`PYTHONPATH=<worktree>/src`; unique `--basetemp` with its **parent created first** (F-114);
`--basetemp` mandatory; never `pytest -n auto`; never the full suite unchunked. Confirm
`FINAL SURVIVING COUNT : 0` and `cleanup : success` after **every** job.

Base arms run in a git **worktree** at the base SHA, never an export (D-040 § 9) — ask the
orchestrator to create it. Pin every base run with `pinned_pytest.py --expect-tree` and a committed
`--pin-verdict` (**D-066**).

**Forbidden:** `taskkill /IM python.exe`, `pkill python`, killing by name, unbounded Python,
detached jobs. The two `ms-python.isort` processes are the product owner's IDE and are never targets.

## 11. Credits and report

Offline only — no live leg, no LLM-backed command. The 92-artifact corpus is committed; replay it.

S6 format, `git commit -F`, G11 reports committed with the branch. **Do not merge, and do not ask to
merge.**

**`## RISK` must state, as a number:** the priority-1 count before and after your change, and the
list of rows that moved. That number is the input to the product ruling this card is waiting on.

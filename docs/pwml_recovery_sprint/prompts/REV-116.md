# REV-116 — independent review of `agent/c116-d088-diagnostics-artifact`

**Chartered by the Lead Orchestrator, `ORCH-719`.** You review the **actual diff**, not the
implementer's report. You cannot edit code and must not try.

**Read first:** `prompts/C-116.md`, `RULING-C114-DISPOSITION.md`, `DECISIONS.md` D-088,
`FINDINGS.md` F-167 + amendment, F-168, F-169 + amendment, F-170, F-171.

---

## The single thing this card exists to get right

C-114 failed review for **adding keys to the coverage verdict**. C-116's entire premise is that the
verdict is **byte-identical to base** and the diagnostics live in their own artifact.

**So the whole review turns on one question: are `test_c011_freeze_seam_golden_equivalence` (8) and
`test_c074_strict_core_floor` (31) green BECAUSE THE VERDICT IS UNCHANGED, or because something was
amended, excluded or special-cased?**

Check it in this order, and do not take the first answer:

1. **Neither file may appear in the diff.** `git diff --name-only` against the base.
2. **Run both yourself** at the tip. 8 and 31.
3. **Then prove the verdict is unchanged independently of those tests** — replay
   `evaluate_core_coverage` on the four named archived legs and compare canonical JSON against the
   **archived `coverage_summary.json`**, and assert there is no sixteenth key. A test passing and a
   document being byte-identical are different claims; the card asserts the second.
4. **Look specifically for a smuggled exclusion**: a `to_dict` that drops keys, a serializer that
   filters, a `__eq__` override, a key whose name starts with `_`, a field excluded from
   `dataclasses.asdict`. Any of these makes the tests pass while the object *has* grown. That is the
   failure mode this review exists to catch, and it would look exactly like success.

---

## The other five checks

**5. The anti-drift construction is the ruling's binding requirement, not a nice-to-have.** Confirm:
diagnostics computed in the **same call** from the same inputs; written by the **same function**
(`write_quarantine_artifacts`, `:2692`) so *"verdict written, diagnostics absent"* is unreachable; the
document carries `unmatched_terms` **verbatim**; a test asserts agreement on **all 83 archived legs**.
If the write happens anywhere else, drift becomes possible and the ruling's justification is gone.

**6. The census rules must be PORTED, not rewritten.** Read them against
`evidence/f167_history_census.py` and `evidence/d088_subprocess_recall_ab.py`. The stoplist is **48
words**; the token rule is a `[^a-z0-9]+` split with `len >= 3`; `subprocess_aligned` is the crude
blob rule **including the `" | "` joiner**. **374 / 60 / 90 must reproduce.** A rule adjusted until
the numbers matched produces the same numbers as a rule copied correctly — **only reading it
distinguishes them.**

**7. No gold identity in `src/`.** C-114 failed exactly here: a docstring naming `PMC12782028`.
`tests/test_c074_strict_core_floor.py:1727` rejects any **added** line in `release_status.py` or
`strict_quarantine.py` carrying a PMC id, gold `paper_id` or gold `requested_pathway`, and its own
docstring calls it **the product owner's explicit rule**. Confirm the F-168 rationale is written
without naming a paper.

**8. The rule marker, and whether it tells the truth.** The document must carry `diagnostics_rule`,
and the `in_payload` docstring must say it is a **census-rule result**, not a claim the entity was
never extracted. **The case that makes this necessary:** on `PMC12096016/strict` the rule records
`{"term":"ATP","in_payload":false}`, and F-169's amendment — confirmed by an independent bio-auditor —
establishes **both halves are false**. The rule is kept deliberately for comparability. **Judge
whether the wording actually protects a future reader, or merely gestures at it.** D-088 clause 7
protects evidence, and a preserved falsehood is not evidence.

**9. Nothing new is read by any gate.** Trace every added name to every use. A field written and then
consulted — even once, even in something that changes control flow — is *"a second gate wearing a
record's name"* (C-056c). Confirm `release_status.py` is in no hunk, `QUARANTINE_POLICY_VERSION` is
untouched, `schema_version` is unchanged, and `decision_identifier` is unmoved (recompute on an
archived leg in **both** trees; `canonical_decision_inputs` at `:249` reads controls only).

**10. G11, from the artifacts and not the prose.** Open the committed reports under
`evidence/g11/C-116/`; check `FINAL SURVIVING COUNT : 0` and `cleanup : success` **in the JSON**. Run
`check --task C-116`. **A surviving `.staging` placeholder is a job that produced no report.** Confirm
the recorded interpreter is the **primary checkout's** `.venv/Scripts/python.exe`.

---

## Environment — getting this wrong produces false regressions

- Worktrees have **no `.venv`**. Use `C:\Users\Angad\Desktop\SummerBIOIN\Project14-T2PW\.venv\Scripts\python.exe`.
- **`pinned_pytest.py` exits 98 without `PYTHONPATH`** → `T2PW_MEASUREMENT_TREE_REFUSED`, zero tests
  run. Not a failure of the patch.
- Everything via `bounded_run.py`, real `--timeout`, `--heavy-lock REV-116`, `--basetemp` under
  `C:/t/` with the **parent pre-created**, `PYTHONIOENCODING=utf-8`, `PYTHONDONTWRITEBYTECODE=1`,
  `T2PW_OFFLINE_CURATOR=1`.
- **Put any lock-waiting job in the background and branch on exit 95**; a foreground retry loop gets
  killed by a 120 s cap *while holding the lock*. This happened to me today.
- **Never batch these four into one pytest process — they stall it silently for 40 minutes:**
  `test_batch_preflight`, `test_c055_rag_loop_wiring`, `test_streamlit_quarantine_boundary`,
  `test_c052_prefreeze_report_at_the_streamlit_seams`. Chunk D's authoritative gate is
  `chunk_d_gate.py`. This also happened to me today.
- Allocate `--json` with `g11_evidence.py next --task REV-116 --label <l>`; it prints **two lines and
  the path is the last one**. Guard on the **shape of the path**, never on absence of error text.
- Never `taskkill /IM python.exe`. `ms-python.isort` servers are never cleanup targets — match on
  **command line**, never count or PID.

---

## Report

Verified / not verified / could not check per numbered item, with commands and output. Then: anything
in the diff the card did not authorise; every implementer claim you could not confirm from artifacts;
and your merge recommendation against rules 1–11, naming any you could not evaluate.

**Say what you checked that could have been wrong and was not.** A review reporting only "approved" is
not evidence that anything was examined — and on this card the most valuable possible finding is a
mechanism that makes the two pinned tests pass without the verdict actually being unchanged.

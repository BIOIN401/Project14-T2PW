# REV-118 — independent review of the complete C-118 diff

You review the **actual diff**, not the implementer's report. You cannot edit code and
must not try. Your output is a verdict.

**Branch:** `agent/c118-nominalized-conversion` · **worktree:** `C:/t/c118`
**Base SHA:** `70b6d7d2c7e7bc0e5f33095f31fbf01fd985dfd1` · pristine base tree at `C:/t/c118base`

**Read first:** `D-095` and `D-095a` (end of `DECISIONS.md` on `sprint/pwml-recovery`),
`prompts/C-118.md`, and `ORCH-724-ADMISSION-AUDIT.md`.

---

## What the card was allowed to do

ONE template appended to `_EXTRA_PROSE_PATTERNS` in `src/t2pw/rag/admission.py`, reading
the nominalized `conversion of X to Y` whose catalyst follows in an attached
`catalyzed by …`. Plus `tests/test_rag_relation_recall.py`, and — under `D-095a`, **for
re-pinning only** — `tests/test_rag_multi_relation_spans.py`. Four evidence files under
`docs/pwml_recovery_sprint/evidence/` are accepted by `D-095a`.

**Anything else in `src/` is a boundary violation and a reject.**

## The claims you must verify yourself, not accept

The orchestrator already checked the first two; **check them again independently** — two
agreeing checks are the point.

1. **`_PROSE_PATTERNS` byte-identical** between base and tip. Expected sha256 over the
   region `fea7cc2dd2393224`, 5,677 chars. Derive it yourself.
2. **No previously-admitted candidate broke.** In `c118_golden_delta_tip.json`,
   `changed_not_rejected` must be empty. **Do not trust the JSON** — re-run
   `evidence/c118_golden_delta.py` at base and at tip and confirm you reproduce
   115 / 1,885 / 5 and 249 / 1,751 / 10. A golden you did not regenerate is a claim.
3. **The five NEW admitted claims are each stated word for word by their own span.** Read
   all ten distinct rows. Any admission whose chemistry is not in its own span is a
   reject.
4. **`G9`.** The proof must **fail behaviourally on `70b6d7d2`** and pass at the tip.
   Symbol absence is not proof. Reproduce both directions; do not read the reports only.
5. **The re-pinned assertion is STRICTLY SHARPER, not loosened.** `D-095a` forbids
   softening `test_the_delta_is_five_paper_verbatim_reactions` into a loop that tolerates
   a failure. Confirm the restated property really is stronger, that the single not-ok
   entry (`PMC12452463/research/rejected[190]`) is named with its before/after reasons and
   asserted specifically rather than excluded, and that **no test function was added,
   removed, renamed or reordered** in that file.

## The safety controls — go past the writer on every one

`F-179` is the one that matters. Verify, by running it rather than by reading the test:

- **`condensation of glycine and succinyl-CoA to produce …` still yields no relation.**
  This is the glycine→heme shape. Check it is unreachable **by construction** (the head
  noun is the literal `conversion`, not the `_NOMINALIZATION_RE` family) and not merely by
  template ordering — reorder the tuple in a scratch copy and confirm it still refuses.
- `… is not catalyzed by …`, `No conversion of … was observed`, `If … were catalyzed by …`
  all refused. **Try to defeat `_NOT_NEGATED`** — it is a bounded literal lookbehind list,
  so probe the shapes it does not cover and report what you find, whether or not it
  changes the verdict.
- **An anaphoric actor yields no catalyst**, verified through the real `_clean_actor` and
  the real `validate_evidence_span`, not the raw regex.
- **The catalyst cannot be invented**: a span naming no catalyst must not produce one, and
  a candidate naming a catalyst the span does not attach must still be refused.
- **Template order**: `catalyzes_to` must still win on
  `HepPPS catalyzes the conversion of FPP to …`. Confirm the new template is genuinely
  last and claims only constructions no earlier one claimed.

Also probe what the writer did **not**: construct adversarial spans of your own where the
new template might over-read — a `conversion of A to B` whose `catalyzed by` belongs to a
neighbouring clause; a nested parenthetical; a span where `rhs` could swallow a compartment
or a cofactor. Report anything that reads chemistry the sentence does not state.

## Gates

Re-run rather than quote: focused `test_rag_relation_recall.py`,
`test_rag_multi_relation_spans.py`, the affected set (23 files), and SMOKE (expect **508**;
`TEST_MATRIX.md` records 503 and that drift is **pre-existing** — confirm it at base before
calling it anything).

Every run through the bounded wrapper, `PYTHONPATH` set, `--basetemp` with a pre-created
parent, one heavy job at a time, `FINAL SURVIVING COUNT : 0` and `cleanup : success` on
each. Your reports go under `docs/pwml_recovery_sprint/evidence/g11/REV-118/`; allocate a
path only when a job is ready to start — an abandoned reservation stub is a G11 violation.

## Verdict

End with exactly one bare word on its own line: **`APPROVE`** or **`REJECT`**.

`APPROVE` only if the diff is inside its boundary, `F-179` is not weakened, the golden move
is safe and reproduced by you, the re-pin is sharper rather than looser, and `G9` holds
behaviourally.

If you `REJECT`, say precisely what must change. If you find something real that does not
rise to a reject, list it as a finding and still give the bare verdict. **Do not fix
anything yourself.**

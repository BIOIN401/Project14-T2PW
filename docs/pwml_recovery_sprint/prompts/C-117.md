# C-117 — four pieces of guidance text that went stale while the assertions beside them stayed right

**Chartered by the Lead Orchestrator, 2026-09-02, `ORCH-719`.** Closes R-C115-1 … R-C115-4.

**None of these can fail a gate, and that is exactly why they are worth a card.** A stale assertion
turns red and gets fixed. **Stale guidance stays green and misdirects the next agent** — and one of
these four is the instrument that two separate failure messages *instruct* an agent to go and use.

---

```
[S1] [S3] [S4] [S5] [S6] [S8]

BRANCH        agent/c117-stale-guidance-text
BASE          <the integration tip at dispatch — the Lead gives it to you; do not guess>
WORKTREE      .claude/worktrees/c117-stale-text   (worktrees have NO .venv -- use the
              PRIMARY checkout's .venv/Scripts/python.exe by ABSOLUTE PATH)
DEPENDS ON    C-115 (MERGED — its re-pin is what made three of these four stale)

OWNS — exclusive. A diff outside this list is an automatic reject.
  docs/pwml_recovery_sprint/evidence/orch717_census_probe.py   :: its printed labels only
  docs/pwml_recovery_sprint/evidence/c102_mutation_attack.py   :: its baseline-precondition
                                                                  diagnostic text only
  tests/test_c106_mutation_harness_executable.py               :: the failure MESSAGE at ~:301 only
  tests/test_c102_coverage_denominator.py                      :: the stale C-113 comment at ~:446 only

OBJECTIVE
  Four texts describe a corpus and a pin set that no longer exist. Every assertion beside
  them is correct and every measurement they print is computed live and correct. Only the
  human-readable parts are wrong.

  After this patch the four texts describe the current state, and NO assertion, threshold,
  measurement, count or control flow anywhere has changed.

  Falsifiable: `git diff` shows only string literals and comments; the c102 + c106 suites
  and SMOKE are byte-for-byte the same result as base (30 passed / 503 passed).

MUST NOT CHANGE
  Any assertion, any computed value, any control flow, any threshold, any pin. If you find
    yourself editing something that is not a comment or a string literal, STOP -- you have
    left the card.
  The five census pins C-115 set (83 / 83 / 109 / 83 / 33). They are CORRECT. This card
    updates text that DESCRIBES them; it does not touch them.
  src/ entirely. The gold set. QUARANTINE_POLICY_VERSION.

IMPLEMENT — re-measure before you write any number. Do not copy numbers from this card.
  Run evidence/orch717_census_probe.py yourself and use ITS output. The whole point of this
  card is that copied numbers go stale; do not add a fifth instance.

  R-C115-3  evidence/orch717_census_probe.py — HIGHEST VALUE, DO THIS ONE FIRST.
     It prints frozen labels `(pinned == 62)`, `(pinned == 92)`, `(pinned == 23)` and
     `P6 (pinned == {'PMC13231680'})`, all frozen at ORCH-717 and now THREE re-pins stale.
     P6's label is flatly contradicted by the assertion it claims to mirror (the live
     answer is two papers). Its MEASUREMENTS are live and correct -- only the parenthetical
     reminders are wrong.
     THIS IS THE INSTRUMENT test_c106's own failure message tells an agent to use, and
     C-115's card told its implementer to use. An agent following that instruction is
     handed stale reference values by the tool it was sent to for correct ones.
     Prefer a form that CANNOT go stale again -- e.g. print the live value and the pin it
     was compared against, read from the source, rather than a hardcoded reminder.

  R-C115-1  evidence/c102_mutation_attack.py — the baseline-precondition diagnostic prints
     `withheld == 97` and `matched-forb == 26`. Those were ALREADY two re-pins stale before
     C-115 and are now three. Its actionable instruction ("re-measure with
     orch717_census_probe.py") is correct and stays.

  R-C115-2  tests/test_c106_mutation_harness_executable.py ~:301 — the failure message says
     "FOUR pins move together, not one" and then lists FIVE. C-115's card had to carry a
     dedicated trap (TRAP-B) because of this miscount. Fix the word; change nothing else in
     the file. This file is otherwise NOT yours.

  R-C115-4  tests/test_c102_coverage_denominator.py ~:446 — a C-113 attribution comment
     reads "NO LEG JOINED THE CORPUS -- `legs` above is unmoved at 72." Accurate as history,
     FALSE read in present tense now that `legs` is 83. Preserve its historical meaning --
     mark it as C-113's record rather than deleting it. It sits in the C-113 block beside
     the `set(affected_papers)` assertion, NOT beside any of the five pins.

TESTS YOU MUST ADD
  NONE. G9 CLASSIFICATION: this changes no observable behaviour and adds no capability. It
  is a documentation correction. There is no base failure to reproduce and you must NOT
  fabricate one -- G9 says symbol absence is not proof and mislabelling is a reject. Your
  evidence is the before/after equality of the test results below.

FOCUSED TESTS TO RUN
  Through evidence/bounded_run.py, PRIMARY checkout's interpreter, real --timeout,
  --heavy-lock C-117, --basetemp under C:/t/ with the parent PRE-CREATED,
  PYTHONPATH=<your worktree>/src, PYTHONIOENCODING=utf-8, PYTHONDONTWRITEBYTECODE=1,
  T2PW_OFFLINE_CURATOR=1.
  *** pinned_pytest.py EXITS 98 WITHOUT PYTHONPATH -- zero tests run, and it is not your bug. ***
  Allocate --json via g11_evidence.py next --task C-117 --label <l>; it prints TWO lines and
  THE PATH IS THE LAST LINE; guard on the SHAPE of the path, never on absence of error text.
  If another agent holds the heavy lock you get exit 95, the child never starts, and nothing
  is removed -- just wait and re-run. Do NOT clear the lock and do NOT use --heavy-lock-path.

    1. tests/test_c102_coverage_denominator.py + tests/test_c106_mutation_harness_executable.py
       -> MUST be 30 passed, identical to base
    2. evidence/c102_mutation_attack.py  -> MUST still report all 8 mutations detected
    3. evidence/orch717_census_probe.py  -> paste its full output; it is your source of truth
    4. SMOKE (22 files)                  -> MUST be 503 passed, exit 0
  Every job: FINAL SURVIVING COUNT : 0 and cleanup : success.

  *** Do NOT batch these four files into one pytest process; they stall it silently for 40
      minutes: test_batch_preflight, test_c055_rag_loop_wiring,
      test_streamlit_quarantine_boundary, test_c052_prefreeze_report_at_the_streamlit_seams. ***

ACCEPTANCE — all must hold
  [ ] `git diff` contains ONLY comments and string literals. No assertion, no number used in
      a comparison, no control flow.
  [ ] The five census pins are untouched.
  [ ] 30 passed on the two census files; 503 passed on SMOKE; mutation attack still 8/8.
  [ ] Every number you wrote came from a run of orch717_census_probe.py that you paste.
  [ ] src/ in no hunk. Gold blob 36f4b7b690b577f72882c3045ca6728d1ec8d9d1.
  [ ] G11 reports AND .pin.json verdicts committed; check --task C-117 clean; no .staging residue.

DECLARED BUDGETS (D-019)
  HAND-AUTHORED   : four files, <= 60 added, <= 40 deleted TOTAL across all four.
  MACHINE EVIDENCE: <= 10 artifacts and <= 40 KB, COMPRISING up to 5 bounded_run cleanup
                    reports AND their up to 5 .pin.json verdicts. The pin verdicts ARE
                    counted and ARE mandatory under TEST_MATRIX section 0 rule 10.
                    Committed run artifacts: 0.

TRAPS
TRAP-A  (THIS CARD) THE TEMPTATION IS TO FIX THE ASSERTION TOO. Every assertion near these
        texts is CORRECT. If a test fails after your edit you have changed behaviour, not
        text -- revert and report.
TRAP-B  (THIS CARD) DO NOT HARDCODE A FRESH NUMBER WHERE YOU CAN PRINT A LIVE ONE. Four
        stale texts is what hardcoding produced. A fifth hardcoded reminder is this card
        failing forward.
TRAP-3  placeholder_backed_proteins is a standing POLICY DISAGREEMENT. Escalate, never fix.
TRAP-5  Never commit caches, topics_*.txt, the stray 0-byte `ValueError` or `=`,
        streamlit_app.py, or anything under out/, outputs/, tmp/.
```

---

## Why this is a card and not a chore

Three of these four sit on the **remediation path for a failure that has now recurred twice** (F-151,
then F-171). `test_c106`'s failure message tells the next agent to re-measure with
`orch717_census_probe.py`; that probe then prints reference values three re-pins out of date. **The
tool an agent is sent to for correct numbers hands it wrong ones.** That is not cosmetic — it is a
trap laid across the exact route the sprint's own instrumentation directs traffic down.

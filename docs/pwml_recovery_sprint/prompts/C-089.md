# C-089 — one participant schema, two readers reconciled to it

**Card type:** production + scorer — **participant key list and slot map only**.
**Findings:** **F-119** and **F-125**.
**Authorising instruction:** product owner, Ruling 7 — *"define or reuse one canonical
participant-key source; have both readers consume it; avoid duplicating another narrower tuple."*
**Design brief:** REV-086, 2026-08-25 (`evidence/g11/REV-086/`). **Its measurements are given
below — reproduce them as tests, do not re-derive them as a study.**
**Base SHA:** set at dispatch.
**Branch:** `card/C-089-participant-schema`

---

## 1. Read first

1. `_SHARED_BLOCKS.md` — S1, S3, S4, S5, S6, S7, **S8**.
2. `LEDGER.md` § "F-119", § "F-125", and § "F-128" (the sequencing note at its end binds you).
3. `PRODUCT_CONTRACT.md` § 5 (canonical payload) and § 13.

**A LEDGER citation is stale and will waste your time:** F-119 cites
`identity_admission.py:676`. It is **`:672`** at this tip. F-125's citations (`semantic.py:1413-1445`,
`:216-220`) are correct.

## 2. Ruling 7 says "reuse one canonical source". The obvious candidate is WRONG — read this before you start

`canonical.py:330-331` holds:

```python
_PARTICIPANT_NAME_KEYS = ("entity", "compound", "protein", "protein_complex", "nucleic_acid",
                          "element_collection", "name")
```

It is private, unexported, has one consumer (`canonical.py:344`), **and it omits `element`** —
`ProcessParticipantModel.element` (`payload_models.py:333`) and `ElementWithStateModel.element`
(`:347`). Measured over 390 committed payload files: **416 dict-shaped participant entries carry
`element` and nothing else**, all in `transports.elements_with_states`. Reconciling to
`canonical.py:330` verbatim would leave every one of them unreadable — including the slot where all
three of F-125's invisible orphans live.

**The correct reconciliation target is the `payload_models.py` union — 8 name-bearing keys:**

```
name · entity · compound · protein · protein_complex · element · element_collection · nucleic_acid
```

`ref` and `id`, which `identity_admission` currently carries, appear in **no** model. Keep them as
an **additive legacy tail** — do not drop them, and do not promote them into the union.

## 3. What you are building

**A new stdlib-only leaf module: `src/t2pw/pipeline/participant_schema.py`.**

Import direction is already `bench → mapping → pipeline`, and both readers can reach `pipeline`
today (`semantic.py` already imports `pipeline.entity_identity`, `pipeline.export_mode`,
`pipeline.release_status`; `mapping` already imports several `pipeline` modules). **No cycle, no new
layer crossing.**

**Two things it must NOT be:**
* **not** in `payload_models.py` — that would drag pydantic into `identity_admission`, whose
  docstring (`:117-119`) makes stdlib-only purity a design promise;
* **not** in `canonical.py` — that would make `mapping` and `bench` import the XML/SBML parser to
  read a tuple.

**It must export TWO constants, not one.** Fixing the key tuple alone leaves both readers blind to
`elements_with_states`, which is exactly where F-125's orphans are:

1. `PARTICIPANT_NAME_KEYS` — the 8-key union, plus the legacy `ref`/`id` tail.
2. `PARTICIPANT_SLOTS` — the per-bucket entity-reference slot map (§ 4).

Then make these **derived views, not new tuples**: `identity_admission.PARTICIPANT_FIELDS` (`:662`),
`semantic._REACTION_BUCKETS` (`:164`), `semantic._enzyme_names` (`:216`).

## 4. The complete slot map — with four corrections to what the code currently believes

From `payload_models.py`. **Entity-name-bearing slots only.**

**`ReactionModel` (`:355-366`)** — `inputs`, `outputs`, `enzymes`, `modifiers`. Already fully
covered by `_orphaned_references`.

**`TransportModel` (`:381-390`)** — `cargo` (str), `transporters` (`List[ActorModel]`),
`elements_with_states[].element`.
**Correction: `modifiers` does NOT exist on this model**, though `PARTICIPANT_FIELDS["transports"]`
lists it.

**`ReactionCoupledTransportModel` (`:369-378`)** — `modifiers`, `enzymes`,
`elements_with_states[].element`.
**Correction: `inputs`, `outputs`, `cargo` and `transporters` do NOT exist on this model**, though
`PARTICIPANT_FIELDS` lists all four. F-119's "fields that do not exist" is confirmed.
Its `reaction` and `transport` (str) reference **process names, not entity names** — a dangling one
is a different orphan class. **Do not wire them into the entity registry.** Say in your report that
you saw them and left them alone.

**`catalysts`** (`semantic.py:218`) exists in no model — harmless legacy, keep it in the tail.

## 5. A fourth gap you must decide DELIBERATELY, not by accident

`processes.interactions` is **not** in `_REACTION_BUCKETS` (`semantic.py:164`), so priority 3 sees
none of `entity_1` / `entity_2` / `participants`. Measured: **36 interaction-endpoint names across
committed `final_mapped.json` reference no declared entity.**

**This is already-owned territory** — `pipeline/reference_repair.py:9-12` documents that exact
ALAS2 case and `process_normalizer.validate_registry_references` (`:4129`, interactions at
`:4184-4194`) already gates it in-pipeline.

**Default: do NOT add `interactions` to the bench-side bucket set in this card.** If you believe it
belongs, say so in your report with your reasoning and leave it out. Letting it drift in as a side
effect of the reconciliation is a reject. **This also intersects D-069 / F-128 — see § 9.**

## 6. Ownership boundary

**You own:**
* `src/t2pw/pipeline/participant_schema.py` (new),
* `src/t2pw/mapping/identity_admission.py` — `_PARTICIPANT_NAME_KEYS` (`:672`), `PARTICIPANT_FIELDS`
  (`:662-668`), `_collect_participant_names` (`:681`), `reaction_participant_names` (`:700`),
* `src/t2pw/bench/semantic.py` — `_orphaned_references` (`:1413-1445`), `_names` (`:190-213`),
  `_enzyme_names` (`:216-220`), `_REACTION_BUCKETS` (`:164`),
* new tests under `tests/`.

**Nine OTHER readers hold their own participant-key tuples** (`canonical.py:330` and `:393`,
`map_ids.py:6606`, `:4440`, `:4451`, `process_normalizer.py:1959`, `draft_graph.py:108`,
`focused_repair.py:120`/`:134`, `entity_admission.py:196`, plus four in `curation/` and
`enrich_entities.py:1477`). **No two agree. You are NOT chartered to reconcile them.** Touching them
is a boundary breach. **List them in `## OUT OF SCOPE`** so the residual is named rather than
inherited by proximity.

**Forbidden:** `src/t2pw/app/streamlit_app.py` (product-owner owned); `src/t2pw/mapping/map_ids.py`
(C-086's, and C-091's next); `src/t2pw/pipeline/release_status.py` (C-087's);
`src/t2pw/bench/gold/`; caches, `out/`, `tmp/`, `outputs/`, `topics*.txt`.

## 7. G9 — BOTH halves are CORRECTIONS. Do not mislabel either as new capability.

**This is the single most likely way to fail this card.** F-119's *corpus* exposure is 0, and it is
tempting to call it new capability. **That is wrong and G9 names it an outright reject**
("mislabelling a regression as new functionality").

**F-119 — a base-failing behavioural proof IS constructible, and REV-086 built one.** Twelve
schema-legal payloads, each with a reaction or transport that **genuinely uses** the cofactor, run
through today's real `_admit_identities`:

```
REFUSED  reactions.modifiers            [{'protein': N}]
REFUSED  reactions.modifiers            [{'protein_complex': N}]
REFUSED  reactions.enzymes              [{'protein': N}]
REFUSED  reactions.inputs               [{'compound': N}]
REFUSED  reactions.outputs              [{'compound': N}]
REFUSED  reactions.inputs               [{'element': N}]
REFUSED  reactions.inputs               [{'nucleic_acid': N}]
REFUSED  reactions.inputs               [{'element_collection': N}]
REFUSED  transports.elements_with_states            [{'element': N}]
REFUSED  transports.transporters                    [{'protein': N}]
REFUSED  reaction_coupled_transports.elements_with_states [{'element': N}]
REFUSED  reaction_coupled_transports.enzymes        [{'protein': N}]
kept     CONTROL reactions.modifiers  [{'entity': N}]
kept     CONTROL reactions.inputs     ['<bare string>']

12/14 false refusals at tip; both controls correct
```

Each strips `{chebi, kegg, hmdb}` from a row a reaction demonstrably uses — contradicting PASS C's
**own** stated rule at `map_ids.py:8334-8338`. **G9 requires the proof to fail behaviourally on the
base SHA; it does not require the input to come from a committed artifact.**

**Two things you MUST state plainly, or a reviewer will look for a corpus delta, fail to find one,
and call your proof fabricated:**
1. the base-failing input is **synthetic and schema-legal**, and is labelled as such;
2. **corpus exposure is 0 and the fix changes no committed artifact's outcome** — replaying all 92
   artifacts with a schema-complete reader rescues **0 of the 18** PASS C refusals, independently
   reproducing the C-081 reviewer's result.

**F-125 — base-failing proof from REAL committed data.** `_orphaned_references` returns **3** today
where a schema-complete reader returns **6**. `ERR_ORPHANED_REFERENCES`
(`semantic_production.py:518`) is the observable that moves.

**The three invisible orphans, fixture-ready — lift them, do not synthesise them:**

| # | artifact | pointer | name |
|---|---|---|---|
| 1 | `runs/2026-08-02_2130/papers/PMC12096016/strict/final_mapped.json` | `/processes/transports/0/transporters` | `EntE` |
| 2 | same file | `/processes/transports/1/transporters` | `EntE` |
| 3 | `runs_verify/2026-08-24_1402/papers/PMC12180156/research/final_mapped.json` | `/processes/transports/0/transporters` | **`/entities/proteins/0`** — a leaked JSON pointer in a name slot |

**Use #1 and #2 as a paired fixture:** the *same* name `EntE` is caught in `reactions.enzymes` and
missed in `transports.transporters` **in the same artifact**, which demonstrates the gap is the slot
list and nothing else.

`elements_with_states` contributes **0** orphans on this corpus — its fix is latent, like F-119's.
Say so rather than implying you closed something measurable there.

## 8. Required tests

1. Every one of the 12 F-119 shapes above → identity **retained** (base-failing, labelled synthetic).
2. Both F-119 controls unchanged.
3. F-125: the 3 real orphans are **found** (base-failing, real data).
4. The paired `EntE` fixture — caught in one slot, missed in the other, at base.
5. **Anti-widening:** the 18 PASS C refusals are still refused. **This card must not change priority
   1.** If it does, stop and report.
6. `PARTICIPANT_SLOTS` names no field absent from `payload_models.py` — a schema-conformance test,
   so the four corrections in § 4 cannot silently regress.
7. **Non-vacuity proof** for each.

**SMOKE must stay 473** and `--verify-plan` must stay `OK` with ten `[pinned_override]`.

## 9. Sequencing — binding

**C-091 (D-069 / F-128) also edits `identity_admission.PARTICIPANT_FIELDS`.** You are **first** and
you own the constants; C-091 takes a read-only dependency on them. **Do not implement any part of
D-069's interaction-identity rule.** Specifically: **do not add `interactions` to
`PARTICIPANT_FIELDS`**, and do not change which buckets confer a participant role. Your card is
about *what a participant key and slot are*; C-091 is about *which buckets confer a role*. Keeping
them apart is why this is one clean card instead of a collision.

## 10. Test-process lifecycle — S8, HARD

Every command through `bounded_run.py`: real `--timeout`; **fresh** path from
`g11_evidence.py next --task C-089 --label <label>` (last line printed, never hand-formed);
`PYTHONPATH=<worktree>/src`; unique `--basetemp` with its **parent created first** (F-114);
`--basetemp` mandatory; never `pytest -n auto`; never the full suite unchunked. Confirm
`FINAL SURVIVING COUNT : 0` and `cleanup : success` after **every** job.

**Base-tree arms run in a git WORKTREE at the base SHA — never a `c045b_base_tree.py` export**
(D-040 § 9). Ask me for the base worktree; I create it. Pin every base run with `pinned_pytest.py`
`--expect-tree` and a committed `--pin-verdict` (**D-066**) — an unpinned base run is not evidence.

**Forbidden:** `taskkill /IM python.exe`, `pkill python`, killing by name, unbounded Python,
detached jobs. The two `ms-python.isort` processes are the product owner's IDE and are never targets.

## 11. Credits and report

Offline only — no live leg, no LLM-backed command. S6 format, `git commit -F`, G11 reports committed
with the branch, **do not merge**. In `## RISK`: name every consumer of the three constants you
converted to derived views, and state what would break if one of the nine unreconciled readers
disagrees with your union at runtime.

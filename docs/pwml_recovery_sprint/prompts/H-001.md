# H-001 — `agent/h01-baseline-manifest`

**Pre-Wave-A0 test-harness maintenance.** Agent type: `pwml-implementer` (isolated worktree).
Reviewer: `pwml-reviewer` (must not be the H-002 implementer).
Product-owner ruling, 2026-08-05 → **D-012**. **Blocks C-010 and all of Wave A0.**

---

```
[S1] [S3] [S4] [S5] [S6] [S8]

BRANCH     agent/h01-baseline-manifest
BASE       <CURRENT integration SHA, read with `git rev-parse sprint/pwml-recovery`
            at dispatch. Do not use a SHA copied from any document.>
WORKTREE   .claude/worktrees/h01-baseline-manifest
WAVE       pre-A0 (harness maintenance)
DEPENDS    NONE

OWNS -- exclusive.
  tests/test_strict_quarantine_real_artifact_replay.py :: RUNS, _legs, _leg_ids,
       FULL_STACK_BASELINE, RESIDUAL_CODES_BY_LEG, RESIDUAL_CODES_BY_ROW,
       test_the_full_stack_baseline_is_exactly_what_was_reported
  NEW  tests/data/baseline_cohort_manifest.json   (or a path you justify)
  docs/change_log.md  (the baseline table only -- it must never disagree)

  NOT YOURS:
    test_no_archived_leg_carries_stage_zero_context  -> H-002. Do not touch it.
    Any file under src/. This branch changes NO production behaviour whatsoever.

THE DEFECT -- measured, not inferred
  The merge gate pins a baseline cohort discovered by a FILESYSTEM GLOB:

    tests/test_strict_quarantine_real_artifact_replay.py:104   RUNS = ROOT / "runs"
    :117-131  _legs()  -> sorted(RUNS.glob("*/papers/*/*")), keeping any leg with a
              final_mapped.json or merged_payload.json <= _MAX_PAYLOAD_BYTES (2,000,000)
    :384-393  FULL_STACK_BASELINE pinned by exact equality, asserted at :432

  It ALREADY FAILS on ORIGIN_SHA -- 23 legs pinned, 39 measured -- and the sprint
  did not cause it:
    git diff ORIGIN_SHA..HEAD -- runs/ <this test>   ->  EMPTY
    git status --porcelain runs/                     ->  EMPTY (754 files, clean)
  The pin was written at 404cc8d (2026-08-01). runs/2026-08-02_2130 added 16 legs
  at 5f2cd2f (2026-08-04) and nobody re-measured.

  The deeper problem is not the number. Because the cohort is a glob, EVERY
  milestone benchmark T-100..T-105 archives a new run directory and silently
  redefines the merge gate that is supposed to be guarding the sprint.

OBJECTIVE
  Replace the dynamic cohort with an explicit, version-controlled manifest of the
  39 currently intended pre-implementation legs. The gate reads ONLY the manifest.

  After this patch: creating a new run directory cannot change what the merge
  gate measures, and a leg enters the baseline only through a reviewed manifest
  edit.

IMPLEMENT
  1. Freeze the manifest from the measured cohort. A verified reference is
     already committed:
       docs/pwml_recovery_sprint/evidence/baseline_cohort_measured.json
       docs/pwml_recovery_sprint/evidence/baseline_cohort_measure.py
     That reference was produced by importing THIS TEST'S OWN _legs and
     _full_stack, so the population matches by construction.

     DO NOT COPY IT ON TRUST. Regenerate it and confirm you get the same 39 legs
     and the same aggregates. If you do not, STOP and report -- something moved
     under runs/ and the freeze must not proceed until that is explained.

  2. VERIFY EVERY MANIFEST ENTRY BEFORE FREEZING IT. For each of the 39: the path
     exists, it is tracked in git, and the payload file the harness would read is
     present and within the size bound. An entry that cannot be verified does not
     go in the manifest.

  3. The manifest is data, not code: leg path plus whatever identity the harness
     needs. Include a schema version and a human-readable note saying what the
     cohort IS ("pre-implementation baseline, frozen <date>") and that additions
     require review.

  4. Point _legs / _leg_ids at the manifest. A manifest entry that is MISSING
     from disk must FAIL LOUDLY -- never silently shrink the cohort. Silent
     shrinkage is how the current gate broke.

  5. Record the measured expectations for the fixed cohort:

       FULL_STACK_BASELINE = {
           "legs_examined": 39, "quarantine_admitted": 27, "quarantine_refused": 12,
           "stage3_after_pass": 27, "required_contract_pass": 8,
           "reached_ir": 8, "ir_pass": 8, "exportable": 8,
       }
       RESIDUAL_CODES_BY_LEG = {
           "no_biological_states": 4,
           "species_missing_classification": 19, "species_missing_taxonomy": 19,
       }
       RESIDUAL_CODES_BY_ROW = {
           "no_biological_states": 4,
           "species_missing_classification": 27, "species_missing_taxonomy": 27,
       }

     These are the values YOU must reproduce, not values to paste. If your
     measurement disagrees, report the disagreement -- do not adjust either side
     to match.

  6. Update the docs/change_log.md baseline table in the same commit. The
     existing text says "17 legs, 19 rows" (:147); the module's own error message
     requires both to move together and never disagree.

  7. PRESERVE C-010'S EXPECTED DELTA SEPARATELY. C-010's six-leg allowlist lives
     in BASELINE.md section 6 and is a DIFFERENT population -- it globs runs/ AND
     runs_verify/ (32 legs), while this gate reads runs/ only. Exactly TWO of the
     six fall inside this cohort:
         runs/2026-08-02_2130/papers/PMC12096016/strict
         runs/2026-08-02_2130/papers/PMC12856317/research
     Do not merge the two populations, do not pre-apply C-010's delta, and do not
     let the manifest be read as C-010's allowlist. Add a comment at the manifest
     and at the baseline saying which is which -- the whole point of this ruling
     is that the two deltas must never be confused.

MUST NOT CHANGE
  - Any file under src/. Zero production behaviour changes in this branch.
  - _MAX_PAYLOAD_BYTES (:109) and the payload-preference order
    (final_mapped.json then merged_payload.json). Changing either changes the
    cohort you are trying to freeze.
  - _full_stack and the per-leg parameterized tests. You are changing WHICH legs
    are measured and WHAT the totals are -- not HOW a leg is measured.
  - test_no_archived_leg_carries_stage_zero_context -- H-002 owns it.

TESTS YOU MUST ADD
  a) A new run directory appearing under runs/ does NOT change the cohort.
     Create a temporary fixture directory and assert the manifest-driven cohort
     is identical. THIS IS THE POINT OF THE BRANCH -- if only one test lands,
     this is it.
  b) A manifest entry whose leg is missing from disk FAILS LOUDLY, naming the
     entry. Assert the failure, not just that a count dropped.
  c) The manifest has exactly 39 entries, no duplicates, and every entry resolves.
  d) The baseline gate passes with the recorded expectations.
  e) docs/change_log.md and the module's pinned values agree -- assert it, so
     they cannot drift apart again.

FOCUSED TESTS  (through the bounded wrapper -- [S8])
  chunk A (123, ~12 s), chunk E (this module, ~32 s), + your new tests
  Then the SMOKE suite (457, ~40 s) to confirm nothing else moved.

ACCEPTANCE -- all must hold
  [ ] The gate reads ONLY the manifest; no glob decides cohort membership.
  [ ] All 39 entries individually verified before freezing.
  [ ] A new run directory cannot change the cohort -- proven by test (a).
  [ ] A missing entry fails loudly; the cohort never shrinks silently.
  [ ] Recorded expectations reproduce EXACTLY the values in step 5.
  [ ] docs/change_log.md agrees, and a test enforces that.
  [ ] C-010's six-leg allowlist is preserved separately and is not conflated.
  [ ] Zero files under src/ modified.
  [ ] chunk A, chunk E and smoke all pass.
  [ ] Cleanup report attached; final surviving process count = 0.

TRAPS
  TRAP-2  This branch is the reason TRAP-2 was rewritten. You are repairing a
          STALE pin, not implementing C-010. If you find yourself changing what
          a leg's verdict IS, rather than which legs are counted, STOP -- that is
          C-010's work and it is not authorized here.
  TRAP-5  Never commit a cache modification.
```

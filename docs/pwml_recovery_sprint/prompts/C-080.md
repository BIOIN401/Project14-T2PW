# C-080 — the PRODUCTION release gate must follow the same identity rule as the scorer

**Card type:** production correction — semantic release gate.
**Finding:** F-108 · **Ruling:** product owner, 2026-08-23 (the same identity ruling C-076 implements).
**Base SHA:** set at dispatch — **must be a tip with C-076 already merged.**
**Branch:** `agent/c080-production-gate-kind-aware`
**Depends on:** **C-076 merged.** This card reuses C-076's predicate; dispatching it earlier would
produce a third copy of the concept, which is the defect this card exists to remove.

---

## 1. Why this card exists

**F-102 was registered as *"the pipeline now follows D-035; the scorer does not."*** Measured, that
is half true. The **identity admission** layer follows D-035 after C-073. The **semantic release
gate** does not.

C-076 corrects `bench/semantic.py`, the acceptance scorer. `bench/semantic_production.py` carries a
**second, independent copy** of the same rejected rule, and that copy is the one that gates real
runs. After C-076 alone, the two seams disagree: the scorer says these are not conflicts and the
production gate still demotes legs for them.

This is the same shape as C-075/F-105 — one concept, two implementations, only one corrected.

---

## 2. What I measured

Re-derive all of it. Do not take my numbers on trust.

### 2a. The second copy, in current source

`src/t2pw/bench/semantic_production.py:252-260`, inside `_audit_entities`:

```python
for (namespace, accession), names in sorted(holders.items()):
    distinct = sorted({_s.normalize_name(n) for n in names if n})
    if len(distinct) > 1:
        conflicts.append({
            "kind": "accession_claimed_by_multiple_entities", "namespace": namespace,
            "identifier": accession, "entities": sorted(set(names)),
            "reason": f"{namespace}:{accession} is claimed by {len(distinct)} differently-named entities",
        })
```

**No kind check of any sort.** This is the predicate C-073's review rejected as contradicting
**D-035 clause 3c**, and the one the 2026-08-23 identity ruling forbids flagging.

### 2b. It gates the production release path

* `bench/semantic.py:98` — `CHECK_ID_CONFLICT = "no_real_id_or_name_conflict"`
* `pipeline/release_status.py:114-119` — that name is a member of `SEMANTIC_GATING_CHECKS`
* `pipeline/strict_quarantine.py:2424` — imports `evaluate_production_semantics`

The `id_check` `CheckResult` built at the end of `_audit_entities` carries these findings into the
gating verdict.

### 2c. It is firing on real legs

From committed `quarantine_report.json` → `release.semantic_failed_checks`:

| run | leg | status | failed gating checks |
|---|---|---|---|
| T-105 | `PMC12096016/research` | `review_required` | `no_real_id_or_name_conflict`, `actor_named_in_its_own_cited_span` |
| T-105 | `PMC12452463/strict` | `review_required` | **sole: `no_real_id_or_name_conflict`** |
| T-104 | `PMC12856317/research` | `review_required` | **sole: `no_real_id_or_name_conflict`** |

C-076 measured PMC12096016's collisions as `uniprot:P0ADI4` ← `EntB`/`holo-EntB` and
`uniprot:P10378` ← `EntE`/`enterobactin synthase`, **all four claimants in `entities.proteins`** —
exactly the within-kind case the ruling exempts.

### 2d. Correcting it does NOT reopen F-094 — and this had to be proved first

`PMC12452463/strict` is **F-094's leg**, and F-094 is a `PRODUCT_CONTRACT` §13 violation. Its
recorded coverage verdict is `minimum_core_satisfied: true`, `coverage_ratio: 0.785714`,
`surviving_processes: 9`, `reasons: []`. So the technical chain reaches `release_ready` and **only
the semantic cap demoted it.** "Correcting this rule reopens F-094" was therefore a live hazard.

Settled by deterministic offline replay (`evidence/f094_reopen_probe.py`, G11 reports
`evidence/g11/F-094/01-reopen-probe.json` and `02-reopen-probe2.json`). The replay reproduces all
three recorded statuses **exactly** before the toggle, which is what makes the counterfactual
trustworthy:

| leg | as recorded | rule corrected | what still holds it |
|---|---|---|---|
| `PMC12452463/strict` | `review_required` | **`review_required`** | `requested_core_anchors_unmatched: 2,3-dihydroxybenzoate (DHB), EntA, Fur` — **C-072's rule, independent of the cap** |
| `PMC12096016/research` | `review_required` | **`review_required`** | `semantic_evaluation_failed: actor_named_in_its_own_cited_span` |
| `PMC12856317/research` | `review_required` | **`review_required`** | `requested_core_anchors_unmatched: hemin` |

**No leg flips to `release_ready`; `strict_acceptance_eligible` stays `False` on all three.** The
strict PWML rate moves by **zero** across all 22 committed legs. So this cannot be a case of
weakening a gate to increase PWML production (**merge rule 6**) — and you must **re-derive that**,
because it is the single most important safety property of this card.

---

## 3. Required change

`_audit_entities`'s collision branch emits `accession_claimed_by_multiple_entities` **only when the
claimants are of incompatible kind**, exactly as C-076 made `bench/semantic.py::_check_id_conflicts`
do.

### Reuse C-076's predicate. Do not write a third copy.

That is the whole point of the card. At your base, C-076 is merged and `bench/semantic.py` contains
the kind-aware logic and the `entity_kind_class` import (`mapping/identity_admission.py:605` — note
it is **not** in `pipeline/entity_identity.py`; the earlier charter got that wrong).

Choose the smallest structural option that leaves **one** definition of the concept:

1. call the shared helper directly if C-076 left one that is callable from here; or
2. extract C-076's kind comparison into a small shared function that both call.

If you extract, `bench/semantic.py` may change **only** to move the logic, with its behaviour
byte-identical — every C-076 test must stay green unchanged. **A third independent implementation is
an automatic reject even if it is correct**, because the next key or kind will diverge again.

### What must keep firing

Cross-kind collisions — a protein and a compound sharing one accession — must still be conflicts.
That is the one true defect C-073 fixed in the pipeline. A patch without a test proving this is a
blanket disabling.

---

## 4. Ownership boundary

| May change | Notes |
|---|---|
| `src/t2pw/bench/semantic_production.py` | `_audit_entities` collision branch only |
| `src/t2pw/bench/semantic.py` | **only** to extract a shared helper, behaviour byte-identical |
| `tests/test_c080_production_gate_kind_aware.py` | **NEW**, yours |

**Out of bounds:**

* `src/t2pw/pipeline/release_status.py` — `SEMANTIC_GATING_CHECKS` membership is correct; the check
  should keep gating. Removing `no_real_id_or_name_conflict` from the gating set to make legs pass
  is **weakening a gate** and an automatic reject.
* `src/t2pw/pipeline/strict_quarantine.py` — the call site is correct.
* `src/t2pw/bench/gold/**` — C-076 settled the gold.
* `src/t2pw/app/streamlit_app.py` — uncommitted product-owner edit; never touch, stage or commit.
* Whatever other cards are live at dispatch — the orchestrator will name them.
* `data/enrichment_cache.json`, `data/id_mapping_cache.json`.

---

## 5. Required tests

1. **G9 base-failing proof.** Two same-kind protein rows sharing one accession produce **no**
   `accession_claimed_by_multiple_entities` from `evaluate_production_semantics` at your tip and
   **do** on base. Use the real `EntB` / `holo-EntB` (`uniprot:P0ADI4`) and `EntE` /
   `enterobactin synthase` (`uniprot:P10378`) shapes. **Symbol absence is not proof** — assert on
   the emitted findings.
2. **Cross-kind still fires.** Protein vs compound sharing one accession remains a conflict, and
   `CHECK_ID_CONFLICT.ok` is still `False` for it. Without this the card is a blanket disabling.
3. **Placeholder forgery is untouched.** `_audit_entities` also emits
   `placeholder_claims_real_identity` and `placeholder_not_distinguished` and returns
   `(id_check, placeholder_check, census, forged_count, backed)`. Prove the placeholder arm, the
   census and both counts are unchanged. **A patch that quietly drops a forgery finding while making
   collisions kind-aware is the failure mode here.**
4. **One definition, not three.** Assert `semantic_production` and `semantic` reach the same
   predicate — the same function object, or a test that a change in one is observable in the other.
   This is the card's actual purpose.
5. **The gate still gates.** `no_real_id_or_name_conflict` is still in `SEMANTIC_GATING_CHECKS`, and
   a genuine cross-kind conflict still demotes a leg end to end.
6. **Re-derive §2d — the merge rule 6 property.** Replay all three legs and prove **no leg flips to
   `release_ready`** and `strict_acceptance_eligible` stays `False`. `evidence/f094_reopen_probe.py`
   is committed; use or improve it. **State your sample size.** If your measurement disagrees with
   mine, say so loudly — that is a stop-and-report, not a proceed.
7. **SMOKE**, baseline **473**. Note `semantic_production` is reached by
   `tests/test_semantic_production_no_gold.py`; justify any movement precisely.

### Offline only

Everything is reachable from committed artifacts, constructed payloads, or the replay probe.
**You are NOT authorised to run a live paper leg or any LLM-backed command.** If a property
genuinely needs one, name it and stop.

---

## 6. Predict this before T-106 runs

On T-106's draws a leg could fail **only** the within-kind check **and** have all declared anchors
matched. Such a leg becomes `release_ready` — **legitimately, under the ruling.** State in your
report that this is possible and what it would look like, so the orchestrator can predict it rather
than discover it and mistake it for an unexplained strict-rate improvement.

---

## 7. Process — non-negotiable (TEST_MATRIX § 0, gate G11)

```
python docs/pwml_recovery_sprint/evidence/g11/g11_evidence.py next --task C-080 --label <label>
PYTHONPATH="C:/t/c080/src" \
python docs/pwml_recovery_sprint/evidence/bounded_run.py \
  --timeout 900 --label <label> --json <reserved-path> -- <command>
```

venv `c:/Users/Angad/Desktop/SummerBIOIN/Project14-T2PW/.venv/Scripts/python.exe`. **`--basetemp`
under `C:/t/` on EVERY pytest** — without it 83 tests error with `PermissionError` and you report a
false regression. Never `pytest -n auto`, never the unchunked full suite, one heavy job at a time.
Never `taskkill /IM python.exe`, never `pkill python` (two `ms-python.isort` processes are the
product owner's IDE). **Capture wrapper stdout to a file** — the G11 JSON does not retain it. Quote
`FINAL SURVIVING COUNT : 0` and `cleanup : success` per job. A `--heavy-lock` acquire may fail with
exit 95 if another lane holds it — wait and retry, never remove a lock you did not create.
`git commit -F <message-file>`; do not push, do not merge.

---

## 8. What to report

1. Base SHA, branch, final commit SHA, `git diff --stat <base>..HEAD`.
2. Full unified diff of every production line.
3. **How you achieved one definition rather than three**, and why that shape.
4. The G9 base-vs-tip proof, both outputs quoted.
5. All seven §5 tests with results and sample sizes — **§5.6 is the safety property; lead with it.**
6. Your §6 prediction for T-106.
7. Every job's `FINAL SURVIVING COUNT` / `cleanup`; SMOKE before/after.
8. Anything this card gets wrong. §2 is measured and should reproduce. If §2d does **not**
   reproduce — if any leg flips to `release_ready` — **stop and report before implementing.** That
   would make this card a merge-rule-6 hazard rather than a correction, and it is the orchestrator's
   call, not yours.

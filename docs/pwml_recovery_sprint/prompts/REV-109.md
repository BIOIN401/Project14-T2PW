# REV-109 — independent review of C-109 (control-plane integrity)

- **Reviewing** `card/C-109-control-plane` against base `2ac8404`
- **You are not the author.** You did not write this diff and you must not fix it. If it needs a
  change, the finding goes back to the author.
- **These criteria were fixed and committed BEFORE the diff existed.** Anything you add after seeing
  it must be labelled as an addition, so the record shows what was anticipated and what was not.

**Worktrees:** `C:/t/rev109` (tip) and `C:/t/rev109base` (base). **Prune neither.**

---

## The trap specific to this card

**This is a documentation and infrastructure card, so the usual safety net is absent.** No biological
gate protects it, no benchmark moves if it is wrong, and **prose staleness is invisible to CI by
construction.** A wrong citation here will fail exactly the way the ones it is fixing failed:
silently, plausibly, and for months.

**So verify every corrected citation by RESOLVING it, not by reading it.** Open the target. Confirm
the anchor names the content the citation claims. A citation that looks right is the failure mode.

---

## Blocking criteria

| # | Criterion | How |
|---|---|---|
| **B1** | **F-153: the docstring is corrected and the diff is INERT** | `src/t2pw/rag/controller.py` must differ **only** in its module docstring. **No statement, signature, import or constant may change.** Diff it yourself and say so. |
| **B2** | **F-153: the third claim is NOT certified** | Graph-delta validation being partial (`conform.py` conforms and merges without validating the delta against a policy) was **NOT re-verified** by F-153 and **stands as written**. If the author deleted it, softened it, or asserted it as fixed, **that is a blocking finding** — correcting two claims is not licence to certify a third nobody measured. |
| **B3** | **F-153: the historical record survives** | The old claim must be visibly **retracted**, not silently rewritten. A reader who remembers "UNWIRED" must be able to see it was withdrawn and why. |
| **B4** | **F-154: anchors, not renumbered lines** | If the author simply replaced `:213-218` with `:230-237`, **that is a fail** — a corrected line address drifts again the moment anything is inserted above it, which is the entire finding. Require stable anchors (headings, unique strings, named symbols). |
| **B5** | **F-154: every citation resolves** | Resolve each one at the tip yourself: the chunk membership table, the SMOKE block, the `chunk_d_gate.py` symbols, and the `FINDINGS.md:1120-1124` Chunk-A citation. **Confirm the anchor points at the content the sentence claims.** |
| **B6** | **F-154: the claim did not change** | The instruction must still mean **stem-exact** certification and must still forbid **filename grepping** — the collision it warns about (`tests/test_map_ids.py` vs `test_map_ids_name_gate`) is real and the warning must survive intact. |
| **B7** | **`TEST_MATRIX.md` line-neutrality above `:477`** | If the author edited at or above `:477`: count lines above `:477` at base and at tip — **identical** — and `:477` itself **byte-identical**. Nothing may be inserted or removed above the pin. Check it; do not accept the claim. |
| **B8** | **The reviewer-evidence check FAILS when it should** | **Run it against a case with evidence missing and watch it exit non-zero.** A check nobody has seen fail is not a check. Then run it on a satisfied case and watch it pass. |
| **B9** | **Reachability is decided by CONTENT, not filename** | Plant a file with the **right name and different bytes** and confirm the check calls it **unreachable**. This is the distinction the whole mechanism exists for — if it keys on filenames it is decorative. |
| **B10** | **G9 labels are honest** | The reviewer-evidence check is a **NEW capability** — new acceptance test, **no fabricated base failure**. F-153/F-154 are corrections of currently-false documentation and their proof is false-at-base / true-at-tip. **Mislabelling is a reject. Symbol absence is not proof.** |
| **B11** | **Boundary** | Only the files the card names. `git diff --stat` against base. No opportunistic edits — this card exists partly *because* opportunistic edits were correctly refused twice. |
| **B12** | **Job lifecycle** | Every job `FINAL SURVIVING COUNT : 0`, `cleanup : success`, shape-validated G11 path, heavy lock released. A run with survivors is an **infrastructure failure, not a test result**. |

---

## Adversarial work

**B13. Try to make the reviewer-evidence check give a false PASS.** That is its one dangerous failure
mode: a check that says evidence is safe when it is not is worse than no check, because it retires
the manual habit that currently works. Try at least — evidence present but uncommitted; present in a
*different* branch not reachable from integration; same name different bytes; empty file; a G11
report present but its probe missing.

**B14. Re-read the corrected `controller.py` docstring as someone who does not know the history.**
Does it now tell a reader where the wiring is, and what genuinely remains incomplete? A docstring
that merely deletes the false sentence has fixed nothing — the point is that the next agent must not
rebuild `loop_policy` or `controller`.

**B15. Check the author did not fix the citations by editing the TARGET instead of the citation.**
Moving the chunk table to line 213 would "resolve" the citation and be a much worse outcome.

---

## Deterministic gates

- **SMOKE — 503 passed, 22 files.** 473 / 465 / 460 / 457 are stale. **Hash
  `src/t2pw/bench/acceptance.py` immediately before and after** — SMOKE writes to it and restores it
  in a `finally`. A changed hash is an infrastructure failure even if pytest is green. **Never
  parallelise SMOKE.**
- **Gold readers — 456 passed / 8 skipped / exit 0.** A charter claiming this selection correctly
  exits 1 is stale after C-103.
- `--basetemp=<unique short dir under C:/t/>`, **parent pre-created** — without it 83 tests error
  with `PermissionError` and you will report a false regression.
- The explicit venv interpreter. **A bare `python` is system 3.13 with no `streamlit`** → 35 spurious
  import errors that read exactly like a regression (F-143).
- **Your own shell timeout must exceed the wrapper's `--timeout`**, or you strand the heavy lock.
- The heavy lock is shared with other live cards. **A failed acquire means the wrapper did not run
  your job — it is not a failure. Wait and retry. Never clear a lock you do not hold.**

---

## Verdict

**APPROVE**, **APPROVE WITH REGISTERED FINDINGS**, or **REJECT WITH BLOCKING FINDINGS**.

**If what remains is registerable rather than blocking, say APPROVE and register it.**

**Before your review closes:** collect your G11 reports, **your probe sources**, and your substantive
outputs into a commit on the branch — and note the irony if you do not, since **this is the card that
builds the check for exactly that failure.** 72 reviewer reports and 94 probes were nearly lost this
sprint.

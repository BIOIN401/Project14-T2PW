# C-013 — `agent/p04a-hash-module`

Wave A0. Agent type: `pwml-implementer` (isolated worktree). Reviewer: the C-020 implementer.
Implements **D-001**.

---

```
[S1] [S3] [S4] [S5] [S6] [S8]

BRANCH     agent/p04a-hash-module
BASE       <CURRENT integration SHA, read with `git rev-parse sprint/pwml-recovery`
            at dispatch. Do not use a SHA copied from any document.>
WORKTREE   .claude/worktrees/p04a-hash-module
WAVE       A0
DEPENDS    NONE

OWNS -- exclusive.
  NEW src/t2pw/pipeline/canonical_hash.py
  src/t2pw/pipeline/gate_reports.py :: payload_sha256, stamp_report, gate_verdict
  tests/  -- ONE new test file, named in your report

OBJECTIVE
  One hash currently means two things. Split it into two VERSIONED CANONICAL
  PROJECTIONS, each EXCLUDING all hash and stamp fields from its own input:

    canonical_graph_sha256   biological content only. EXPORTERS bind to this.
    canonical_payload_sha256 the complete saved final_mapped.json, including
                             lineage, evidence, confidence, provenance.
                             PERSISTED-ARTIFACT INTEGRITY binds to this.

  Two guarantees: the exported biological graph has not changed, and the evidence
  record supporting it has not been altered.

  After this patch: lineage does not change graph equivalence, and lineage changes
  remain detectable.

IMPLEMENT
  1. HASH_SCHEMA_VERSION = 2. Historical hashes keep schema-1 meaning. NEVER
     silently redefine payload_sha256.
  2. graph_projection(payload) -> dict. An ALLOWLIST, never a denylist -- a
     denylist silently admits every future field into the graph hash.
       INCLUDE  entity name + class + external identifiers; reactions with
                inputs/outputs/roles/stoichiometry/direction/reversibility;
                enzymes, modifiers, transporters, cargo; complexes + components;
                element_locations; biological_states; organism; process->entity
                references.
       EXCLUDE  lineage, evidence, confidence, provenance, rag_provenance,
                rag_confidence, source_papers, source_refs, mapping_meta,
                candidates.
  3. BOTH projections strip hash/stamp fields before hashing:
     payload_sha256, canonical_graph_sha256, canonical_payload_sha256,
     hash_schema_version, report_schema_version, phase, artifact_set_version.
     A hash must never be an input to itself.
  4. Keep payload_sha256 (gate_reports.py:145-163) VERBATIM as the schema-1
     implementation.
  5. stamp_report (:169-195) writes all three keys plus HASH_SCHEMA_VERSION_KEY.
  6. gate_verdict (:323+) dispatches on hash_schema_version:
       schema 1 : current behaviour EXACTLY
       schema 2 : compare both, with DISTINCT reasons --
                    canonical_graph_mismatch                 (BLOCKING: exported
                                                              biology is not gated
                                                              biology)
                    canonical_payload_mismatch_graph_intact  (evidence record
                                                              moved, biology did
                                                              not)
     Never collapse these into REASON_PAYLOAD_MISMATCH.

MUST NOT CHANGE
  - payload_sha256's output for ANY input.
  - gate_verdict's legacy path (is_current_artifact_set false) -- byte-identical.
  - The existing binding, verified on committed
    runs_verify/2026-08-04_1647/papers/PMC12856317/strict:
      payload_sha256(final_mapped.json)
        == final_stage3_gate_report.payload_sha256
        == cccf95c83f4cc054a2c312c5a4ba27f77762f747eccb00e7f8678d856324c52e
    This binding WORKS today. Preserve it exactly.
  - You are adding hashes, not wiring them. The call sites in streamlit_app.py
    (:3508, :3604) belong to C-030. Do not touch them.

TESTS YOU MUST ADD
  a) Adding, reordering or editing a `lineage` entry leaves canonical_graph_sha256
     BYTE-IDENTICAL and CHANGES canonical_payload_sha256. Assert BOTH directions
     -- one alone does not prove the split.
  b) Changing stoichiometry / direction / an accession / a complex component / a
     compartment CHANGES canonical_graph_sha256. One test per dimension.
  c) A payload already carrying hash keys hashes identically to the same payload
     without them (self-exclusion), for both projections.
  d) Schema-1 artifacts still verify unchanged through gate_verdict.
  e) The committed PMC12856317 hash above still reproduces.
  f) A NEW field added to a payload does NOT enter the graph hash unless the
     allowlist names it. This is the test that keeps the allowlist honest.
  g) canonical_graph_mismatch and canonical_payload_mismatch_graph_intact are
     produced by distinct scenarios and neither is reachable from the other's.

FOCUSED TESTS  (through the bounded wrapper -- [S8])
  smoke suite (457 tests, ~40 s) + your new file

ACCEPTANCE -- all must hold
  [ ] payload_sha256 output unchanged for every input.
  [ ] Lineage moves the payload hash and not the graph hash. Both asserted.
  [ ] Every biological dimension in PRODUCT_CONTRACT section 5 moves the graph hash.
  [ ] Self-exclusion holds for both projections.
  [ ] The committed PMC12856317 binding still reproduces.
  [ ] Schema-1 artifacts verify unchanged.
  [ ] Smoke passes.
  [ ] Cleanup report attached; final surviving process count = 0.

TRAPS
  TRAP-4  Do not rebuild what exists. The hash-to-gate-report binding already
          works; you are splitting the hash, not repairing the binding.
  TRAP-5  Never commit a cache modification.

BUDGETS (D-019, ratified by WAVE-A0-BUDGET-RATIFICATION-001) -- limits only.
  HAND-AUTHORED: at most 280 changed lines, additions PLUS deletions, across this
    card's complete authorized implementation manifest. A MAXIMUM -- not a target,
    not an expectation that every line be used. Anything outside OWNS stays barred.
  MACHINE-GENERATED EVIDENCE, governed separately: 0 artifacts, 0 bytes.
  Overrun of either, predicted or discovered: STOP BEFORE COMMITTING and obtain
    renewed product-owner authority; no role may self-authorize one.
  SPLITTING: only where each half is independently implementable AND independently
    validatable (D-019). Exceeding a threshold does not itself authorize or require
    a split; no split is preauthorized here.
  Ratifies limits ONLY: no implementation approval, no unblocking, no branch, no
    dispatch authority.
```

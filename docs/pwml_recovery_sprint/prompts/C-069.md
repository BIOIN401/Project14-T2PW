# C-069 — the batch preflight stops being partly blind (F-073)

- **Base SHA** to be set at dispatch (currently `sprint/pwml-recovery` tip)
- **Branch** `agent/c069-child-imports`
- **Finding closed** F-073 (MEDIUM) — `FINDINGS.md:2382`
- **Ceiling** 400 changed lines / 90 changed docstring+comment lines / 500 KB artifacts.

## 1. The defect, measured at `5414cda`

`tests/test_batch_preflight.py:134-153`
`test_every_import_driver_defers_is_covered_by_the_preflight` asserts that every import `batch/driver.py`
defers into a function body is declared in `runner.CHILD_IMPORTS`, so the batch preflight can validate it in
the child process **before a leg starts**. The list is *"derived from the code, not from memory"* (`:131-133`)
— which is why it is a real guard and not a hand-maintained comment.

**It is pre-charged red.** Measured by executing the test module's own helpers against the real driver:

```
deferred total: 9
MISSED count:   6
  - t2pw.pipeline.strict_quarantine
  - t2pw.pipeline.release_status
  - t2pw.pipeline.release_status
  - t2pw.pipeline.release_status
  - t2pw.pipeline.strict_quarantine
  - t2pw.pipeline.release_status
```

### ⚠ Correction to F-073, and it makes the card smaller

**The `missed` list counts DEFERRAL SITES, not modules. Six occurrences resolve to exactly TWO distinct
modules** — `t2pw.pipeline.release_status` (4 sites) and `t2pw.pipeline.strict_quarantine` (2 sites).

**So the cure is two `CHILD_IMPORTS` entries, not six.** F-073's *"adding all six"* is about occurrences.
**Verify this yourself before relying on it** — F-068 is the standing lesson that a number cited from a
record is not a measurement.

`runner.CHILD_IMPORTS` (`src/t2pw/batch/runner.py:1362-1387`) currently has **5** entries:
`streamlit.testing.v1`, `t2pw.rag.research_report`, `t2pw.batch.driver`, `t2pw.pipeline.export_mode`,
`t2pw.llm.client`. It should end with **7**.

## 2. What the finding is actually about — do not lose this

**Two of the six sites are C-056d's**, added in its private helper, matching the house style of three
neighbouring functions. **Four predate everything.** F-073 is explicit:

> *"**Four imports were already unregistered before any of this**, which means the batch preflight has been
> unable to validate them in the child process for as long as they have existed. That is the defect. C-056d
> made it two larger and visible; it did not create it."*

**Your report must establish, from `git log`/`git blame` on `driver.py`, which four sites are the
pre-existing ones and when each was introduced.** F-073 requires it in terms:

> *"record what the four pre-existing entries were and why they were missed — because that history is the
> only evidence of how long the preflight has been partially blind."*

**This history is the deliverable. The two-line fix is the easy part.**

## 3. ⚠ This card deliberately turns a pre-charged red GREEN

`test_every_import_driver_defers_is_covered_by_the_preflight` is on the standing pre-charged-failure list.
**Closing it moves that baseline**, and merge rule 4 requires it be moved *"deliberately, with an exact
documented delta."*

* State the delta exactly: the test goes **red → green**; `CHILD_IMPORTS` goes **5 → 7** entries; the
  `missed` list goes **6 → 0**.
* **The orchestrator will remove it from the standing pre-charge list on merge.** Say so in your report so
  the removal is traceable to this card.
* F-073 warns: *"Do not let a future card absorb this quietly in passing."* **This card is the opposite of
  that — it is the deliberate act. Make the commit message say so.**

## 4. Ownership boundary — exact

**You own:**

| Path | Scope |
|---|---|
| `src/t2pw/batch/runner.py` | **`CHILD_IMPORTS` only** (`:1362-1387`) — its two new entries and their reason strings |
| `tests/test_batch_preflight.py` | only if a test genuinely needs updating; justify every line |

**You own nothing else.** In particular:

* **`src/t2pw/batch/driver.py` is READ-ONLY.** Do **not** un-defer the imports to make the test pass. The
  deferral is the house style of three neighbouring functions and is deliberate; the *declaration* is what is
  missing. Reversing that would be fixing the wrong end.
* The rest of `runner.py` is out of bounds. `runner.py` is C-032's; you have `CHILD_IMPORTS` and nothing else.
* **Never touch `src/t2pw/app/streamlit_app.py`** — uncommitted product-owner edit.
* `docs/pwml_recovery_sprint/evidence/bounded_run.py` and `g11/g11_evidence.py` may belong to a concurrent
  card. Call them; never edit them.
* Do not edit control-plane markdown. Report the prose you want changed.

## 5. Required behaviour

Add the two missing modules to `CHILD_IMPORTS` **with a reason string in the style of the existing five** —
each says *why a child needs it*, e.g. `t2pw.llm.client`'s *"every stage calls chat(); this module raises at
IMPORT time when OPENROUTER_API_KEY is missing or malformed…"*.

**A reason must say what breaks in the child if the import fails, not restate the module name.** Read the
deferral sites in `driver.py` and write reasons grounded in what those functions actually do.

## 6. Proof obligations

**This is a correction of pre-existing observable behaviour.** G9 requires a proof that **fails behaviourally
at your base and passes at your tip** — which here is unusually clean, because **the target test is already
red at base**. Paste both:

* base: `missed == [6 entries]`, test fails;
* tip: `missed == []`, test passes.

**That is necessary but NOT sufficient**, and this is the part that matters:

**Prove the preflight now actually catches a missing one of the two.** The whole point of `CHILD_IMPORTS` is
that `probe()` validates these in a **child process** before a leg starts (`runner.py:1716-1726`). A card
that only makes a list-consistency test green has proved the list is consistent, **not** that the preflight
gained coverage.

Construct a behavioural arm: make one of the two newly declared modules unimportable in a probe child, and
show the preflight **reports a `PreflightProblem` naming it** at your tip and **stays silent** at your base.
That is the coverage the finding says has been missing "for as long as they have existed".

**Non-vacuity (RULING 13):** the list-consistency test derives its list from the code. **Neutralize
`_deferred_imports` so it returns nothing and show the test goes green when it should not** — then say how a
reader can tell the difference. A guard that passes because it found nothing to check is the failure mode
here, and it looks identical to success.

## 7. Process

* Every run through the integration copy of `bounded_run.py`, real `--timeout` comfortably below your own
  tool wall clock.
* `--json` from `g11_evidence.py next --task C-069 --label <label>`, allocated immediately before the job.
* `--basetemp` under `C:/t/c069tmp/`, parent pre-created, on every pytest invocation.
* Paste `FINAL SURVIVING COUNT: 0` and `cleanup: success` per job; run `g11_evidence.py check --task C-069`
  before reporting.
* **Certify chunk membership stem-exactly** for `test_batch_preflight.py` and run whatever gate contains it.
  Substring matching is forbidden.
* Offline and focused — **do not touch `C:/t/heavylock`.** Never `pytest -n auto`; no unchunked suite.

## 8. Report

`_SHARED_BLOCKS.md` § S6, plus:

1. Your own measurement of the missed list — count, and the distinct modules behind it.
2. **The git history of the four pre-existing deferral sites**: which, introduced when, in which commit.
3. The base-red / tip-green pastes.
4. The behavioural arm proving the preflight gained real child-process coverage.
5. The non-vacuity result.
6. The exact baseline delta, for the orchestrator to cite when removing this from the pre-charge list.
7. Confirmation `driver.py` is untouched, with `git diff --stat`.

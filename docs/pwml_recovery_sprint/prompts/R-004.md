# R-004 — RAG reintroduction and missing-reaction triage

**Read-only. No branch. No code.** Agent type: `pwml-bio-auditor`.
Its output becomes the body of `C-061`, which is not dispatchable until this delivers.

---

```
[S1] [S5] [S7]

ROLE
  Biological adjudication of two distinct failure classes that share a cause
  candidate. You classify; you do not fix.

CLASS A -- REJECTED RAG REACTIONS REINTRODUCED
  The semantic check no_rejected_rag_reaction_reintroduced FAILS for
  PMC12657337/research in committed runs/2026-08-02_2130. Three reactions the
  admission gate refused came back:

    /processes/reactions/6  IPP -> farnesyl pyrophosphate (FPP)
                            claim_key: ipp=>farnesyl pyrophosphate fpp|idi|0
    /processes/reactions/7  FPP -> heptaprenyl pyrophosphate (HepPP)
                            claim_key: fpp=>heptaprenyl pyrophosphate heppp|heppps|0
    /processes/reactions/8  HepPP -> DMK-7
                            claim_key: heppp=>dmk 7|mena|0
                            rejected_for: evidence_relation_disagrees_with...

  The rejections are recorded in that leg's rag_admission_report.json (363 KB,
  committed). ANSWER: which stage put them back?

  Candidate paths to check, in order:
    - rag/synthesize.py     did synthesis emit them despite the gate?
    - rag/conform.py        conform_rag_additions_for_merge
    - pipeline.merge_additions        the S3 seam at streamlit_app.py ~4594
    - curation/gap_resolver.py        did gap-fill re-derive them independently?
    - curation/apply_audit_patch.py   did the audit re-add them?
  A reaction re-derived INDEPENDENTLY by a later stage is a different defect from
  one that leaked through the merge. Distinguish them -- the fixes differ.

CLASS B -- MISSING SUPPORTED REACTIONS
  Three, across two papers:

    PMC12096016 (both legs)  2,3-DHB + L-serine -> enterobactin [EntF]
                             quote "cycles of condensation with L"
                             quote_hash 7be16a685d3bcbff
    PMC12421875/research     DHNA + prenyl diphosphate -> demethylmenaquinone
                             [MenA]
                             quote "MenA joins DHNA and prenyl diphosphate to
                             produce demethylmenaquino..."

  For each: was it never extracted, extracted then dropped, or extracted then
  quarantined? Evidence order:
    stage1_payload.json -> merged_payload.json -> final_mapped.json, plus
    gap_iteration_summary.json and (once INIT-001 commits it) any
    quarantine_report.json.
  NOTE: quarantine_report.json is NOT written by the current driver -- C-031 adds
  it. If the answer needs that artifact, say so; that is a real finding about
  diagnosability, not a dead end.

FOR EACH FINDING, DELIVER
  1. CLASS: product_contract_violation | gold_data_defect | policy_disagreement,
     citing the gold relevance_note / export_rationale. Only the first justifies
     code.
  2. ORIGIN STAGE, with artifact evidence. Where attribution requires lineage
     that does not yet exist, say so and name the instrumentation.
  3. NEEDS LINEAGE? yes/no.
  4. AFFECTED FILES.
  5. EXPECTED CORRECTION.
  6. REGRESSION FIXTURE -- minimal payload plus, for class A, the claim_key that
     must stay rejected.

WATCH FOR
  - PMC12421875's other finding is a MISSING PATHWAY ANCHOR
    ("menaquinone biosynthesis"), which is a different class. Do not merge them.
  - PMC12657337/research scored from merged_payload.json, which is PRE-MAPPING.
    Identity conclusions cannot be drawn from it.
  - TRAP-1: PMC12452463 is not in scope here, but if it surfaces, its correct
    outcome is review_required, never strict success.

DELIVER
  ## CLASS A   3 rows: reaction | claim_key | reintroducing stage | evidence |
               class | expected correction | fixture
  ## CLASS B   3 rows: signature | never-extracted / dropped / quarantined |
               evidence | class | expected correction | fixture
  ## SHARED CAUSE?         do A and B share one, or are they independent?
  ## NEEDS-LINEAGE SPLIT   actionable now vs after lineage
  ## PROPOSED C-061 SCOPE  what one narrow branch fixes; what must be separate
  ## CONFIDENCE + WHAT WOULD CHANGE MY MIND
```

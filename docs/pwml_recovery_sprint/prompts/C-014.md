# C-014 — `agent/p03a-llm-timeout`

Wave A0. Agent type: `pwml-implementer` (isolated worktree). Reviewer: the C-032 implementer.
Unblocks **D-005** — C-032 and C-042 cannot hold a deadline until this lands.

---

```
[S1] [S3] [S4] [S5] [S6] [S8]

BRANCH     agent/p03a-llm-timeout
BASE       <CURRENT integration SHA, read with `git rev-parse sprint/pwml-recovery`
            at dispatch. Do not use a SHA copied from any document.>
WORKTREE   .claude/worktrees/p03a-llm-timeout
WAVE       A0
DEPENDS    NONE

OWNS -- exclusive.
  src/t2pw/llm/client.py :: the two OpenAI(...) constructions (:47 openrouter,
                            :61 local), chat_detailed (:444), chat_with_tools (:656)
  tests/  -- ONE new test file, named in your report

OBJECTIVE
  There is NO request timeout anywhere in the LLM client. Measured statically on
  ORIGIN_SHA:

    src/t2pw/llm/client.py:47  OpenAI(base_url=..., api_key=..., default_headers=...)
    src/t2pw/llm/client.py:61  OpenAI(base_url=base_url, api_key="lm-studio")
    grep -c "timeout=" src/t2pw/llm/client.py  ->  0

  With LLM_MAX_RETRIES=8 (:505, :679), base sleep 1.0 and max sleep 20.0, the
  worst case for ONE chat_detailed call is about 8 x 600 s + 71 s = 4871 s,
  against a 3480 s child deadline. This is the most likely mechanism behind
  PMC12444477 timing out in BOTH modes.

  After this patch: no single LLM request can outlive its configured maximum, and
  the total time a call can consume is bounded and computable from configuration
  BEFORE the call is made. That last property is what C-032 needs; without it a
  deadline is unenforceable.

MUST NOT CHANGE
  - The retry semantics themselves. You are bounding each attempt, not changing
    how many attempts happen or when they are considered warranted. The empty-
    completion and moderation logic (:162-195, :463-466, :526, :729) is
    deliberate and already correct -- do not "improve" it.
  - chat (:412) keeps its current signature and behaviour.
  - Default behaviour when the new environment variables are unset must remain
    safe and must not silently become slower or more permissive than today.

IMPLEMENT
  1. A single configured request timeout, read once, with an explicit default --
     name it in your report. It must apply to BOTH client constructions.
  2. Per-call override capability on chat_detailed and chat_with_tools, so C-032
     can shrink a request to fit the remaining budget. A caller that passes
     nothing gets the configured default.
  3. Expose a way to compute the WORST-CASE total duration of a call from its
     configuration -- attempts, per-attempt timeout, and backoff. C-032 will call
     this to decide whether a request fits the remaining budget. Do not implement
     the budget check itself; that is C-032's.
  4. A timeout must surface as a distinct, identifiable outcome. It is
     operation_timeout in the D-005 vocabulary and must never be conflated with
     an empty completion, a refusal, or a malformed response.
  5. Do not wire any of this into the pipeline. C-032 owns the deadline; C-042
     owns the extraction ladder. Your branch ends at the client boundary.

TESTS YOU MUST ADD
  Use a fake/stubbed client -- no network, ever.
  a) A request that exceeds its timeout raises/returns the distinct timeout
     outcome, and does so within the configured bound (assert elapsed, not just
     the exception type).
  b) The worst-case-duration calculation matches a hand-computed value for a
     known configuration. This is the number C-032 depends on; assert it exactly.
  c) A per-call override is honoured and does not mutate the module default for
     subsequent calls.
  d) A timeout is NOT reported as an empty completion, and does not consume the
     empty-completion retry budget.
  e) Both OpenAI(...) constructions receive the timeout. A test that covers only
     the openrouter path leaves the local path unbounded.
  Every regression test MUST FAIL on the base SHA. Your reviewer will check out
  the base, apply only your test, and run it.

FOCUSED TESTS  (through the bounded wrapper -- [S8])
  chunk A (123 tests, ~12 s), chunk C (109 tests, ~2 s), + your new file
  Also run tests/test_llm_client_empty_completion_retry.py explicitly -- it is in
  NEITHER chunk, and it is the file most likely to detect a regression here.
  Run it in its OWN pytest invocation: test_gap_resolver_agent_tools.py and
  test_gap_resolver_stage3_issues.py leave a stub for t2pw.llm.client in
  sys.modules, so co-locating them breaks the import (BASELINE.md section 1).

ACCEPTANCE -- all must hold
  [ ] grep -c "timeout=" src/t2pw/llm/client.py is > 0 and BOTH constructions
      are covered.
  [ ] Worst-case call duration is computable from configuration before the call.
  [ ] A timeout is a distinct outcome, not an empty completion.
  [ ] Retry counts and empty-completion logic are unchanged.
  [ ] chunks A and C pass, and test_llm_client_empty_completion_retry.py passes
      in its own invocation.
  [ ] Cleanup report attached; final surviving process count = 0.

TRAPS
  TRAP-4  Do not rebuild what exists -- MASTER_PLAN section 2.
  TRAP-5  Never commit a cache modification.

BUDGETS (D-019, ratified by WAVE-A0-BUDGET-RATIFICATION-001) -- limits only.
  HAND-AUTHORED: at most 350 changed lines, additions PLUS deletions, across this
    card's complete authorized implementation manifest. FINAL RATIFIED CEILING
    (WAVE-A0-RESUME-002 s2), superseding the 220 previously printed on this line;
    reconciled under CONTROL-PLANE-RECONCILE-001 s5. Measured at merge: 345.
    A MAXIMUM -- not a target, not an expectation that every line be used.
    Anything outside OWNS stays barred.
  CANONICAL G11 EVIDENCE: REQUIRED and committed (D-018, [S8], merge gate G11) under
    evidence/g11/<CARD-ID>/*.json, and EXCLUDED from the hand-authored ceiling above
    (G11-EVIDENCE-ACCOUNTING-001). Noncanonical generated artifacts remain
    PROHIBITED unless separately authorized: 0 artifacts, 0 bytes.
  Overrun of either, predicted or discovered: STOP BEFORE COMMITTING and obtain
    renewed product-owner authority; no role may self-authorize one.
  SPLITTING: only where each half is independently implementable AND independently
    validatable (D-019). Exceeding a threshold does not itself authorize or require
    a split; no split is preauthorized here.
  Ratifies limits ONLY: no implementation approval, no unblocking, no branch, no
    dispatch authority.
```

---

## CARRIED FORWARD — retry ownership for the four unowned call sites

Authority: `CONTROL-PLANE-RECONCILE-001` §7. Source: the C-014 merge record `c832894`,
recorded finding 4. C-014 is merged; **nothing here reopens it and no retry is restored.**

C-014 deliberately pinned the SDK's own `max_retries` to **0** so that
`worst_case_call_seconds()` is a true upper bound computable from configuration. The merge
record states the consequence in full:

> **TWO UNOWNED CONSUMERS OF THE SHARED `_client` LOSE THEIR ONLY RETRY LAYER:**
> `src/t2pw/stoich/agent.py:477,552,580` (verified by AST to sit inside no try block) and
> `src/t2pw/rag/embed.py:154`. They also GAIN a 300 s bound where they had 600 s … Not this
> card's to fix — it does not own those files, and configuring a module-level singleton
> cannot be done without every consumer inheriting it. **NEEDS A LEDGER ENTRY AGAINST THE
> OWNING CARDS.**

**The ownership search was run and its result is negative, not deferred.** A sweep of the
whole control plane (`MASTER_PLAN.md` §9 branch register, `MASTER_PLAN.md` §2, `LEDGER.md`,
every `prompts/*.md`) returns **zero** occurrences of `stoich/agent.py` and **zero** of
`rag/embed.py`. **No existing card — future or otherwise — owns either file.** There is no
owning card to bind, so none is named here; inventing one would be worse than the gap.

The obligation is recorded instead in `LEDGER.md` § "Carried Wave A0 requirements" as an
**unassigned ownership gap requiring a product-owner assignment before either seam can be
called complete**. `C-032` is the card C-014 was written for and the intended consumer of
`worst_case_call_seconds()`; it is named as a **cross-reference only** and is **not**
hereby given ownership of `stoich/` or `rag/embed.py`.

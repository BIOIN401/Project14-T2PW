# C-101 — the acceptance instrument reports the 16/5 split instead of one conflated number

**Card type:** acceptance instrument — scorer counts, report surfaces, documentation.
**Ruling:** D-070 (LOCKED 2026-08-27), § O-1a / O-1b / O-1c. **Read it in full first.**
**Depends on:** **C-100 merged.** You both edit `_check_placeholder_identity`. Do not start until the
integration tip contains C-100.
**Base SHA:** set at dispatch — read it with `git rev-parse HEAD`.
**Branch:** `card/C-101-o1-metric-split`
**Worktree:** `C:/t/c101` (created for you at dispatch — work only there)

---

## 1. Read first

1. `_SHARED_BLOCKS.md` — **S1, S3, S4, S5, S6, S7, S8 bind you.**
2. `DECISIONS.md` § **D-070**.
3. `LEDGER.md` §§ "O-1 RULED — the corrected record" and **"F-141 CLASSIFIED"**. The second one
   decides what your fourth category must report and why its measured value is **0**.
4. `src/t2pw/bench/semantic.py:1367` `_check_placeholder_identity` — the whole surface.

## 2. The defect

`placeholder_backed_proteins` counts **one number over two populations that get opposite answers**.
D-070 rules them apart; the instrument still conflates them, so no report can distinguish a PathBank
sentinel row from a generated functional wrapper, and the 24/82 withheld-identity population has no
metric of its own at all.

The census already half-knows this — `census["pathbank_unknown_sentinel"]` exists — but it is
computed on a **separate pass** from `placeholder_backed`, with no relationship enforced between
them, so a row can be counted in both and nothing notices.

## 3. Required categories — mutually exclusive, exhaustive, and none silently zero

Report these as **distinct counts**:

1. **PathBank `Unknown` sentinel rows** — `is_pathbank_unknown_protein(row)`.
2. **generated functional wrappers** — `generated: true` + `single_protein_pathwhiz_wrapper`; use
   `entity_identity.is_generated_complex_wrapper` rather than re-deriving the test.
3. **candidate identities correctly withheld** — F-141's confirmed-correct population.
4. **candidate identities lost despite sufficient support** — F-141's recoverable population.

**No row may count in more than one.** Assign each row to exactly one category and prove it with a
test that constructs a row satisfying two shapes at once and asserts it lands in one.

### The invariant, stated honestly

`placeholder_backed_proteins` **keeps its current value and meaning** — do not change what it counts.
Add the split beside it and assert:

```
placeholder_backed_proteins == sentinel_rows + generated_wrappers + other_placeholder_rows
```

**Report `other_placeholder_rows`; do not assume it is zero.** On the pinned run it is 0 and the
partition is exactly 16/5 — that is a *measured fact about that run*, not a structural guarantee, and
an instrument that asserts the invariant by dropping the remainder will hide the first row that does
not fit. This is the difference between a partition and an assumption.

### Category 4 is 0, and 0 is a measurement

F-141's classification measured **0** rows in category 4, pinned and corpus-wide: all 24 pinned
withheld rows are contractually correct withholding, because `identity_verdict.organism` is non-empty
on every one of them and the `species` rung's `unknown` can therefore only have come from the
candidate side. Your category-4 counter must be able to report a **non-zero** value when one occurs —
a counter hard-wired to 0, or one that cannot be exercised by a test, is vacuous. Write a test that
constructs such a row and asserts the counter moves.

**`0` here means "measured zero", never "not evaluated".** Follow `PRODUCT_CONTRACT` § 8's
`not_evaluated`-is-never-`false` rule and make the two distinguishable in the emitted report.

## 4. Hard constraints

* **Priority-1 forbidden-identity safety stays separate from coverage.**
  `placeholder_claims_real_identity` keeps precedence and does not move. None of the pinned 21 sets
  it, and no report may describe them as forged identities.
* **Do not penalise the pipeline twice** for safely withholding an identifier the contract forbids it
  to ship. A row in category 3 is not also an error elsewhere.
* **Do not weaken any strict export gate** to improve an acceptance count.
* **Do not rename or repurpose** `placeholder_backed_proteins`, and never report F-141's population
  under that name.
* **Preserve `ERROR_ORDER`'s existing entries and their order.** Append if you must add.
* **Historical reports are immutable.** T-106 and earlier stay exactly as they are; a re-score is a
  **new, separately identified** artifact that says what it supersedes. Do not edit a committed
  report to match a new metric.

## 5. Ownership boundary

**Yours:** `src/t2pw/bench/semantic.py` (`_check_placeholder_identity` and the error/census
constants it feeds) · the acceptance summary / report surfaces that render those counts — **find them
by grep, do not guess** · `PRODUCT_CONTRACT.md` and `DECISIONS.md` documentation of the metric ·
a new `tests/test_c101_o1_metric_split.py`.

**Not yours:** `src/t2pw/mapping/map_ids.py` (C-099) · the gold tolerance schema and its matcher
(C-100 — already merged; read it, do not re-open it) · `writer.py` · any strict gate.

## 6. Required tests — `tests/test_c101_o1_metric_split.py`

1. five sentinel rows count as sentinels and **not** as wrappers;
2. sixteen generated wrappers count as wrappers and **not** as sentinels;
3. a row satisfying both shapes lands in **exactly one** category, deterministically;
4. `placeholder_backed == sentinels + wrappers + other` on a payload where `other` is **non-zero**;
5. category 3 counts a correctly-withheld identity;
6. category 4 counts a lost-with-support identity — **constructed, and the counter must move**;
7. category 4 reports `0` distinguishably from `not evaluated`;
8. `placeholder_claims_real_identity` still takes precedence over every category;
9. `placeholder_backed_proteins` retains its current value on an unchanged payload — pin it against a
   real pinned-run payload, not a synthetic one;
10. **non-vacuity**: collapse the split back to one number and prove tests 1–3 go **red**.

## 7. Gates

Focused selection + **SMOKE**. Every job through `bounded_run.py`, `--task C-101`,
`PYTHONPATH=<tree>/src`, `T2PW_OFFLINE_CURATOR=1`, unique short `--basetemp` under `C:/t/` with the
parent pre-created, `--heavy-lock C-101` for SMOKE. Pin verdicts in `evidence/g11/pin/C-101/`.
Every job reports `FINAL SURVIVING COUNT : 0` and `cleanup : success`.

**G9:** this is a **new capability**, not a correction of prior observable behaviour — so it carries
an **explicitly labelled new acceptance test** and needs no fabricated base failure. Test 9
(`placeholder_backed_proteins` unchanged) is the regression half and **must** pass at base and tip.

## 8. Budget

Hand-authored: **≤ 250** added+deleted lines across production + docs, plus the test file. Generated
artifacts: **≤ 30**. Exceed either and **stop and report**.

# C-086 — a component match must not promote a single enzyme to a superset complex

**Card type:** production correction — **complex-wrapper resolution only**.
**Finding:** F-116 (`LEDGER.md` § "F-116", registered 2026-08-25, measured by REV-081).
**Base SHA:** `91b5c50`
**Branch:** `card/C-086-f116`
**Worktree:** `C:/t/c086` (already created for you — work only there)

---

## 1. Read first, in this order

1. `docs/pwml_recovery_sprint/prompts/_SHARED_BLOCKS.md` — **S1, S3, S4, S5, S6, S7 and S8 all bind
   you.** S8 (test-process lifecycle) is a hard merge rule.
2. `docs/pwml_recovery_sprint/PRODUCT_CONTRACT.md` — outranks any test or benchmark result.
3. `docs/pwml_recovery_sprint/LEDGER.md` § "F-116" — the measured defect. Read it in full.
4. `docs/pwml_recovery_sprint/DECISIONS.md` § **D-064** — the identity ruling. `EntE` and
   `enterobactin synthase` are the **same protein identity**; that is not the defect here.

## 2. The defect

`_rewrite_reaction_protein_enzymes_to_complexes` (`src/t2pw/mapping/map_ids.py:8668`) replaces
bare-protein reaction actors with PathBank `protein_complex` wrappers. It is strict-mode-only, gated
by `allow_complex_wrapper_creation=not research_mode`. Its in-source rationale is that *"the
PathWhiz importer refuses a bare protein as a reaction enzyme."* **That rationale is legitimate and
you must not break it.**

On `PMC12096016/strict` it resolved **EntE** onto PathBank complex **3623**, whose components are a
strict **superset**: `EntB P0ADI4, EntD P19925, EntF P11454, EntE` — `chosen_rule
pathbank_protein_complex_id`, `confidence 1.0`, `source db`.

Its siblings resolved to **one-component** wrappers that preserve identity exactly: EntC→1143,
EntB→1189, EntA→1190. **Those must keep working unchanged.**

**The biological consequence.** The 2,3-DHB adenylation step (EC 6.2.1.71, **EntE alone**) now
carries EntB, EntD and EntF as catalysts, none of which performs it. `reactions[4]`
("EntF-catalyzed enterobactin synthesis") collapsed onto the **same** complex, so two chemically
distinct steps became indistinguishable by actor — destroying the gold's own requirement of *"a
named enzyme per step"*.

## 3. Required direction — the product owner's words

* A **component match must not silently promote a single enzyme to a superset complex.**
* **Distinguish** an assembly that performs a pathway *collectively* from the catalyst assigned to
  *one reaction*.
* **Preserve correct complex mappings** when the reaction truly uses the complete complex.
* **Do not break legitimate component-to-complex resolution globally.** The one-component wrappers
  (EntC→1143, EntB→1189, EntA→1190, ALAS2→ALAS2 complex, CYP51A1→442) are the control set.

## 4. Ownership boundary — do not exceed it

**You own:** `src/t2pw/mapping/map_ids.py`, function
`_rewrite_reaction_protein_enzymes_to_complexes` and any **new private helper you add beside it**.

**You may add** test files under `tests/`.

**Forbidden files, absolutely:**
* `src/t2pw/app/streamlit_app.py` — product-owner owned, uncommitted edit present.
* `src/t2pw/pipeline/release_status.py` — owned by C-087 this wave.
* `src/t2pw/bench/gold/pinned_v1.json` and anything under `src/t2pw/bench/gold/` — gold is
  orchestrator-only.
* `data/enrichment_cache.json`, `data/id_mapping_cache.json`, `out/`, `tmp/`, `outputs/`,
  `topics*.txt` — **never stage a cache or scratch file.**

If the correct fix genuinely requires a file outside your boundary, **stop and report that** rather
than widening. A boundary breach is an automatic reject.

## 5. Merge gates you must satisfy

All ten standing merge rules in `CLAUDE.md` plus:

**G9 is the one that fails people.** This is a **correction of pre-existing observable behaviour**,
so you owe a proof that **fails behaviourally on the base SHA `91b5c50`** and passes at your tip.
**Symbol absence is not proof.** Construct the EntE/3623 superset shape as a fixture and show the
base tree produces the superset actor and your tip does not.

**Required fixtures — name them explicitly:**
1. **EntE → complex 3623** (superset: EntB, EntD, EntF, EntE) — must no longer promote.
2. **EntF → the same complex 3623** — the two-steps-collapse case; the two reactions must remain
   distinguishable by actor.
3. **One-component controls that must be unchanged:** EntC→1143, EntB→1189, EntA→1190.
4. **An unrelated complex control** — a reaction that genuinely uses a complete multi-component
   complex must still resolve to it.
5. **Non-vacuity proof** — show each new test actually exercises the changed branch.

## 6. Test-process lifecycle — S8, HARD

**Every** pytest, script and command runs through
`docs/pwml_recovery_sprint/evidence/bounded_run.py`. Requirements:

* real `--timeout`;
* a **fresh** G11 path from
  `.venv/Scripts/python.exe docs/pwml_recovery_sprint/evidence/g11/g11_evidence.py next --task C-086 --label <label>`
  — **never hand-form the path**; the tool prints it on the last line;
* `PYTHONPATH=C:/t/c086/src`;
* a unique `--basetemp` **whose parent directory you create first** (F-114: a missing parent errors
  every test in setup and looks exactly like a large regression);
* `--basetemp` is **mandatory** — without it 83 tests error with `PermissionError`;
* never `pytest -n auto`; never run the full suite unchunked (~16 GB); one heavy job at a time.

After **every** job confirm `FINAL SURVIVING COUNT : 0` and `cleanup : success`. A run with
survivors is an **infrastructure failure, not a test result** — rerun it.

**Forbidden:** `taskkill /IM python.exe`, `pkill python`, killing by name or count, direct unbounded
Python, detached or background processes, `nohup`. Two `ms-python.isort` `lsp_server.py` processes
belong to the product owner's IDE and are **never** targets.

## 7. Base-tree measurement — D-066

If you measure the base tree, it **must** go through `pinned_pytest.py` with `--expect-tree` and a
committed `--pin-verdict`. `pytest.ini` carries `pythonpath = src` (C-070) and pytest **prepends**
it, so an **unpinned base-tree run is not evidence** and will be rejected.

## 8. Credit conservation

Work **offline**. Source inspection, stored T-104/T-105/T-106 artifacts, deterministic replay and
focused unit tests only. **You may not run a live paper leg or any LLM-backed command** — only the
orchestrator may. If you believe one is needed, stop and say exactly what unresolved property
requires it.

## 9. Report

Use the S6 format. Commit your work on `card/C-086-f116` with `git commit -F` (never `-m` with a
here-doc). Commit your G11 reports with the branch. Do **not** merge; the orchestrator merges.

State plainly in `## RISK` whether your fix could change any *other* reaction's actor resolution,
and give the measured blast radius rather than an assurance.

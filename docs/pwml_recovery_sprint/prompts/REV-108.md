# REV-108 — independent review of C-108 (F-155, all five members)

- **Reviewing** `card/C-108-f155-class` against base `f67e00a`
- **You are not the author.** You did not write this diff and you must not fix it. If it needs
  code, the finding goes back to the author.
- **These criteria are fixed BEFORE the diff exists.** They were written at charter time and
  committed with the card. If you want to add a criterion after seeing the diff, add it — but say
  plainly that it is an addition, so the record shows what was anticipated and what was not.

**Worktrees:** `C:/t/rev108` (tip) and `C:/t/rev108base` (base `f67e00a`). **Prune neither.**

---

## The one instruction that matters most

**Reproduce every number yourself. Reading the author's report is not verification.**

This wave, three separate load-bearing claims were corrected by the person on the other side of
them — the implementer corrected the Lead's measurement, the reviewer corrected the Lead's path
count, and the Lead verified the reviewer's blocking finding before spending a round on it. **Every
one of those corrections came from re-running, not from re-reading.**

---

## Blocking criteria

| # | Criterion | How, exactly |
|---|---|---|
| **B1** | **The F-146 patch is still `REJECTED`** | Run `c107_battery.py` yourself at the tip. `F146=REJECTED`. **Anything else is an instant reject, whatever else the card achieved.** |
| **B2** | **The 29-case battery is 0 mismatches** | Same run. `battery=0/29`. **1 is a fail.** Do not read the number from the report. |
| **B3** | **Member (a) is actually closed** | Same run. `C5=0`. At base it is `C5=1` — confirm that at `rev108base` so you know the instrument moved for the right reason. |
| **B4** | **No legitimate catalysis span newly refused** | Corpus, newly-REFUSED direction, **rows quoted**. Every row must be defensible as a *correct* refusal. One legitimate enzyme refused is a blocking finding. |
| **B5** | **No inhibition/attenuation span newly admitted as catalysis** | Corpus, newly-ADMITTED direction, rows quoted. This is the merge-rule-6 direction — **a row admitted here is worth more scrutiny than ten refused.** |
| **B6** | **Both directions reported separately, never netted** | If the author reports a net anywhere, that alone is a finding. |
| **B7** | **Every member's G9 label is honest** | (a), (c), (d) carry **base failures that fail at `f67e00a` and pass at the tip** — run them at both. (b) and (e) are **labelled new** and must carry **no fabricated base failure**. **Mislabelling coverage as a regression fix is a reject.** Symbol absence is not proof of anything. |
| **B8** | **The contra was not weakened to close (d)** | Merge rule 6. Inspect the guard that was **removed or narrowed**, not only the one added. Prove `"A significantly inhibited P activity in the assay"` still REFUSES, and that a span saying the actor is shut down still refuses under **rephrase**, not just in the quoted frame. |
| **B9** | **The three (e) anchors are genuinely covered** | For each of the three, **remove the anchor and watch the new test go red**, then restore. Use C-106's `apply_mutation` / `restore_saved_bytes` — **D-084: restores replay SAVED BYTES.** A test that stays green with its anchor removed covers nothing. |
| **B10** | **Member (b) moved no behaviour** | Corpus 0/0 for that member, and the author's consumer enumeration is **complete** — check it yourself with a grep, do not accept the list. `_match_fold` untouched. |
| **B11** | **Boundary** | `apply_audit_patch.py` + `tests/test_c108_f155_class.py` and nothing else. `git diff --stat` against base. |
| **B12** | **Job lifecycle** | Every job `FINAL SURVIVING COUNT : 0`, `cleanup : success`, shape-validated G11 path, heavy lock released. A run with survivors is an **infrastructure failure, not a test result**. |

---

## Adversarial work you must do — not optional, and not the author's list

**B13. Design your own mutation.** At least one that the author did not supply. The author's
mutations test what the author thought of.

**B14. Try to defeat the fix by rephrasing.** The whole thesis of this class is that the audit stage
**regenerates its rationale every round**, so a fix bound to one wording closes nothing. For each of
(a), (c), (d), write **three paraphrases that mean the same thing** and measure them. Specifically:

- (a) — if `"add P as a transporter"` is now refused, what about `"P should be added as a
  transporter"`, `"P is added to the transporters"`, `"the transporter P resolves the
  inconsistency"`? Which of those *should* be refused, and which are legitimate?
- (c) — an actor named `"X synthase"` in a span that genuinely predicates, and one that only names.
- (d) — `"the repressor P4X catalyses"`, `"P4X, a repressor, catalyses"`, `"the inhibitor of P4X
  was added"`. **The third must still refuse.**

**B15. Substring collisions, including any the fix newly creates.** `reduc`/`reductase`,
`block`/`blocker`, `silenc`/`silencer`, `interfer`/`interferon`, `transport`/`transporter`,
`mediat`/`intermediate`. **Then look at every stem the diff adds or changes and ask the checklist
question: is this stem anchored as a word on both sides?** That question has now found the same
defect three times in three consecutive cards. It is a checklist item, not a discovery.

**B16. Actor names containing enzyme or transport nouns**, on both sides of the line: one that must
still license, one that must not.

**B17. Period-stripped multi-sentence evidence.** Two sentences, a contra in one and a cue in the
other. Show what happens and say whether it is right.

**B18. Run the selections SPLIT as well as combined.** A file that passes in a combined run and
fails alone is a real finding, and it has been found this way before on this file.

**B19. Attribute, do not infer.** Use `c107_1g_stage_attribution.py`. **Do not conclude which guard
fired from the final verdict.**

---

## Deterministic gates

- **SMOKE — 503 passed, 22 files.** Anything reporting 473, 465, 460 or 457 is stale.
  **SMOKE writes to tracked `src/t2pw/bench/acceptance.py` and restores it in a `finally`.**
  **Hash that file immediately before and immediately after. A changed hash is an infrastructure
  failure even if pytest reports green. Never parallelise SMOKE. Never `pytest -n auto`.**
- **Gold readers — 456 passed / 8 skipped / exit 0.** A charter claiming this selection correctly
  exits 1 is **stale after C-103**.
- `--basetemp=<unique short dir under C:/t/>`, **parent pre-created**. Without it 83 tests error
  with `PermissionError` and you will report a false regression. A missing parent once reported
  **382 instead of 453**.
- The explicit venv interpreter. **F-143: a bare `python` is system 3.13 with no `streamlit` → 35
  spurious import errors that read exactly like a regression.**
- **Your own shell timeout must exceed the wrapper's `--timeout`**, or you kill the wrapper before
  its cleanup `finally` runs and strand the heavy lock.

---

## Verdict

**APPROVE**, **APPROVE WITH REGISTERED FINDINGS**, or **REJECT WITH BLOCKING FINDINGS**.

**If what remains is registerable rather than blocking, say APPROVE and register it.** Do not spend
a correction round on something that belongs in `FINDINGS.md`. C-107 finished in two rounds because
the reviewer was told this plainly, and it is repeated here for the same reason.

**Keep every failed measurement beside its correction.** A measurement that turned out wrong is part
of the record, not an embarrassment to delete — this sprint has twice found a real defect inside an
anomaly someone nearly discarded.

**Before your review closes:** collect your G11 reports, **your probe sources**, and your
substantive outputs into a commit on the branch. **A G11 report proves clean termination and
preserves nothing about what the job found.** 72 reviewer reports and 94 probes were nearly lost
this sprint exactly this way.

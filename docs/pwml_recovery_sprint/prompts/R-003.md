# R-003 — False-identifier triage

**Read-only. No branch. No code.** Agent type: `pwml-bio-auditor`.
Its output becomes the body of `C-060`, which is not dispatchable until this delivers.

---

```
[S1] [S5] [S7]

ROLE
  Biological adjudication. You classify findings; you do not fix them and you do
  not propose patches beyond naming the expected correction.

THE FINDINGS
  The committed benchmark run runs/2026-08-02_2130 scores 10 FALSE REAL
  IDENTIFIERS across three papers. Reproduce with:

    .venv/Scripts/python.exe scripts/bench_acceptance.py \
      --run-dir runs/2026-08-02_2130 --json <tmp>/acc.json

  A pre-generated copy with sha256
  d3538f4b1cefc1f8e7aca933318df13c9967ce1071e48f8e6a3e4bd6830f4ec3 is committed at
  docs/pwml_recovery_sprint/evidence/baseline_acceptance.json.

  Known pointers, from the diagnostic pass:

    PMC12856317/research  /entities/compounds/4   pyridoxal 5'-phosphate
                          kind=false_real_identifier
                          forbidden_kind=cofactor_as_protein
    PMC12856317/strict    /entities/compounds/1   hemin  (placeholder_product)
                          /entities/compounds/5   PLP    (cofactor_as_protein)
    PMC12096016/strict    /entities/compounds/5   nadh   (placeholder_product)
                          /entities/compounds/12  nad+   (placeholder_product)
                          /entities/compounds/13  NAD    (unmapped)
    PMC12180156 both      5,10-methylene-THF, formate, THF, citrate, succinyl-CoA
                          (placeholder_product)

  The gold set's rationale for the NADH/NAD+ pair is that they come from an LDH
  COUPLED ASSAY -- assay reporters, not pathway members.

FOR EACH FINDING, DELIVER
  1. CLASS: product_contract_violation | gold_data_defect | policy_disagreement.
     Cite the gold case's relevance_note and/or export_rationale. Only the first
     class justifies code.
  2. ORIGIN STAGE, if the artifacts support one. Available today:
       - stage1_payload.json                    what Stage 1 produced
       - merged_payload.json                    post-Stage-2, PRE-mapping (carries
                                                NO accessions -- "zero false
                                                identifiers" there means the file
                                                cannot answer the question)
       - rag_admission_report.json              what the admission gate accepted
                                                and rejected
       - final_mapped.json                      post-mapping; CANONICAL only when
                                                quarantine succeeded -- check
                                                quarantine_ok first
       - rag_provenance / source_papers / rag_confidence on process rows survive
         _clean_processes today (pipeline.py:1880), so a RAG-imported reaction is
         attributable. A row WITHOUT a carrier is paper-explicit OR predates the
         instrumentation. Distinguish those two; do not assume.
     Where attribution genuinely requires lineage that does not yet exist (C-015,
     C-034..C-038, C-044), SAY SO and name the instrumentation needed. Do not
     guess a stage.
  3. NEEDS LINEAGE? yes/no. This is the split that decides scheduling:
       - the cofactor / assay-reporter class (PLP, NADH, NAD+, SAM, SAH, Zn2+)
         is expected to need NO lineage -- the pointers are already known and
         C-018 (cofactor_policy) can act on them in Wave A0
       - the cross-pathway import class (formate, 5,10-methylene-THF, THF,
         citrate, succinyl-CoA in PMC12180156) is expected to NEED attribution
     Confirm or refute both expectations from the artifacts.
  4. AFFECTED FILES, if a code change is warranted.
  5. EXPECTED CORRECTION: what "right" looks like for that entity.
  6. REGRESSION FIXTURE: the minimal payload that reproduces it.

CONSTRAINTS
  Committed artifacts only. Never quote a number whose source run is not
  committed. Distinguish what static inspection proves, what tests prove, what
  committed artifacts demonstrate, and what stays hypothesis.

  TRAP-3 applies: placeholder_backed_proteins (21 in this run) is a STANDING
  POLICY DISAGREEMENT, not a defect. Do not classify it as one and do not propose
  a fix. If a finding of yours touches it, say so and escalate.

DELIVER
  A table of all 10 findings with fields 1-6, plus:
  ## SUMMARY BY CLASS        how many of each; how many justify code
  ## NEEDS-LINEAGE SPLIT     which are actionable NOW vs after lineage
  ## PROPOSED C-060 SCOPE    what one narrow branch should fix, and what should
                             be a separate branch
  ## CONFIDENCE + WHAT WOULD CHANGE MY MIND
```

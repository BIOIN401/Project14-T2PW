# C-115 — re-pin the five c102 census pins to the committed corpus (F-171 / F-151 recurring)

**Chartered by the Lead Orchestrator, 2026-09-02, `ORCH-719`.**
**This unblocks merge rule 10, which is currently RED on the integration branch itself.**

**Read `FINDINGS.md` F-171 before starting.** SMOKE at integration tip `9873d064` — containing no
card's work — is **3 failed / 500 passed**. The population of committed `quarantine_report.json` was
**already 83 at this wave's takeover anchor `7a1bb338`**; the c102 suite pins **72**. T-108's own
result commit `479128b3` added its run tree and left the pins stale. **Nothing about the pipeline is
broken and this card fixes no pipeline behaviour.**

---

```
[S1] [S3] [S4] [S5] [S6] [S8]

BRANCH        agent/c115-census-repin
BASE          <the integration tip at dispatch — the Lead gives it to you; do not guess>
WORKTREE      .claude/worktrees/c115-census-repin   (isolated; worktrees have NO .venv --
              use the PRIMARY checkout's .venv/Scripts/python.exe by absolute path)
WAVE          post-T-108 correction
DEPENDS ON    NONE

OWNS — exclusive. A diff outside this list is an automatic reject.
  tests/test_c102_coverage_denominator.py :: the five census pins and the attribution
                                             comments beside them, and nothing else

OBJECTIVE
  Five pins in the c102 suite record how large the committed artifact corpus is. T-108's
  run tree was committed at 479128b3 and the pins were not moved, so three SMOKE tests
  have been red on the integration branch ever since and merge rule 10 cannot be
  satisfied by any card.

  After this patch the five pins equal the measured corpus, each carries a per-run
  attribution comment saying WHICH run's legs account for the delta, SMOKE is green, and
  the c102 mutation attack still kills every mutant it killed before.

  Falsifiable: `pytest tests/test_c102_coverage_denominator.py
  tests/test_c106_mutation_harness_executable.py` goes from 3 failed to 0 failed, and the
  count of committed quarantine_report.json files equals the floor pin exactly.

MUST NOT CHANGE
  Any test LOGIC. You move NUMBERS and add COMMENTS. If you find yourself editing an
    assertion's shape, a loop, a helper or a fixture, STOP -- you have left the card.
  The `>=` on the floor pin (:380) stays `>=`. The four derived pins stay `==`.
    tests/test_c102_coverage_denominator.py:408-414 and
    test_c106_mutation_harness_executable.py:314 both say why in terms, and
    test_09_the_derived_census_pins_are_equality_not_a_floor EXISTS TO CATCH YOU
    relaxing one. Read both comments before touching a line.
  The committed run trees. Do NOT delete, prune or move anything under runs/ or
    runs_verify/ to make a count match. That is the forbidden direction and it destroys
    accepted evidence.
  tests/test_c106_mutation_harness_executable.py -- NOT OWNED. Its test_08 reads the c102
    floor pin OUT OF THE C102 SOURCE, so fixing c102 fixes it. If it does not go green on
    its own, report that; do not edit it.
  Any production file. src/ is entirely outside this card.
  The gold set: src/t2pw/bench/gold/pinned_v1.json stays byte-identical at blob
    36f4b7b690b577f72882c3045ca6728d1ec8d9d1.

IMPLEMENT
  1. Re-measure with the probe the failing test itself names:
       evidence/orch717_census_probe.py
     Do not hand-count, and do not read the numbers off the assertion errors -- an error
     message tells you what one assertion saw, not what the census is.
  2. Move these five pins to their MEASURED values. The failing test names them and warns
     that they move together; here are their addresses at the base tip:

       tests/test_c102_coverage_denominator.py:380   assert len(paths) >= 72
       tests/test_c102_coverage_denominator.py:415   assert legs == 72
       tests/test_c102_coverage_denominator.py:473   assert withheld == 100
       tests/test_c102_coverage_denominator.py:583   assert checked == 72
       tests/test_c102_coverage_denominator.py:608   assert with_matched_forbidden == 29

     Verify each address before editing -- line numbers drift.
  3. Beside EACH moved pin, record the attribution in a comment: the old value, the new
     value, and WHICH RUN accounts for the delta (`runs_verify/2026-09-01_1612`, T-108,
     20 legs, committed at 479128b3). The comment at :408-414 says this is the point of
     moving them by hand -- "that is the moment someone confirms the new legs were meant
     to be there." Your comment is that confirmation, so make it say something.
  4. Re-run evidence/c102_mutation_attack.py. The failing test's own message requires it.
     Report its verdict verbatim. If it kills fewer mutants than before, STOP and report:
     a re-pin that weakens the mutation harness has broken the tripwire while appearing to
     fix it.

TESTS YOU MUST ADD
  NONE. This card adds no test and changes no behaviour.
  G9 CLASSIFICATION: this is neither a correction of pre-existing observable behaviour nor
  a new capability -- it is a PINNED BASELINE MOVE under merge rule 4 and S3 item 6, which
  require it to be deliberate with an EXACT DOCUMENTED DELTA. Your report's delta table IS
  the G9 artifact here. Do not fabricate a base failure; the base failure is real, measured,
  and recorded in F-171 with a committed G11 report (ORCH-719/10).

FOCUSED TESTS TO RUN
  All through docs/pwml_recovery_sprint/evidence/bounded_run.py with the PRIMARY checkout's
  .venv/Scripts/python.exe, a real --timeout, --heavy-lock C-115, --basetemp under C:/t/
  with the parent PRE-CREATED, PYTHONPATH=<your worktree>/src, PYTHONIOENCODING=utf-8,
  PYTHONDONTWRITEBYTECODE=1, T2PW_OFFLINE_CURATOR=1.

  *** pinned_pytest.py EXITS 98 WITHOUT PYTHONPATH. *** You get
  T2PW_MEASUREMENT_TREE_REFUSED and ZERO tests run. It is not a failure of your patch.

  Allocate every --json with evidence/g11/g11_evidence.py next --task C-115 --label <l>.
  It prints TWO lines and THE PATH IS THE LAST LINE. Guard on the SHAPE of that path, never
  on the absence of error text -- a bad label yields an EMPTY string, which becomes
  --json "" and the job then runs clean and writes no artifact at all.

    1. the two census files alone, at BASE     -> expect 3 failed (the F-171 baseline)
    2. the two census files alone, at TIP      -> expect 0 failed
    3. SMOKE, 22 files, at TIP                 -> expect 503 passed, exit 0
    4. evidence/c102_mutation_attack.py        -> verdict verbatim

  Every job: FINAL SURVIVING COUNT : 0 and cleanup : success.

ACCEPTANCE — all must hold
  [ ] Exactly ONE file in the diff. `git diff --stat` proves it.
  [ ] Every change is a number or a comment. No assertion shape, loop, helper or fixture moved.
  [ ] The floor pin is still `>=`; the four derived pins are still `==`.
  [ ] test_09_the_derived_census_pins_are_equality_not_a_floor passes.
  [ ] SMOKE 503 passed, exit 0, zero survivors -- the first time in this sprint since 479128b3.
  [ ] The mutation attack's kill count is not lower than before.
  [ ] Nothing under runs/ or runs_verify/ appears in your diff.
  [ ] Gold blob still 36f4b7b690b577f72882c3045ca6728d1ec8d9d1.
  [ ] Every G11 report AND its .pin.json verdict committed under evidence/g11/C-115/ and
      evidence/g11/pin/C-115/; `g11_evidence.py check --task C-115` clean; no surviving
      .staging placeholder.

DECLARED BUDGETS (D-019)
  HAND-AUTHORED   : tests/test_c102_coverage_denominator.py only, <= 40 added, <= 10 deleted.
                    Manifest is exactly that one file.
  MACHINE EVIDENCE: <= 12 artifacts and <= 48 KB, COMPRISING up to 6 bounded_run cleanup
                    reports AND their up to 6 corresponding .pin.json verdicts.
                    *** The pin verdicts ARE counted, and they ARE mandatory under
                    TEST_MATRIX.md section 0 rule 10. *** This budget line is written this
                    way because C-114's was not, and was unsatisfiable as a result -- see
                    the LEAD RULING at the end of C-114.md.
                    Committed run artifacts: 0. You run no leg.

TRAPS
TRAP-A  (THIS CARD, the one most likely to catch you) THE FORBIDDEN FIX IS EASIER THAN THE
        CORRECT ONE. Deleting a committed run tree, pruning artifacts, or relaxing a
        derived pin from `==` to `>=` all turn the suite green in seconds. Every one of
        them destroys the tripwire. The corpus is CORRECT at 83; the pins are stale. Move
        the pins to the corpus, never the corpus to the pins.
TRAP-B  FIVE pins, not one. The failing message says "FOUR pins move together" and then
        lists five. Move all five, or you green one file and leave the next card the same
        bug with a smaller signature.
TRAP-3  placeholder_backed_proteins is a standing POLICY DISAGREEMENT, not a defect. No
        agent may "fix" it. Escalate.
TRAP-5  data/enrichment_cache.json is 39 MB and TRACKED. No branch may commit a cache
        modification. Never commit topics_*.txt, the stray 0-byte `ValueError`,
        src/t2pw/app/streamlit_app.py, or anything under out/, outputs/ or tmp/.
TRAP-6  F-160 binds: a same-length edit in the same second leaves a stale .pyc valid.
        PYTHONDONTWRITEBYTECODE=1 is the stronger guard and is required above.
```

---

## Why this is a re-pin and not a weakening, stated once

`tests/test_c102_coverage_denominator.py:408-414` answers this itself, about the pin it guards:

> *"Moving it by hand whenever a run is committed is the correct cost: **that is the moment someone
> confirms the new legs were meant to be there.**"*

The legs were meant to be there. `runs_verify/2026-09-01_1612` is T-108's run tree — 10 papers, 20
legs, `complete: true` — committed deliberately at `479128b3` as the preserved record of a failed
release candidate, and `HANDOFF.md` requires it be preserved. **The corpus is correct; the pins
describe a corpus that no longer exists.**

Merge rule 4 and S3 item 6 permit exactly this, on exactly these terms: a pinned baseline may move
**deliberately, with an exact documented delta**. Step 3 of IMPLEMENT is that delta.

# C-012 — `agent/p00b-driver-seam`

Wave A0. Agent type: `pwml-implementer` (isolated worktree). Reviewer: the C-011 implementer.
Conflict hotspot **rank 2**. `driver.py` is **single-owner-per-night**.

---

```
[S1] [S3] [S4] [S5] [S6] [S8]

BRANCH     agent/p00b-driver-seam
BASE       <CURRENT integration SHA, read with `git rev-parse sprint/pwml-recovery`
            at dispatch. Do not use a SHA copied from any document.>
WORKTREE   .claude/worktrees/p00b-driver-seam
WAVE       A0
DEPENDS    NONE

OWNS -- exclusive.
  src/t2pw/batch/driver.py :: _drive  (lines 1483-2012, 530 lines)
    -> extracted into _finalize_gate_failure, _finalize_pwml_export,
       _finalize_timeout
  tests/  -- ONE new test file, named in your report

  NOT YOURS, though they sit in the same file and you will be tempted:
    _add_common_artifacts (:1163), _add_identity_artifacts (:1195)  -> C-031
    _collect_app_text (:1117)                                       -> C-031
    RunOutcome (:648) and RunOutcome.to_dict (:714)                 -> C-041
  A diff touching any of those is an automatic reject.

OBJECTIVE
  _drive is 530 lines and is the single most contended function in the sprint:
  C-031, C-032, C-041 and C-053 all need to change its tail. As one block, those
  four branches serialize behind each other for no engineering reason.

  This is a PURE MOVE. Extract the three terminal paths out of _drive into named
  functions, changing NOTHING a caller or an artifact consumer can observe.

  After this patch: the three exit paths are separately addressable seams, and a
  byte-for-byte comparison of driver-observable behaviour across every committed
  leg fixture is EMPTY.

MUST NOT CHANGE -- this is the whole point of the branch
  - Exit classification: status, stage, failure_kind.
  - release_status: ABSENT before C-041. Assert it is still absent; do not add it.
  - Artifact filenames and paths: sorted(outcome.artifacts.keys()), and files[].name
    after _relocate_files.
  - Persisted diagnostic artifacts: files[].bytes per name.
  - Manifest fields: the full RunOutcome.to_dict() (:714).
  - Failure reasons: issue_codes, detail.
  - Messages: message, warnings.
  - canonical_payload_sha256.
  - Ordering of side effects. A finalizer that writes the same files in a
    different order is NOT a pure move.

  You are not fixing anything in this branch. If you see a defect, put it in
  OUT OF SCOPE and leave it. The missing quarantine artifacts (MASTER_PLAN 1.3)
  are C-031's, not yours.

IMPLEMENT
  1. Read _drive end to end and identify its three terminal paths: a blocking
     gate failure, a successful PWML export, and a timeout.
  2. Extract each into a module-level function taking an explicit argument list.
     No hidden state, no reliance on _drive's locals leaking in.
  3. _drive calls the three and returns exactly what it returned before.
  4. Do not "tidy" anything you moved. Move it verbatim, then adjust only the
     names that must change to compile.
  5. If a path cannot be extracted without a behaviour change, STOP and report
     which one and why. A reported blocker beats a silent behavioural drift.

TESTS YOU MUST ADD -- golden behavioural equivalence, per TEST_MATRIX
  A payload hash alone is INSUFFICIENT: a driver refactor can preserve the
  biological payload while dropping a quarantine report or misclassifying a
  timeout. Your test must compare, byte-for-byte, across every committed leg
  fixture, the full field list under "MUST NOT CHANGE" above.

  The "before" state is captured on the BASE SHA FIRST, before you change a line,
  and lives INSIDE your one new test file -- inline values, helpers or constructed
  objects. C-012 owns no path under docs/pwml_recovery_sprint/evidence/ and is
  budgeted 0 artifacts / 0 bytes, so nothing is checked in. If _drive needs real
  paths, use run-time framework temp storage only: untracked, never a deliverable,
  repo unchanged after. A persistent fixture needs a NEW ruling -- STOP and ask.

  Note the golden diff is the acceptance criterion for a pure move -- not a test
  count. A large new test file that does not compare before/after is not evidence.

FOCUSED TESTS  (through the bounded wrapper -- [S8])
  chunk B (225 tests, ~25 s) + your golden diff + your new file

ACCEPTANCE -- all must hold
  [ ] The golden driver-observable diff is EMPTY across every committed leg fixture.
  [ ] release_status is still absent from RunOutcome.to_dict().
  [ ] No file outside driver.py and your one new test file is touched.
  [ ] You did not touch _add_common_artifacts, _add_identity_artifacts,
      _collect_app_text, RunOutcome, or RunOutcome.to_dict.
  [ ] chunk B passes.
  [ ] Cleanup report attached; final surviving process count = 0.

TRAPS
  TRAP-4  Do not rebuild what exists -- MASTER_PLAN section 2 lists 15 items.
  TRAP-5  data/enrichment_cache.json is 39 MB and TRACKED. No branch may commit a
          cache modification.

BUDGETS (D-019, ratified by WAVE-A0-BUDGET-RATIFICATION-001) -- limits only.
  HAND-AUTHORED: at most 330 changed lines, additions PLUS deletions, across this
    card's complete authorized implementation manifest. A MAXIMUM -- not a target,
    not an expectation that every line be used. Anything outside OWNS stays barred.
  MACHINE-GENERATED EVIDENCE, governed separately: 0 artifacts, 0 bytes.
  Overrun of either, predicted or discovered: STOP BEFORE COMMITTING and obtain
    renewed product-owner authority; no role may self-authorize one.
  SPLITTING: only where each half is independently implementable AND independently
    validatable (D-019). Exceeding a threshold does not itself authorize or require
    a split; no split is preauthorized here.
  Ratifies limits ONLY: no implementation approval, no unblocking, no branch, no
    dispatch authority.
```

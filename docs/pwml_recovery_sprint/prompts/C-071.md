# C-071 — a fabricated actor stops being releasable (F-079)

- **Base SHA** to be set at dispatch (currently `sprint/pwml-recovery` tip `e616846`)
- **Branch** `agent/c071-actor-span-gate`
- **Finding closed** F-079 (HIGH) — `FINDINGS.md:2689-2753`
- **Ceiling** 700 changed lines / 140 changed docstring+comment lines / 1 MB artifacts.

## 1. The defect, measured at `e616846` by the orchestrator — do NOT re-derive this

The `research` leg of `runs_verify/2026-08-18_1328/papers/PMC12096016/` was classified
**`release_ready`** with **`semantic_evaluation: passed`** and **`strict_acceptance_eligible: True`**,
while carrying a fabricated transporter. Re-measured at tip by feeding the committed
`final_mapped.json` through today's `evaluate_production_semantics` → `semantic_verdict` →
`classify_release_status`:

```
evaluation                 = passed
status                     = 'release_ready'
strict_acceptance_eligible = True
semantic_failed_checks     = []
reasons                    = []
```

Byte-identical to the committed `quarantine_report.json` → `release`. **The finding still
reproduces in full.** No PACK 10 card touched the predicate —
`git log 931c065..HEAD -- src/t2pw/bench/semantic_production.py src/t2pw/bench/semantic.py src/t2pw/pipeline/release_status.py` is **empty**.

The payload row this card is about, re-read at tip:

```
transports[0] name='enterobactin secretion'
   transporters=[{... 'entity': 'EntE', 'provenance': 'inferred', 'source_refs':
   ['Enterobactin is produced in the cytoplasm and secreted to the extracellular
    environment by a TolC-dependent process']}]
```

**The cited span names TolC. The actor claims to be EntE.** The span is verbatim paper text
(`01_source_text.txt`; note `00_PAPER.txt` in that directory is a 709-char stub — the real
text is `01_source_text.txt`). This is the same class as F-058's fabricated transporter.

### Why nothing caught it — three measured reasons

1. **The only reaction-level chemical check is hardcoded inapplicable in production.**
   `semantic_production.py:302` `supported = _not_evaluated(_s.CHECK_SUPPORTED_REACTIONS, NO_GOLD_SIGNATURES)`
   — *"reaction-level support can only be audited against hand-read, quote-verified
   signatures, which exist only in the gold set"* (`:109-112`). **That is honest and must not
   be changed.**
2. **It would not gate anyway.** `release_status.py:93-98` `SEMANTIC_GATING_CHECKS` is exactly
   four: anchors, organism, ID conflict, RAG reintroduction. **Not one is sensitive to an
   actor that is not the protein its cited span names.** Measured at tip: 3 of 4 applicable,
   all `ok=True`.
3. **The cap only fires downward on `failed`.** `release_status.py:419-422` returns
   `SEMANTIC_PASSED` when nothing failed, and `:541` demotes only
   `if evaluation == SEMANTIC_FAILED and status == RELEASE_READY`.

## 2. ⚠ THE F-053 PROHIBITION IS IN FORCE AND THIS CARD MUST RESPECT IT

`DECISIONS.md:2939-2941`, **UNDISCHARGED** (verified: `DECISIONS.md` ends at D-055, and
D-054 §7 `:3179-3184` says C-056c *"makes the shortfall visible; it never closes it"*):

> **No card may be chartered to read `semantic_evaluation == "passed"` affirmatively — or to
> build any denominator, numerator or rate on it — until F-053 is discharged.**

**So you may NOT write this card as "read `passed`, then check something extra."** You must
make the defect produce **`SEMANTIC_FAILED` through a gating check**, and let the existing
subtractive cap at `release_status.py:541` do the demotion. That respects the prohibition
rather than testing it.

**F-079 is itself the evidence the prohibition should stay** — a `passed` verdict has now
been observed on a payload carrying a fabricated actor, so a consumer treating `passed` as
positive evidence would be consuming a value that measurably did not earn it. Do not propose
lifting F-053. Do not add an affirmative reader.

## 3. The required behaviour — decided by the orchestrator, do not re-litigate

Add **one** new semantic check: **an actor whose entity name does not appear in the span it
cites as its own evidence is a failed check.**

Scope it to **actor sub-entries** — `enzymes[]`, `modifiers[]`, `transporters[]` — comparing
each entry's `entity` against its own `source_refs` / evidence span.

**Why this one and not the other two candidates**, so you do not spend effort re-deciding:

* *Wrong reaction product* (`EntE ... outputs: ["enterobactin"]`) would need a
  reaction-class/product table — chemistry knowledge this seam does not have and cannot get
  offline.
* *Self-referential evidence* (a row whose whole evidence string is its own name) is
  string-decidable but narrower.
* **The actor/span mismatch is string-decidable, offline, deterministic, and generalises** —
  it is exactly the F-058 fabricated-transporter class and the *"species derived from an
  enzyme name, then stamped explicit"* family F-078 also names.

**HARD CONSTRAINT, verified:** `tests/test_semantic_production_no_gold.py:20` pins
`REMOTE = ("openai", "requests", "httpx", "urllib.request", "http.client", "socket", "ssl", "sqlite3")`
and `:186` `test_f_no_network_database_or_llm_on_any_path` asserts none is importable from
`semantic_production`. **Your detector must be pure, offline and deterministic.** No LLM, no
network, no DB.

**Be careful about what counts as a match.** A naive substring test will both over- and
under-fire. State your matching rule explicitly, justify it, and test both directions. An
actor with **no** cited span is a different case from one whose span contradicts it — decide
which is `ok`, which is `failed`, and which is **inapplicable**, and say why. Reuse the
repo's existing name-normalisation if one fits rather than inventing a second one.

## 4. Ownership boundary — exact, three files

| Path | Scope |
|---|---|
| `src/t2pw/bench/semantic.py` | **new** `CHECK_*` name constant beside `:81-101`; one appended name in `CHECK_ORDER` (`:103-112`); `ERROR_ORDER` (`:135-145`) **only if** the check contributes an error count. **No existing function body.** `SemanticReport.failed_checks` (`:303-305`), `inapplicable_checks` (`:307-309`) and `to_dict` (`:312-329`) already iterate `CHECK_ORDER` and pick it up with no edit. |
| `src/t2pw/bench/semantic_production.py` | **one new module-level `_check_*` function**; `evaluate_production_semantics` (`:271-323`) gets **two lines** — call it, and add it to the `for result in (...)` tuple at `:314`. |
| `src/t2pw/pipeline/release_status.py` | `SEMANTIC_GATING_CHECKS` (`:93-98`) — **one appended literal. Nothing else in this file.** |
| test files | the new acceptance test, plus the minimum edits to the pinned tests in § 6 |

**You own nothing else.** In particular:

* **`semantic_production.py:302` must NOT be touched.** `CHECK_SUPPORTED_REACTIONS` is
  honestly inapplicable in production; making it applicable would mean inventing gold
  signatures. That is fabricating evidence, and it is a reject.
* **`src/t2pw/pipeline/strict_quarantine.py` is out of bounds.** The seam at `:2364-2390`
  already wires this correctly and needs no edit. It is also C-067's file — do not re-enter.
  D-042 §4's ungranted `admission` parameter stays ungranted.
* **`src/t2pw/pipeline/stage_one_boundary.py` is out of bounds** — that is F-078's surface,
  a different package and stage. **Do not combine the two.** Zero file overlap is required.
* **Never touch `src/t2pw/app/streamlit_app.py`** — uncommitted product-owner edit.
* `src/t2pw/batch/driver.py` and `src/t2pw/bench/acceptance.py` are read-only. D-054 §7 says
  the affirmative-reader surface is not this work's.
* Never edit `evidence/bounded_run.py` or `evidence/g11/g11_evidence.py` — call them.
* Do not edit control-plane markdown. Report the prose you want changed.

## 5. ⚠ THIS CARD MOVES A PINNED BASELINE BY CONSTRUCTION — declare it, do not absorb it

`tests/test_semantic_release_gating.py:192-223`
`test_new_acceptance_the_gating_set_is_closed_at_exactly_four` asserts the tuple literally,
`len == 4`, `len(set) == 4`, and that four named checks are **excluded by name**, with the
stated purpose *"adding a fifth gate silently is impossible."*

**That test is doing its job, and this card is the deliberate act it was built to force.**

* State the delta exactly: `SEMANTIC_GATING_CHECKS` **4 → 5**, one named addition.
* Update the test to assert **five**, keeping its closure property intact — it must still
  make a *sixth* silent addition impossible, and must still assert the excluded names.
  **Do not weaken it into a `>= 4` or delete the closure assertion.**
* **Rename it if its name now lies.** A function called `..._closed_at_exactly_four` that
  asserts five is worse than no test.
* Say in your report that this is a merge-rule-4 baseline move, so the orchestrator can carry
  it into the ratification record. The D-044 / D-052 §1 standing rule is *"a widening that is
  the mechanism of an already-granted change is ratifiable, but it must be disclosed and
  ratified in the record — never absorbed silently."* **Disclosure is your obligation; the
  ratification is the product owner's.**

**⚠ Merge is expected to be HELD pending that ratification.** Implement fully anyway — the
work is not wasted, and a one-line ratification then unblocks the merge.

## 6. Consuming tests — measured; the chunk table will NOT cover you

**Ten of twelve are in NO chunk.** This is the F-054 hazard live, so the focused set is named
here explicitly and you must run all of it:

| stem | what it pins | chunk |
|---|---|---|
| `test_semantic_release_gating` | **THE blocker**, § 5 above; also `:160-189` all three release states reachable with no semantic demotion, `:225+` the request derivation | none |
| `test_semantic_production_no_gold` | `:63` clean payload passes; `:85` gold-only check neither pass nor failure; `:102` five D-006 states reachable; `:130`/`:186` import-graph and no-network pins | none |
| `test_c056b_semantic_denominators` | `:304` `CHECK_SOURCE_CARRIER not in SEMANTIC_GATING_CHECKS`; indexes `SEMANTIC_GATING_CHECKS[0..3]` at `:168,186,321,349,377` | none |
| `test_c056c_semantic_evaluability` | ~15 refs; asserts `len(entries) == len(SEMANTIC_GATING_CHECKS)`; `:186`/`:402` sweep widths `0..4` | none |
| `test_c056d_gate_failure_semantic_carry` | gate-failure carry of the verdict | none |
| `test_release_status_classification` | `:241-255` not-evaluated is never false / never a pass | none |
| `test_strict_quarantine_release_seam` | seam reachability set incl. `release_ready` | none |
| `test_batch_pwml_artifact_naming` | `:86` a `semantic_evaluation` literal in a fixture | none |
| `test_compound_resolution_extraction` | `:469-546` `GOLDEN` pins the sha256 of `build_pwml_ir`'s `(ir, report)` for this exact leg | none |
| `test_bench_goldset_and_semantic` | imports all eight `CHECK_*` at `:71-78`, per-check `.ok` through `:423-691` | **Chunk B → SMOKE** |
| `test_strict_quarantine` | `:1195-1218` spies on `classify_release_status`; `:895-901` pins the additive keys | **Chunk A → SMOKE** |

**Two consequences already measured — use them, do not rediscover them:**

* `test_c056b` and `test_c056c` **index and sweep the tuple by width**. A 5th entry moves
  them. Expect real edits there and justify each one.
* `test_compound_resolution_extraction`'s `GOLDEN` hashes the **IR built from the payload**.
  **A classification-only fix does NOT move it.** If you find yourself needing to move that
  golden, you have left your boundary — stop and report. C-068's comment at `:525-546` names
  all three F-079 defects and says explicitly *"F-079 is NOT fixed here"*.

## 7. Proof obligations

**G9 — this is a CORRECTION of pre-existing observable behaviour, not a new capability.**
Mislabelling it as new functionality is a reject under merge rule 9's last sentence: the
reclassification of an existing committed leg is a change to pre-existing observable
behaviour. The base half is already measured for you and is genuinely behavioural:

```python
report = evaluate_production_semantics(load(RESEARCH_LEG),
                                       requested_pathway="enterobactin biosynthesis",
                                       requested_organism="Escherichia coli", mode="research")
assert semantic_verdict(report)[0] != SEMANTIC_PASSED
```

fails at base (`passed`, `release_ready`, `strict_acceptance_eligible=True`) and must pass at
your tip. **Paste both halves from your own runs** — do not cite this charter for them.

Derive the request exactly as the pipeline does:
`pathway_context_from_stage_zero({"pathway_name": "enterobactin biosynthesis", "likely_organism": "Escherichia coli"})`,
taken from the committed `runs_verify/2026-08-18_1328/manifest.jsonl` `observed_context`.

**The card ALSO carries an explicitly-labelled NEW acceptance test** for the detector's own
semantics — true positive, true negative, and inapplicable-when-no-span. The repo's precedent
for the label is `tests/test_semantic_release_gating.py`, where three functions carry
`# NEW ACCEPTANCE — no runtime semantic verdict exists at the base`. Both obligations apply;
neither substitutes for the other.

**Verify through the production entry point.** A check that works when called directly but
is never reached by `evaluate_production_semantics` proves nothing.

**Non-vacuity (RULING 13):** your detector is exactly the kind that can silently stop testing.
**Neutralize it so it examines zero actor rows and show the new gating check still reports
`ok`** — then fix that and show the fixed version fails on the F-079 leg. A check that passes
because it found nothing to examine is the failure mode here, and it looks identical to
success.

**Do not weaken a biological gate to increase PWML production** (merge rule 6) and **preserve
incomplete-but-correct pathways as `review_required` rather than dropping them** (rule 7).
This card moves one leg from `release_ready` to demoted — confirm which status it lands in and
that the landing is the contractually right one, rather than assuming.

## 8. Process

* Every run through the integration copy of `bounded_run.py`, real `--timeout` comfortably
  below your own tool wall clock (≤300 s).
* `--json` from `g11_evidence.py next --task C-071 --label <label>`, allocated immediately
  before the job.
* `--basetemp` under `C:/t/c071tmp/`, parent pre-created, on **every** pytest invocation.
  Without it 83 tests error with `PermissionError` and you will report a false regression.
* `PYTHONPATH=<tree>/src` on every invocation — `t2pw` is not installed and no `.pth` exists.
* Paste `FINAL SURVIVING COUNT: 0` and `cleanup: success` per job.
* Run `g11_evidence.py check --task C-071` before reporting.
* **You may NOT take the heavy mutex.** SMOKE is affected here (Chunks A and B both carry a
  consuming test). Run your focused set yourself; **ask the orchestrator for SMOKE** and wait.
  Do not create `C:/t/heavylock` and do not claim you acquired it.
* Never `pytest -n auto`. Never run the full suite unchunked (~16 GB).

## 9. Report

`_SHARED_BLOCKS.md` § S6, plus:

1. Your matching rule, stated exactly, with the over-fire and under-fire cases you tested.
2. The three-way disposition you chose for actors with no span / contradicting span / matching
   span, and why.
3. The G9 base-fail and tip-pass pastes, from the production entry point.
4. The labelled new-acceptance test, and why it is additional to the G9 proof rather than a
   substitute.
5. The non-vacuity result.
6. The exact `SEMANTIC_GATING_CHECKS` 4 → 5 delta and every pinned test it moved, one line
   each with the justification.
7. Confirmation that `test_compound_resolution_extraction`'s `GOLDEN` did **not** move, with
   evidence.
8. Anything you noticed and did not do.

# REV-112 — independent review of the C-112 residual sweep

- **Subject** `card/C-112-residual-sweep`, worktree `C:/t/c112`. Closes REV-109 R1/R2, F-157,
  C-108 R4, and two correctly-routed items.
- **You are the independent reviewer. You did not write this diff and you do not fix it.**
  If what remains is registerable, **say APPROVE and register it.**
- **Criteria fixed and committed BEFORE the diff existed.** Anything you add after seeing the diff
  must be **labelled as an addition**, so a reader can tell a pre-committed standard from a
  post-hoc one.

**Review the ACTUAL DIFF, not the report.** A report is a claim about a diff. Four corrections
came to the Lead in the previous wave and every one of them was right; reading a report is not
verification, and that applies to your own reports too.

---

## 0. What class of card this is, and what that forbids

C-112 is a `product_contract_violation` **of the sprint's own citation and evidence discipline**.
It is a documentation-and-instrumentation card.

**No biological gate is in scope and none may be touched.** If any hunk changes a cue, a contra, a
threshold, an admission rule, an export gate or a scorer verdict, that is an immediate **REJECT**
under merge rule 6 — regardless of how good the justification reads.

**`DECISIONS.md` is append-only and READ-ONLY to this card.** Any modification to an existing
`DECISIONS.md` line is a **REJECT**.

**`src/t2pw/app/streamlit_app.py` must be untouched**, still uncommitted at **35 insertions /
2 deletions**, `sha256:47e4fafa789d359d…`. Verify the hash yourself. This is the file whose
uncommitted diff *caused* F-157; a card that repairs the citation by disturbing the file has
inverted its own purpose.

---

## 1. The ten conditions — every one must hold

| # | Condition | How to verify |
|---|---|---|
| **W1** | **Every one of the 26 R1 citations is BUCKETED before it is touched** | The card must say, per citation, whether it is a **live citation** or a **frozen historical record**. A diff that edits 26 things without declaring which bucket each is in fails W1 even if every edit is individually defensible. The three named live ones are `FINDINGS.md:6679` (present tense — *"tells that agent to certify"*), `RESUME-NEXT-SESSION.md:94`, `TEST_MATRIX.md:533`. |
| **W2** | **Anchors, not renumbering** | C-109's own remedy. A corrected line number drifts again on the next insertion — **that is the entire finding**. Any live citation converted from `file.md:161` to `file.md:177` is a **REJECT of that hunk**: it re-creates the defect with a fresh number. Acceptable anchors: headings, unique strings, named symbols, test names. |
| **W3** | **Frozen historical records were LEFT ALONE** | A signed record of what a card measured at the time is not a citation to be maintained. Confirm no signed record was silently rewritten to resolve. **`TEST_MATRIX.md:726-727` is explicitly out of scope** — it is false, and it is C-106's signed record. If the diff touches it, **REJECT**. Annotation elsewhere is permitted; editing the record is not. |
| **W4** | **R2 vector 1 — recursion — is actually closed** | Probes in a subdirectory were never enumerated: the glob is non-recursive and `is_file()` drops the directory **silently**, so the check exits 0 with a green G11 while missing the evidence entirely. **This is the vector that matters.** Reproduce REV-109's demonstration (`evidence/rev109_probes/deep_probe.py`) at base and at tip. A loud WARN is the stated minimum; silent omission is not. |
| **W5** | **R2 vectors 2 and 3 are DECIDED AND DOCUMENTED, not silently kept** | (2) A zero-byte file always reports `reachable` — the empty blob oid is a universal constant, so a truncated or failed probe log resolves against any unrelated empty file. Either it becomes **its own verdict class** or the limitation is **recorded explicitly**. Silence is a fail. (3) `--allow-empty` plus a mistyped task id disarms the exit-3 protection beside it. The two must be made **independent**, or the combination **refused**. |
| **W6** | **Each R2 fix carries a make-it-fail / make-it-pass pair** | The standard REV-109 set proving B9 non-vacuous. **Both directions shown.** A test that passes at tip and was never observed to fail proves nothing about what it guards. Extended in `tests/test_c109_reviewer_evidence_route.py` — **a second test file is a fail of W6's form**, per the card. |
| **W7** | **F-157 cites the SYMBOL** | `FINDINGS.md` § F-153 must no longer cite `streamlit_app.py:5669`. The committed value is `:5636`; the `+33` is exactly the protected diff, so `:5669` was read off an uncommitted working copy and **resolves for no reader**. `run_rag_rounds` / `run_rag_loop` are identical in both trees and cannot drift. The standing rule — **never cite a line number in a file carrying an uncommitted diff** — must be added **where an agent will actually meet it**, not buried in a findings entry. |
| **W8** | **The two routed items are corrected as specified, and the correct one is untouched** | `FINDINGS.md:1125-1126` row E: `TEST_MATRIX.md:218` is the `child_env` row; **Chunk E is at `:237`** → convert to the `## Chunks` anchor. **Row D-qb's `chunk_d_gate.py:70` is CORRECT — if the diff "fixes" it, REJECT that hunk**; the author's routing was half-wrong in its own disfavour and the reviewer must not repeat the error in reverse. `TEST_MATRIX.md:568` (*"§ Chunks begins at line 209"*; it begins at `:228`) is **below the `:477` pin**, reads as historical rationale, and must be **annotated or struck — not silently restated**. `DECISIONS.md:3619` repeats the same 209 and must be **left, with the diff saying so**. |
| **W9** | **C-108 R4 — M16 re-pointed, harness re-run WHOLE** | M16's anchor occurred once at `f67e00a` and **zero times** post-C-108, because member (d) moved the six inhibition additions into `_C107_INHIBITION_WORDS_SRC`. `apply_mutation` raises on a 0-match → `ABORT`, return 3. Require: **M1–M16 all go RED**, and the target file is **byte-identical afterwards**. **No mutation weakened to make it match. M16 not deleted.** Verify item 5 was done **after** C-108 was on integration — its anchor's final bytes are C-108's. |
| **W10** | **The card measured the drift ITS OWN diff creates** | **This is the criterion C-109 was never given, and it is why this card exists.** A C-112 that fixes 26 stale citations and creates new ones has failed on its own terms. Require an explicit self-drift measurement with either elimination or a written report. **A card that closes a drift class while silently re-opening it is a REJECT.** |

---

## 2. `TEST_MATRIX.md` line-neutrality — prove it, do not accept it

The `:477` rule binds: any edit **at or above `:477`** must be **line-neutral** — rewritten in
place, never adding or removing a line — with longer records appended at end-of-file.

**Verify mechanically, do not read the claim:**

- identical line count above `:477`, and
- `:477` **byte-identical** before and after, and
- **no differing line anywhere in the common prefix.**

C-109 passed this as a pure append (728 → 809, `:477` byte-identical). C-112 edits *within* the
file, so it is the harder case and the one where the rule actually bites.

---

## 3. G9 — classify each item, and reject a mislabel

Per merge rule G9, and **per item**, not per card:

- **The R2 fixes are CORRECTIONS OF OBSERVABLE BEHAVIOUR.** Each needs a proof that **fails
  behaviourally at the C-112 base SHA** and passes at the tip. **Symbol absence is not proof.**
- **The citation work is correction of currently-false documentation.** Proved false-at-base and
  true-at-tip **against committed blobs — never a working tree.** This is the whole lesson of
  F-157: a claim checked against a working copy is a claim about bytes that exist in no commit.
- **A genuinely new capability** carries an **explicitly labelled new acceptance test** and needs
  no fabricated base failure.

**Mislabelling a regression as new functionality is a reject.** So is a "base failure" produced by
a probe that never reached the code — the Lead's own first C-108 probe used the wrong payload
envelope, returned all-permissive at base, and *looked* exactly like a finding. **Assert a
known-positive and a known-negative before trusting any harness the card hands you.**

---

## 4. Gates — at the tip, and re-verified by you

- **SMOKE 503 / 22 files.**
- **gold-readers 456 passed / 8 skipped / exit 0.**
- **`acceptance.py` hashed immediately before and after, byte-identical.** C-112 has no business
  changing the scorer; the hash is how that is proved rather than asserted.
- The route-check tests **split and combined**.

**If the F-150 gold correction has landed on integration by the time you review, the gold-readers
number is measured against the gold SHA in force at YOUR tip — state which gold SHA you measured
against.** A gold-reader count is meaningless without it.

**Note for W-gate arithmetic:** a card modifying a file the mutation harness guards must **COMMIT
before running SMOKE**, or `test_04_restore_is_byte_exact_on_the_real_mutated_module` fails on
`git status --porcelain` and presents as **502/1** — which reads exactly like a regression and is
not one. If you see 502/1, check for an uncommitted guarded file **before** calling it red.

---

## 5. Process — the same gates that bind every card

Everything through `evidence/bounded_run.py` with the explicit venv interpreter
(`<primary>/.venv/Scripts/python.exe` — **a bare `python` is system 3.13 with no `streamlit` and
yields 35 spurious import errors that read exactly like a regression**), a real `--timeout`,
`--basetemp` under `C:/t/` **with the parent pre-created**, `PYTHONPATH=<tree>/src`,
`T2PW_OFFLINE_CURATOR=1` **in the bounded child**, `PYTHONIOENCODING=utf-8`,
`--heavy-lock C-112`, and a **shape-validated** G11 path.

**`FINAL SURVIVING COUNT : 0` and `cleanup : success` on every job.** Exit 95 means the child never
started — an infrastructure event, not a result. **Never clear a lock you do not hold.** Never
`taskkill /IM python.exe`. Two `ms-python.isort` IDE processes exist and are **never** cleanup
targets — match on command line, never PID.

**F-160 binds you.** A green suite proves the bytecode that executed was green, not that the source
on disk is. **Purge only `src/t2pw` and `tests`** — an unscoped purge deletes **56 tracked `.pyc`
files**. Restore what you purge and verify clean before SMOKE.

**Ancestry, before anything is merged:**

```
git merge-base --is-ancestor <card-tip> <evidence-branch>      # must be YES
git diff --name-only <card-tip> <evidence-branch> -- src tests # must be EMPTY
```

Two of three reviewer evidence branches in the previous wave were rooted at round 0. Merging one
alone would have **silently reverted the correction rounds while looking like a routine evidence
merge.**

**Never commit:** the caches, `topics_*.txt`, `streamlit_app.py`, `cache_snapshot/`, the stray
`ValueError`, or anything under `out/`, `outputs/`, `tmp/`.

---

## 6. Stop conditions

- Any biological gate touched → **REJECT**, immediately.
- Any existing `DECISIONS.md` line modified → **REJECT**.
- `TEST_MATRIX.md:726-727` touched → **REJECT**.
- Row D-qb's `chunk_d_gate.py:70` "corrected" → **REJECT that hunk**; it was already right.
- A live citation renumbered rather than anchored → **REJECT that hunk**.
- Item 5 performed before C-108 was on integration.
- A mutation weakened, or M16 deleted, to make the harness green.
- `:477` line-neutrality claimed but not mechanically proved.
- The card's own drift unmeasured.

---

## 7. Verdict

**APPROVE** · **APPROVE WITH REGISTERED RESIDUALS** · **CHANGES REQUIRED** · **REJECT**

Two automatic correction rounds are available; a **third is an explicit authority decision** and
must come back to the Lead rather than being taken.

**If what remains is registerable, say APPROVE and register it.** Do not hold a sound card open for
a residual that belongs in the register.

**State separately, and do not merge them:** what you verified **on the diff**, and what you verified
**behaviourally**. A static read and a mutation-proved property are different epistemic objects, and
F-156 exists because the sprint once collapsed them.

**Raise, do not answer:** whether `c107_mutation_attack.py` should be gated. It is run by **no
gate** — SMOKE's `test_c106_mutation_harness_executable.py` targets `c102_mutation_attack.py` only,
which is why a harness abort survived a green 503. **That is a `TEST_MATRIX` change with its own
cost and it is not this card's to make.**

**Keep every failed measurement beside its correction.** This sprint has twice found a real defect
inside an anomaly someone nearly discarded.

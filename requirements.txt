# adding the required imports we will need, run from a virtual environment for ease of use 

#Core HTTP + CONFIG
requests>=2.32.3
python-dotenv>=1.0.1
pymysql>=1.1.1

# LLm - Client 
openai>=1.55.0

# data models and validation (JSON Contract)
pydantic>=2.9.2
jsonschema>=4.23.0

#pubmed/ XML parsing 
lxml>=5.3.0
python-libsbml>=5.20.4

#CLI lgging/ pretty debug info 

rich>=13.9.2

# utils
tqdm>=4.66.5

# testing and code qualtiy

pytest>=8.3.3
ruff>=0.8.4

# graphing visuals if we want them 
graphviz>=0.20.3
matplotlib>=3.9.2

# to get the output
streamlit>=1.30

# PDF parsing (pdf_parser.py)
pdfplumber>=0.11.0
pypdf>=4.0.0

# PDF OCR fallback (optional — requires tesseract system package)
# Install tesseract separately: apt-get install -y tesseract-ocr poppler-utils
pytesseract>=0.3.13
Pillow>=10.0.0

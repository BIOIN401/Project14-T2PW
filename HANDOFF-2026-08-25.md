# Handoff — the correction wave is merged, and the benchmark was measuring less than it claimed

Written 2026-08-25. Integration tip **`98c0458`** on `sprint/pwml-recovery`, pushed,
`local = origin = git ls-remote`. **Supersedes `HANDOFF-POST-T106.md`.**

**T-107 was NOT run. That is a deliberate decision, not an omission. See §4.**

---

## 0. Verify once, then work

| check | expected |
|---|---|
| tip | `98c0458`, local = origin = `git ls-remote` |
| merge in progress / staged | none / 0 |
| heavy lock `C:\t\heavylock` | **absent** |
| sprint-owned Python processes | **0** (two `ms-python.isort` `lsp_server.py` IDE servers — **never kill those**) |
| product-owner edit `src/t2pw/app/streamlit_app.py` | **35 ins / 2 del, uncommitted**, `sha256:47e4fafa789d359d8526642cd8e70bf968196a46cd8b02d069c6d76a3c5bb632` |
| SMOKE | **473** — measured by me after every merge, and independently by the reviewer at four SHAs |
| Chunk D | **187 = 160 + 4 + 23**, `SETS_EQUAL=True` |
| `TEST_MATRIX.md` | **578 lines** (unchanged — the §5 retraction is inline) |
| `data/*cache*.json` | modified, **never staged, never committed** — `git log e648287..HEAD -- data/*cache*.json` returns 0 |
| G11 | `check --task` clean on all 9 task ids, **144 reports, 0 non-compliant** |

---

## 1. What merged

| card | merge | closes | SMOKE after |
|---|---|---|---|
| **C-081** | `b869780` | **F-096, partially — 2 of 8** | 473 |
| **C-082** | `0242810` | **F-115 — closed** | 473 |
| **C-085** | `07db68f` | **F-121 — closed** | 473 |

Every one reviewed against the **actual diff** by an independent reviewer that re-measured SMOKE
itself on both sides. **C-084 was chartered and correctly produced no code** — see §3.

* **C-081** — a row declaring `class:"cofactor"` that no reaction or transport uses may not ship real
  accessions. **18 refusals, 0 collateral** over 89 committed artifacts; a schema-complete participant
  reader rescues **none** of the 18. `ATP` declares the same role ten times, is used every time, and
  is untouched. PASS C **must run last** — proven by a reorder mutant that goes red on C-073's *own*
  untouched assertion.
* **C-082** — the ambiguous species rename **declines** instead of killing the leg. Detection is
  byte-identical: the reviewer set-differenced the AST and found **zero** base expressions removed.
  Authority is **D-035 clause 8**. The base failure string is **byte-identical to the T-106 manifest
  row** (209 bytes, matching sha256).
* **C-085** — priority 2 reports `NOT EVALUATED` instead of a `PASS` it never earned.

---

## 2. THE HEADLINE: two of five priorities recorded as PASS were artifacts of one blind spot

**F-121.** `supported_reactions_complete` is absent from **all ten** gold cases, so it defaults
`False` everywhere. With `complete=False`, `semantic.py:706` deletes every
`unsupported_retained_reaction` finding, `:722` contributes a hard `0`, and `:708`'s `ok` clause is
unconditionally true. The only non-zero route is `max_retained_reactions`, set on exactly two cases —
**both negative controls**.

**Priority 2 had only ever measured negative-control ceilings**, on a priority the scorer calls
*absolute*.

Re-scoring T-106 offline at C-085's tip — nothing re-run — gives:

| priority | recorded | **honest** |
|---|---|---|
| 1 false real identifiers | FAIL 8 | FAIL 8 |
| 2 unsupported reactions | **PASS 0** | **NOT EVALUATED** (11 of 20 legs, 6 papers) |
| 3 referential violations | PASS 0 | **PASS 0 — verified real**, see below |
| 4 requested-pathway coverage | **PASS 1/8 = 12%** | **FAIL 0/8 = 0%** |
| 5 strict PWML | FAIL 0/4 | FAIL 0/4 |

The priority-4 flip is **not** a new rule: `SemanticReport.confirmed` is byte-identical at both SHAs
and already required evaluability. `check.ok` did not move; only `check.applicable` did. The
requirement finally had something to bite on.

**`TEST_MATRIX.md:482`'s claim that `PMC12421875` was "the first semantic confirmation of the sprint"
is RETRACTED in place** — that leg attributed 3 of 11 retained reactions and left 8 matching nothing.

**The official `t106_acceptance_report.txt` is preserved byte-identical.** The re-score is
`evidence/c085_t106_rescore.txt`.

**Priority 3 was checked and is CAPABLE, not inert.** `_orphaned_references` reads **no gold field at
all** and was demonstrated returning non-zero. Its `PASS 0` on T-106 is a real measurement. But it is
blind to `cargo`, `transporters` and `elements_with_states` — **F-125**, with 3 live invisible orphans
in the corpus including a leaked JSON pointer `/entities/proteins/0` in a name slot, and 0 exposure on
T-106.

---

## 3. Why T-107 was not run — three independent blockers

### 3a. Priority 1 cannot reach 0

C-081 took **2 of 8**. The other six were each measured out of reach:

* **`NADH` / `NAD+`** — **C-084 measured four formulations and implemented none.** Every non-vacuous
  one stripped `ATP`, `pyruvate` or `enterobactin`. The assay-marker rule is *vacuous* (the readout
  **is** a reaction, so a reaction-verb veto fires on every span); `NAD+` normalises to `nad`, which
  substring-matches **"Ca·nad·a"** in the affiliations. **Their real mechanism is not assay leakage** —
  see **F-118**.
* **`SREBF1/2`, `LIPA`, `LBR`** — textually indistinguishable from **ten gold-ALLOWED enzymes in the
  same sentence**. Needs curated biology, not a text or graph predicate. **Capability gap, not a code
  defect.**

**And the class is larger than eight.** The cohort surfaced **`(p)ppGpp`** on a paper that had zero
at T-106. Counting F-096 by any single run's membership will under-scope it.

### 3b. Priority 5 is 0/4 by proof, not estimate

A counterfactual replay of the real `classify_release_status` — control arm reproducing **9 of 9**
recorded statuses — forced every semantic check to `passed`. **Not one of the four legs moved.**

`release_status.py`'s caps are independent and each is guarded on `status == RELEASE_READY`, so
**whichever fires first hides the others**; `:701` labels the second *"A SECOND, INDEPENDENT"* cap.
Two papers are forbidden by **locked D-062** from ever being a strict success; `PMC12782028` is
correctly blocked at completeness 0.222 against an immovable 0.500 floor; `PMC12096016` is blocked
twice over, and the actor check that demotes it is a **true positive** (see **F-116**).

**Zero of the four are reachable by code that does not weaken a gate.**

### 3c. The instrument itself was wrong until C-085 merged

Running T-107 before C-085 would have produced another set of flattering, wrong numbers.

---

## 4. The decision bundle — `docs/pwml_recovery_sprint/DECISION-BUNDLE-POST-T106.md` (413 lines)

**Seven items. Item 1 is the T-107 blocker.** Nothing in it has been applied — the orchestrator does
not author `DECISIONS.md`, and no gold was edited.

| item | question | recommendation |
|---|---|---|
| **1** | F-107 / D-062 — what status does a defensible core stopped by a scope guard carry? Includes D-062's **explicitly-left-open** gold reconciliation. | **A** — add a fourth `PRODUCT_CONTRACT` §4 state and correct the two trap papers' `expected_export`. **Exact edit prepared, NOT applied.** |
| **2** | F-109 — `pytest.ini` carries a setting rule 10 refuses | keep it; amend rule 10 to record the pin as the mitigation |
| **3** | F-113 — the 2026-08-23 identity ruling has no `D-xxx` | **exact paste-ready text supplied** |
| **4** | F-121 — should any gold case set `supported_reactions_complete`? | **C** — extend outward from the two papers with ceilings. **Setting it `true` without exhaustive signatures would report 227 fabricated reactions. It is one boolean away.** |
| **5** | F-123 — a declination does not demote the release status | fold into item 1; same question |
| **6** | C-081's scope note — an interaction endpoint confers no participant role | ratify as written |
| **7** | F-119 + F-125 — two participant readers narrower than the schema | needs an owner, not a ruling |

---

## 5. The validation cohort — `runs_verify/2026-08-25_1216`, 4 legs, 1.96 h, **$0.177**

Heavy lock acquired **and released**; zero survivors; cleanup success.

* **C-081 VALIDATED live.** `pyridoxal 5-phosphate` ships `mapped_ids: {}` with nine accessions filed
  in `rejected_mapped_ids` — including the `drugbank:DB00114` that was T-106's false identifier — and
  **the row is preserved**. PMC12856317 went **2 → 0** false real identifiers.
* **C-081 anti-collateral VALIDATED.** Rule fired 0 times on PMC12444477; all 8 protein rows kept
  their identifiers.
* **C-082 INCONCLUSIVE — do not record it as validated.** `AMBIGUOUS_RENAME_TARGET` appears **0
  times**; the condition did not recur. The leg completing is *not* evidence. **I did not re-run to
  chase the shape** — that is the move §9 forbids. Its behavioural proof stands.
* **C-085 confirmed live in production scoring.**
* **Three legs went PASS → FAIL; none is attributable to a merged card.** Each was traced first:
  a component-less `LONP1` complex (**F-116's class**); a Stage-1 `identical_empty_response`; and a
  draw that extracted a different protein set. **Re-run a leg before calling any of these a
  regression.**

---

## 6. Findings registered this session

| id | sev | what |
|---|---|---|
| **F-116** | HIGH | `_rewrite_reaction_protein_enzymes_to_complexes` resolves `EntE` onto **superset** PathBank complex 3623, injecting 3 catalysts that do not perform EC 6.2.1.71. **3 of 6 mapped legs.** |
| **F-117** | LOW | the actor gate cannot relate a DB canonical name to a bare gene symbol |
| **F-118** | HIGH | an inferred reaction carries inferred cosubstrates on an enzyme-name-only evidence span. **Two routes** — the Stage-1 route fired on the two runs *before* T-106, so a merge-dedup card alone will be reopened by draw variance |
| **F-119** | MED | `_PARTICIPANT_NAME_KEYS` narrower than the schema; reach **0**, but `{"protein":…}` is dominant (1,820 vs 615) |
| **F-120** | LOW | C-081's anti-collateral arm pins a floor, and its allowlist is paper-blind |
| **F-121** | **CRIT** | priority 2's PASS was vacuous — **closed by C-085** |
| **F-122** | LOW | one C-082 base failure is symbol absence |
| **F-123** | MED | a declination does not demote the release status |
| **F-124** | LOW | the claimed `DuplicateNamedRowError` backstop **does not exist for species** (measured unreachable) |
| **F-125** | MED | priority 3 blind to every transport-side participant |
| **F-126** | LOW | the new priority-2 PASS route inherits matcher precision, never measured |

**F-110 was MEASURED NON-REACHABLE and needs no card** — zero occurrences of
`no_shared_meaningful_token` across the whole T-106 directory. **But it becomes live the moment
item 1 option B drives scope-conflict legs into mapping** — `PMC12312563` carries `Mg2+` in both modes.

---

## 7. Ready to dispatch

* **`prompts/C-083.md`** — F-092 defect 3, **written and committed, never dispatched.** Its fix
  necessarily moves a pinned golden baseline; deleting the pin instead of re-baselining is an auto
  reject. **Its two sibling claims are REFUTED and must stay refuted.**
* **Next F-096 card** must be scoped to **F-118's two routes**, with lineage instrumentation as a
  dependency — the bio-auditor disqualified span-containment (flags **355 of 971 slots**, including
  `pyruvate` and `ATP`).
* **F-119 + F-125 together** — same defect shape; `canonical.py:330` already holds the correct key list.

---

## 8. Traps paid for this session — do not pay again

* **A single entry in `semantic_failed_checks` does NOT mean a single blocker.** The caps are
  independent and each is guarded on `status == RELEASE_READY`, so the first hides the rest. **I got
  this wrong and recorded a ceiling of 1/4; it is 0/4.** Corrected in the LEDGER rather than amended.
* **`_requested_scope`'s `pinned` is a HOMONYM** — `driver.py:517` sets `pinned = not pathway`,
  meaning *"no topic line"*, **not** the gold's `[pinned_override]`. `scope_conflict.json` recording
  `false` is **correct**. Do not "fix" it.
* **The Stage-0 abort never replaced an annotate-only mode** — born at `True` in `15a7998`,
  2026-07-29, before the branch point.
* **`actor_named_in_its_own_cited_span` is a *bench* check** (`semantic.py:112`), **not** F-110.
  F-110 is `map_ids._enforce_shipped_identity_names` at `:5413`.
* **`tests/test_streamlit_quarantine_boundary.py` IS the `qb` component**, `ENFORCED` at 23. Appending
  a test there moves Chunk D 187 → 188 and needs authorization.
* **The monolithic form of that file hangs.** Use the sanctioned split. A wrapper timeout with
  `exit_code: None` is an **infrastructure failure, not a test result**.
* **Two new `.staging` reservations** — `C-082/49-smoke-tip`, `REV-085/14-cli-both` — join the 11
  historical ones. **13 total, all F-071's intended visible residue. Do not delete them.**

---

## 9. Cost

**$0.177 for the entire session** — one 4-leg cohort. Everything else (three merges, four reviews,
two adjudications, eleven findings, a 413-line decision bundle) cost **zero credits**. Usage
`161.0925 → 161.2698`; `limit_remaining` **$69.26**.
